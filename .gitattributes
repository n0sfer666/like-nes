# Windows-раннер GitHub держит core.autocrlf=true, а шелл там — git-bash. Скрипт, выехавший из
# checkout с CRLF, умирает на собственном шебанге (`#!/usr/bin/env bash\r`) — отказ читается как
# «bash: no such file or directory» и стоит полного круга CI. Нормализация только для текстовых
# гейт-скриптов: бандлы и бейкнутые ассеты обязаны остаться байт-в-байт (детерминизм golden).
*.sh text eol=lf
*.py text eol=lf
# И ровно наоборот для cmd: интерпретатор здесь — сам Windows, и на файле с одними LF он теряет
# последнюю строку блока и метки `goto`. Единственный файл дерева, которому нужен CRLF.
*.bat text eol=crlf
# Исходники материалов (спека #18) — тот же класс, но по другой причине: их текст едет В БАНДЛ, а
# `library.bundle` сверяется с перепечённым БАЙТ В БАЙТ на трёх ОС. Модуль шейдера кладётся в ассет
# как есть, поэтому checkout с CRLF даёт 6192 байта вместо 6080 — ровно по одному лишнему байту на
# строку, — и гейт краснеет на windows-latest, ничего не говоря про сами материалы (прогон 192ed67).
*.wgsl text eol=lf
*.mat text eol=lf
# Шаблоны configure_file — тот же класс, но следствие тоньше: configure_file переводы строк ВХОДА
# сохраняет, поэтому шаблон, выехавший из checkout с CRLF, даёт version.txt с `\r` в конце строк, и
# якорный греп гейта (`^target …$`) упирается в него на единственной ОС, где гейт и должен ловить
# чужое имя рантайма. NEWLINE_STYLE LF в install_engine.cmake закрывает ту же дыру со стороны CMake:
# два замка потому, что шаблон читают ДВА потребителя (движок и packaging игры-образца).
*.in text eol=lf
