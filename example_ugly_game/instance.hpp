#pragma once
#include <cstdint>

// Путём ОТ ЭТОГО ФАЙЛА, а не через каталог цели: у заголовка движка то же имя, каталог образца
// стоит в списке -I первым, и `<instance.hpp>` нашёл бы этот же файл — `#pragma once` сделал бы
// включение пустым, а ошибка вылезла бы у третьего файла, ничего про материалы не знающего.
#include "../engine/material/instance.hpp"

namespace game {

// Одна запись вершинного буфера — та же структура, которой рисует библиотека эффектов (спека #18).
// Своей копии у игры больше нет намеренно: вершинный этап у библиотеки ОДИН на обоих потребителей,
// а вершинный этап есть раскладка этой структуры. Разойдись они — материал рисовался бы верно
// ровно у одного из них, и увидеть это можно было бы только глазами на кадре.
//
// Блок параметров едет вместе с инстансом, а не юниформом: смена значения обязана остаться сменой
// ДАННЫХ, иначе два спрайта с разной силой вспышки разъезжаются по двум вызовам отрисовки
// (инвариант 2 спеки #18).
using Instance = mat::Instance;

constexpr uint32_t MAX_INSTANCES = 2048;   // S9: + частицы

} // namespace game
