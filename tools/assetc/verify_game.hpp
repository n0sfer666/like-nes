#pragma once

#include <string>

// Сверка испечённого `game.bundle` с исходниками, из которых он обязан быть собран.
//
// Бандл игры-образца лежит в git готовым, а перепечь его целиком нигде, кроме машины владельца,
// нельзя: текстурная секция тянет `basisu` и `tint`, которых на раннерах нет. До этой проверки
// правка `input.txt` или `achievements.txt` без перепекания проезжала молча — бандл принимался на
// веру, хотя на нём стоят и игра-образец, и sim-golden.
//
// Сверяются секции, которые пекутся ЧИСТЫМИ парсерами, без внешнего тулчейна. Текстура остаётся за
// ручной сверкой `bundle_hash` (пункт F в docs/owner-setup.txt) — здесь она недостижима.
namespace asset {

bool verify_game_bundle(const std::string& src_dir, const std::string& bundle_path);

} // namespace asset
