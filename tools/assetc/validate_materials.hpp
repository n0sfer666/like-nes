#pragma once

#include <string>
#include <vector>

#include "bundle_writer.hpp"

namespace asset {

// Кросс-бэкендная валидация библиотеки эффектов (гейт 2 спеки #18).
//
// Проверяются НЕ файлы на диске, а секции, которые уже легли в бандл: между текстом исходника и
// байтами секции стоит пекарь (сворачивание CRLF, терминатор строки), и валидировать вход, а
// класть в бандл выход значило бы проверять не то, что поедет в игру.
//
// false — хоть один доступный бэкенд отверг библиотеку; диагностики к этому моменту уже
// напечатаны в stderr в формате панели #7.
bool validate_materials(const std::vector<AssetInput>& assets, const std::string& wgsl_path);

} // namespace asset
