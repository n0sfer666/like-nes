#pragma once
#include "scene.hpp"
#include <cstdint>
#include <string>

// Детерминированная текст-сериализация сцены (гейт 1). Формат — строковый конверт вокруг
// meta-driven flecs-JSON значений компонентов:
//   # like-nes scene v1
//   E <guid>
//   C <ComponentName> <flecs-json-одна-строка>
// Сущности по GUID (std::map), компоненты в фикс. алфавитном порядке → байт-детерминизм.
// Значения — через flecs meta (to_json/from_json), НЕ per-component hand-code (одна рефлексия).
namespace ide {

std::string serialize(const Scene& s);

// ЕДИНЫЙ список причин отказа — и для `deserialize`, и для `restore_entity`. Держится он здесь, а
// не в трёх файлах врозь: прежде счёт причин был переписан прозой в пяти местах (заголовок гейта,
// шапки двух .cpp, спека, заметка аудита), и сверить их между собой было нечем — новая причина
// добавлялась туда, куда вспомнилось (ревью аудита #21, A·2·8). Порядок — порядок проверок.
//
//  1. `not a scene file`                   — первая непустая строка не шапка формата
//  2. `unsupported format version`         — шапка та, версия чужая
//  3. `entity id is not a number`          — не все символы цифры
//  4. `entity id has a leading zero`       — цифры все, но `E 007`: свой номер у своего текста
//  5. `entity id does not fit`             — цифры все, но число больше uint64_t
//  6. `duplicate entity id`                — номер уже встречался в этом тексте
//  7. `unrecognized line`                  — строка не `#`, не `E `, не `C `
//  8. `component before any entity`        — строка `C ` до первой `E`
//  9. `component line has no value`        — `C Position`: значения нет
// 10. `component line has no name`         — `C  {…}`: имя пустое
// 11. `duplicate component`                — это имя уже было у ЭТОЙ сущности
// 12. `unknown component`                  — имя не из таблицы сборки
// 13. `bad json`                           — значение не разобралось как JSON
// 14. `value is not what the format writes` — разобралось, но обратная запись даёт другой текст
//
// Загрузка ОТКАЗЫВАЕТ целиком, `*why` = «line N: entity G, component C: …». Битая строка молча
// пропускалась, и её компонент оставался нулевым — то есть позиция (0,0) была неотличима от
// прочитанной, а первое сохранение закрепляло потерю. Контракт отказа двухуровневый, и записан он
// так, потому что уровни эти разные: отказ ПЕРВОГО прохода (по нему отказывает всякий битый файл)
// оставляет сцену вызывающего НЕ ТРОНУТОЙ; отказ второго недостижим — разбор детерминирован, а
// первый уже прошёл — и оставил бы сцену ПУСТОЙ. Инвариант, за который тут держатся, — «пусто или
// целое»: полусцены не бывает ни на одном исходе (ревью аудита #21, A·2·8).
// Причины 1 и 2 — только здесь: у тела снимка undo шапки нет и быть не должно.
[[nodiscard]] bool deserialize(Scene& s, const std::string& text, std::string* why = nullptr);

// Golden bake-hash: FNV-1a над канонич. текстом. Текст = ASCII + целочисл. fix32-raw →
// run-to-run + cross-machine стабилен (тот же паттерн, что asset/audio/input/plugin golden).
uint64_t golden_hash(const Scene& s);

// Шапка формата — ОДНОЙ константой на писателя и читателя. Прежде её текст был написан руками
// тремя несвязанными копиями (запись, точное сравнение, префикс версии), и сверить их между собой
// было нечем — та же болезнь, против которой заведён список причин выше (ревью аудита #21, A·2·8).
// Префикс отдельной строкой, а не `substr`: он кончается ПРОБЕЛОМ, и вычислять его из шапки
// значило бы кодировать «версия — последнее слово» ещё одним местом.
inline constexpr const char* SCENE_HEADER = "# like-nes scene v1";
inline constexpr const char* SCENE_HEADER_PREFIX = "# like-nes scene ";

// Per-entity snapshot (только `C ...`-строки, без `E`). Для undo destroy_entity — восстановить
// сущность с теми же компонентами. Переиспользует ту же meta-рефлексию.
std::string serialize_entity(const Scene& s, uint64_t guid);

// Тот же отказ, причины 7 и 9–14 из списка выше (причина 8 недостижима: сущность создаётся до
// цикла). Уровни те же, что у `deserialize`: отказ ПЕРВОГО прохода оставляет сцену НЕ ТРОНУТОЙ —
// ни полусобранной сущности, ни стёртой прежней, занятый guid переживает битое тело целиком; отказ
// второго недостижим и снёс бы guid (`load_body` начинает с `create`, а тот зовёт `destruct()`
// на прежней сущности, и вернуть её уже нечем). Двойной проход — решение владельца. Префикс причины другой — `snapshot line N`: номер считается по телу снимка, а не по
// файлу сцены. `[[nodiscard]]` у обеих: выброшенный отказ — это история, шагнувшая вперёд по
// несостоявшемуся undo, ровно то, ради чего заводился возврат (ревью аудита #21, A·2·8).
[[nodiscard]] bool restore_entity(Scene& s, uint64_t guid, const std::string& body,
                                  std::string* why = nullptr);

} // namespace ide
