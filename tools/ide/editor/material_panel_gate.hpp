#pragma once
#include <string>

struct GpuContext;

namespace ide::editor {

// Гейт 3 спеки #18, половина «в редакторе»: правка `.wgsl` перерисовывает превью панели, а битая
// правка оставляет на экране ПРЕЖНЮЮ картинку и называет причину. Отдельный проход, а не человек
// у окна: «картинка не изменилась» глазами неотличимо от «панель мертва», и различает их только
// верная правка перед битой — то есть последовательность, которую надо гонять целиком.
int run_gate3(GpuContext& gpu, const std::string& library_dir, const std::string& work);

} // namespace ide::editor
