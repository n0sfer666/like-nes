#pragma once

#include "table.hpp"

// Шов «`library.mat` и ЧУЖОЙ потребитель», отделённый от шва «библиотека и её собственный шейдер».
// Граница по предмету: смещения параметров читает `sprite_effects.wgsl` в этой же подсистеме, а
// номера текстурных слотов — проход освещения и проход теней в render, то есть код, который про
// материалы знает только через таблицу. Возвращает число находок: печатает их сам.
namespace matlib {

int check_texture_slots(const mat::Table& t);

} // namespace matlib
