// Проба канонизации путей (аудит #21 A·3·7): печатает свой __FILE__ — ровно то, что компилятор
// вшил в бинарь. scripts/check_path_map.sh судит по ней, что абсолютный путь дерева в продукт не
// попадает: код движка в Release __FILE__ не зовёт, и без пробы проверка была бы зелёной даром.
#include <cstdio>

int main() {
    std::puts(__FILE__);
    return 0;
}
