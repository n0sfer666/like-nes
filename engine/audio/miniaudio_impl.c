/* Реализация miniaudio (AudioDevice HAL-бэкенд). Один TU; device.cpp включает только
   объявления (miniaudio.h без MINIAUDIO_IMPLEMENTATION). */
#define MINIAUDIO_IMPLEMENTATION
#include "miniaudio.h"
