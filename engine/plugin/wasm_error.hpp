#pragma once
#include <wasmtime.h>
#include <string>

// Поглощение диагностики wasmtime: и ошибка ХОЗЯИНА (`wasmtime_error_t`), и трап ГОСТЯ
// (`wasm_trap_t`) приезжают владеющими указателями, которые обязан освободить вызывающий. Текст —
// единственная улика на гейте: выбрось её, и красная строка скажет «NO», не назвав, ЧТО именно
// отбило. Своим файлом, а не в шве песочницы: постройка store и чтение чужих ошибок — разные
// предметы, и шапка соседа перечисляет только первый (аудит #21, ревью A·2).
std::string take_error(wasmtime_error_t* e, wasm_trap_t* t);
