#pragma once

// WGSL прохода освещения по ТАБЛИЦЕ (гейт 7 спеки #18, вертикаль 3). Отдельным файлом от
// `shaders.hpp`: тот держит модуль PoC-тракта с `array<vec4f, 6>`, где число источников зашито в
// тип, — а весь смысл этого прохода в том, что числа источников в коде нет вовсе.
const char* light_pass_wgsl();
