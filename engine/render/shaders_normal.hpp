#pragma once

// WGSL прохода нормалей (шаг B гейта 7, спека #18). Отдельным файлом по той же причине, что и
// `shaders_light`: исходник шейдера — данные, а не логика, и мешать его с созданием пайплайна
// значит прятать две ответственности в одном файле.
const char* normal_pass_wgsl();
