build/
build*/
**/build/
**/build*/
dist/
stage/
vdf/
**/dist/
.DS_Store
*.o
*.a
*.dylib
*.dll
*.so
compile_commands.json
__pycache__/
.cache/
deps/
imgui.ini
.serena/

*_san
*_tsan
*.bundle
!**/assets/**/*.bundle
audio_seam.wav
*.save
*.save.tmp
*.save.ready
demo_frames/
юникод тест-каталог/
/*.txt
!/CMakeLists.txt
!/THIRD-PARTY-NOTICES*.txt

# Состояния гейта, которые читает pre-commit hook. Лежали в .git/info/exclude, то есть в ОДНОМ
# клоне: на второй машине владельца те же два файла всплывали бы untracked-шумом ровно там, где
# перед коммитом смотрят `git status` на предмет чужих правок.
.claude/.verify-state.json
.claude/.review-state.json
