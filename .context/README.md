# like-nes — 2D game engine

Профессиональный 2D-игровой движок с приоритетом на оптимизацию (RAM/диск/CPU/GPU),
заложенную в архитектуру. Не «самый простой для старта», а инструмент для серьёзных
indie-продуктов уровня Hollow Knight. Только движок — «завод по сборке игры», без
встроенных редакторов музыки/картинок.

Полный оригинальный бриф: [`notes/original-brief.md`](notes/original-brief.md).

## Столпы

1. Оптимизация (приоритет №1) — в архитектуре, без «танцев с бубном».
2. Кроссплатформенность: desktop + mobile на старте, консоли позже.
3. Минималистичный IDE (UI/UX первично).
4. Устройства ввода из коробки (kbd/mouse/touch/gamepad).
5. Серьёзная 2D-графика и спецэффекты.
6. Расширяемость (плагины + конфигурируемый интерфейс).
7. Один язык логики, обусловленный оптимизацией.
8. Лицензирование: полностью свободный движок (MIT OR Apache-2.0, без royalty).
9. Система достижений (интеграция Steam и др.).
10. Учёт «боли» 2D-разработчиков.

## Roadmap спек (черновой, ≥10 маленьких)

| # | Спека | Статус |
|---|---|---|
| 1 | Выбор языка + архитектура ядра | **Закрыта** (C++, PoC validated, CI 3 ОС) — [`specs/2026-07-18-language-and-core.md`](specs/2026-07-18-language-and-core.md) |
| 2 | Рендер-пайплайн и спецэффекты (2D, Hollow-Knight-уровень) | **Закрыта** (ADR Accepted, render-вертикаль PoC validated T4) — [`specs/2026-07-18-render-pipeline.md`](specs/2026-07-18-render-pipeline.md) |
| 3 | Аудио-подсистема | **Закрыта** (ADR 0004 Accepted, audio-вертикаль PoC validated T4: byte-golden+block-независимый микс / нет sim-readback+TSan / RT-safe callback+ASan / шов asset→audio real vorbis / hybrid fix32-детерм.) — [`specs/2026-07-19-audio-subsystem.md`](specs/2026-07-19-audio-subsystem.md) |
| 4 | Система ввода (все устройства) | **Закрыта** (ADR 0005 Accepted, input-вертикаль PoC validated T4: record→replay sim-hash+poll-rate-независимость / нет-гонки+TSan / RT-safe+force-release / action-map full / live owner-HW: kbd+mouse+трекпад+Xbox pad+hotplug+rumble) — [`specs/2026-07-20-input-system.md`](specs/2026-07-20-input-system.md) |
| 5 | Ассет-пайплайн (формат, компрессия, стриминг) | **Закрыта** (ADR 0003 Accepted, asset-вертикаль PoC validated T4: байт-golden bake / zero-copy+ASan / детерм.-гейт / шов asset→render / hot-reload) — [`specs/2026-07-19-asset-pipeline.md`](specs/2026-07-19-asset-pipeline.md) |
| 6 | Система плагинов + конфигурируемый интерфейс | **Закрыта** (ADR 0006 Accepted, plugin-вертикаль PoC validated T4: детерм.-реестр/топосорт (native≡WASM golden `0x7d9a6e60`) / крэш-изоляция+hot-reload+re-probe / 6 ext-point + asset-codec seam / WASM-sandbox wasmtime (OOB+cap escape→trap) / манифест→докируемые ImGui-панели live-owner) — [`specs/2026-07-21-plugin-system.md`](specs/2026-07-21-plugin-system.md) |
| 7 | IDE: UI/UX и архитектура редактора | **Закрыта** (ADR 0007 Accepted, IDE-вертикаль PoC validated T4, все 8 гейтов: scene round-trip (golden `0x2de54a36`) / undo-redo+группировка / Play-spawn+read-only shmem-зеркало 10k / крэш-изоляция / build-loop+диагностики / перф 10k zero-alloc / **IPC-замер shmem ~17×** / live UI owner-HW: вьюпорт+гизмо+property-grid из **flecs meta** (одна рефлексия=save/load+зеркало+инспектор), оба shell'а на **WebGPU**) — [`specs/2026-07-21-ide-editor.md`](specs/2026-07-21-ide-editor.md) · [`decisions/2026-07-21-ide-editor.md`](decisions/2026-07-21-ide-editor.md) |
| 8 | Билд/деплой и кросс-компиляция | **Закрыта** (ADR 0008 Accepted, build/deploy-вертикаль PoC validated T4, все 7 гейтов + CI 3 ОС: воспр. билд P0–P2+SHA-пин (macOS 181 / Linux 183 арт. 0 diffs) / шов assetc→бандл BC7 / кросс-компиляция native-matrix (wall=max) + mobile true-cross (iOS-sim+Android NDK, wgpu-native из Rust) / бандл `.app`+tarball+папка самодостаточны / release tag→matrix→VDF+gated steamcmd (rc3-прогон) / **игра-образец** sidescroller-shooter (бой/босс/сюжет/частицы/bloom/аудио, golden `0x32a094e89eacf2f2`) live macOS / **mobile runnable** — полная игра на iOS-симуляторе+Android-эму (реальный тач). Follow-up gated: Steam-заливка/notarization/mobile-подпись) — [`specs/2026-07-23-build-deploy.md`](specs/2026-07-23-build-deploy.md) · [`decisions/2026-07-23-build-deploy.md`](decisions/2026-07-23-build-deploy.md) |
| 9 | Лицензирование (free & open source) | **Закрыта** (ADR 0009 Accepted, спека Validated T3, все 4 гейта: **отмена royalty/friendship** → дуальная `MIT OR Apache-2.0` / инвентарь 14 C/C++-компонентов + 138 Rust-крейтов wgpu-native по факту (все permissive, армы дуальных элегированы явно: zstd→BSD-3-Clause) + **дословные нотисы** в `THIRD-PARTY-NOTICES.txt`, сгенерированном `THIRD-PARTY-NOTICES-RUST.txt` и `THIRD-PARTY-NOTICES-NDK.txt` (инвентарь ≠ notice-retention; границы покрытия записаны явно) / DCO в CI на git-трейлерах (автор **или** коммиттер, cutoff `DCO_SINCE`) / 7 файлов лицензий во всех трёх бандлах (desktop/iOS/APK — проверено сборкой каждого) стоковой сборкой + `FATAL_ERROR` при пропаже / атрибуция из `license.hpp` live macOS; sim-golden `0x32a094e89eacf2f2` цел. Follow-up: юр. ревью перед публичным релизом, per-file SPDX) — [`specs/2026-07-25-licensing.md`](specs/2026-07-25-licensing.md) · [`decisions/2026-07-25-licensing.md`](decisions/2026-07-25-licensing.md) |
| 10 | Система достижений (Steam+) | **Закрыта** (ADR 0010 Accepted, спека Validated T4, все 7 гейтов: детерм. ядро-**наблюдатель** (golden `0xe728fef199e87fc9`) / идемпотентный анлок / крэш-during-write 12/12 валидных снимков / авторинг данными `assetc --game` → zero-parse таблица в бандле #5 + рантайм-`define()`, дубликат id = hard error, 890 бит-флипов отбито / плагинный шов `EXT_ACHIEVEMENT_BACKEND` (**ABI 1 → 2**) + оффлайн-очередь / Steam-адаптер плагином на контракт-стабе `ISteamUserStats` (SDK не вендорится) + реконсиляция union / **регресс #8: `0x32a094e89eacf2f2` bare = observer = observer+плагин**, live macOS с тостом. Follow-up: живой прогон с настоящим SDK (процедура в спеке), Windows-путь бандла, мобильные адаптеры) — [`specs/2026-07-25-achievements.md`](specs/2026-07-25-achievements.md) · [`decisions/2026-07-25-achievements.md`](decisions/2026-07-25-achievements.md) |
| 11 | Реорганизация репозитория (движок ≠ игра-образец) | **Закрыта** (ADR 0011 Accepted, спека Validated T3, все 7 гейтов: дерево `engine/` + `tools/` + `example_ugly_game/` + `platform/`, имя `poc` исчезло / **8 golden-хешей целы бит-в-бит** (`0xa213fe65…` ядро — перепечатан в #15 на `0x6c4b121d…`, см. ниже, `0xf2255dc7…` asset, `0x2cf5b559…` audio, `0x4c6429af…` input — там же на `0xcc26a189…`, `0x7ad0493f…` plugin — там же на `0x7d9a6e60…`, `0x2de54a36…` IDE, `0x32a094e8…` sim, `0xe728fef1…` ach) + `compile_commands.json` **336↔336 TU, 0 расхождений** / CI зелёный на 3 ОС, инвентарь шагов **52=52**, наборы skipped идентичны / корень чист, `git status` пуст после гейтов+паковки+релизного dry-run / граница движок→игра машинно проверена по CMake-графу / `.app` с 7 лицензиями стартует из чужого cwd, sha256 `game.bundle` цел / `release.yml` dry-run. CMake 644 → 142 + 13 per-module. Гейт 3 вскрыл порядко-зависимый флак резолва артефактов, невидимый локально. Долг в ADR: ребро `engine→game` в `plugin_host_test.cpp`, `PUBLIC`-экспорт каталогов — оба в #20) — [`specs/2026-07-26-repo-layout.md`](specs/2026-07-26-repo-layout.md) · [`decisions/2026-07-26-repo-layout.md`](decisions/2026-07-26-repo-layout.md) |
| 12 | Windows-паритет ядра движка | **Закрыта** (ADR 0012 **Accepted** по зелёному прогону [30239283418](https://github.com/n0sfer666/like-nes/actions/runs/30239283418) на коммите `991d1b1`, спека Validated T4, 9 гейтов: шесть швов в `engine/platform/` (`platform_io` / `platform_fs` / `platform_path` / `platform_process` / `platform_module` / `platform_guard`), переключатель — CMake, **ни одного OS-`#ifdef` вне шва** (греп-гейт в CI до сборки) / ассет+аудио+плагины+hot-reload+игра собираются на 3 ОС, все 9 golden-хешей обязаны совпасть / `fork` заменён на `platform::Child` (перезапуск себя через `exe_path`) — kill-during-write больше не POSIX-only / ASan clang-cl отдельным build-каталогом (UBSan под MSVC-таргет рантайма не имеет — явная разница покрытия) / юникод+пробел в пути: бейк, mmap и respawn из «юникод тест-каталог» / гейты 2 и 6 — машинный суррогат (`ma_backend_null`, headless `--demo` с проверкой, что кадры **меняются**), сенсорика — owner-HW follow-up. **Windows-половина писалась вслепую и локально не компилировалась ни разу**, статус держался в Proposed до зелёного раннера — цена разрыва: шесть красных прогонов подряд. С 2026-08-08 разрыва нет: владелец гоняет Windows руками через `win-dev.bat`, раннер стал вторым мнением) — [`specs/2026-07-26-windows-core-parity.md`](specs/2026-07-26-windows-core-parity.md) · [`decisions/2026-07-26-windows-core-parity.md`](decisions/2026-07-26-windows-core-parity.md) |
| 13 | Паритет среды разработки на трёх десктопных ОС | **Закрыта** (ADR 0013 Accepted; гейты 1–5 и 7 зелёные на 3 ОС: shmem-зеркало, Play-spawn, build-loop с живым `cl.exe`, диагностики MSVC, перф, кросс-ОС сцена. Единственный `if(NOT WIN32)` в дереве — `ide_ipc_bench`, постоянная граница. Гейты 6 (X11+Wayland) и 8 (сквозной сценарий) закрыты разработчиком на живых сессиях 2026-08-05/06, коммит `0e4294c`) — [`specs/2026-07-26-desktop-dev-parity.md`](specs/2026-07-26-desktop-dev-parity.md) · [`decisions/2026-07-27-desktop-dev-parity.md`](decisions/2026-07-27-desktop-dev-parity.md) |
| 14 | Каркас gameplay-фреймворка + ввод из коробки | **Закрыта** (ADR 0014 Accepted, спека T4, гейты 1–7 машинно: `Schedule` с детерминированным порядком / fix32-форма стика (радиальная и по-осевая мёртвая зона, кривые, триггеры) / пресеты и профили падов — испечённые zero-parse данные в `game.bundle`, golden разрешения ввода `0x416969081dcf230d` на 3 ОС / профиль пада по VID/PID из нового шва HAL `pad_info`, неизвестный → generic, hotplug / перебинды накладкой по ИМЕНИ действия с конфликтом и force, транзакционная запись профиля / игра-образец переведена на пресеты, sim-golden `0x32a094e8…` цел / направление зависимостей подсистемы→слой запрещено грепом в CI / 0 аллокаций в тике. **Гейт 8 (owner-HW) закрыт 2026-08-07** на macOS, Linux (evdev, `pid=0b12`) и Windows (XInput, `pid=02ff`): профиль по паспорту, знаки всех четырёх направлений стика по контракту, перебинд с конфликтом и force, накладка через рестарт, hotplug, игра пройдена с пада — протокол в `docs/owner-verification.md` §3. Живой прогон снёс две редакции свидетеля осей, прошедшие синтетику. Долг в ADR: `write_atomic` продублирован с достижениями; раскладка игры не биндит `mouseaxis:` — трекпад не двигает корабль) — [`specs/2026-07-26-framework-input.md`](specs/2026-07-26-framework-input.md) · [`decisions/2026-07-28-framework-input.md`](decisions/2026-07-28-framework-input.md) |
| 15 | Детерминированная 2D-физика — ядро | **Закрыта** (ADR 0015 Accepted 2026-08-22 по прогонам владельца на живых Linux и Windows; заявлено 350 тел при 16 итерациях — 21.4% кадра на самой медленной машине, число назначено ЗАМЕРОМ и снизило прежние 500, стоившие там 58%. Вертикали 1–3; вертикаль 1 — тела/круг/AABB, sweep-and-prune, импульсный решатель с фиксированным числом итераций; вертикаль 2 — одна форма на четыре типа (выпуклое ядро + радиус скругления), вращение на СВОЕЙ целочисленной таблице sin/cos (угол в оборотах: приведение к периоду точно до бита, libm на пути состояния нет), SAT с отсечением ради ДВУХ точек манифольда, тёплый старт с переносом по id точки. Гейты 1, 2, 6, 7, 9, 10 закрыты машинно: golden состояния `0xf238d0bc34325db6` (перепечатан в раунде #16 из `0xe243e1457a11f65e`: старое значение пинило дефект нормали длинной грани, разбор — решение 8 ADR) на 3 ОС **и в Debug, и в Release** (расхождение уровней оптимизации в целочисленной арифметике = UB, а не «погрешность») / перетасованный порядок создания даёт тот же хеш + негативный контроль / ноль аллокаций в `step()` и `hash()` со счётчиком, проверенным реальной аллокацией / ASan+UBSan на `-O1` молчат на пяти бинарях и дают тот же golden, что `-O3` / `0x32a094e8…`, `0xe728fef1…`, `0x4169690…` целы / границы диапазона и клампы формы отдельными целями — гейт границ и вскрыл насыщение импульса, которое голден из середины диапазона пропускал. Верность манифольда (две точки на грани, одна на угле, одна у перекрытия ТОЛЬКО скруглений) проверяется отдельно от голдена: «три ОС сошлись» и «сошлись на верном» — разные утверждения. Ревью вертикали 2 вскрыло два дефекта на заявленном диапазоне, зелёных на всех гейтах: капсула пролетала сквозь капсулу (у ядра-отрезка нет оси вдоль себя, SAT перекрытие радиусов не выражает) и тело тяжелее 384 единиц не вращалось вовсе (обратный момент инерции не помещается в Q16.16 снизу) — оба потому, что за новой степенью свободы не завели наблюдения. Второй проход ревью — уже по САМИМ фиксам, которых свежий взгляд не видел, — вскрыл, что отсечка нового пути зазора отменяла контакт на настоящем перекрытии (перебор вершин слеп к пересечению: ближайшая пара сидит на скрещении рёбер), а доля и её вклад в `k` клампились на разных порогах. Отсюда `framework_physics_terms_test`: коэффициенты решателя спрашиваются НАПРЯМУЮ тождеством `Δ(v_rel · ось) == λ` плюс абсолютным пином `k` на выводимой руками геометрии — за двести сорок шагов сцены «доля равна нулю» и «доля вдвое меньше нужной» неразличимы. Третий проход — по фиксам второго — вскрыл, что подсказка `cores_meet` чинила не то место: доказанное пересечение уходило на путь зазора, слепой к нему, и возвращалось контактом с нулевой глубиной и нормалью от вершины в восемнадцати юнитах. Ответ вернулся туда, где он точен, — на опорную ось SAT (`axis_contact`); саму раскладку пришлось искать перебором 95 млн пар, потому что за весь набор гейтов эта ветка не выполнялась НИ РАЗУ, то есть была зелена вакуумно. Узкая фаза разложена на три цели по числу путей (точка об ядро, отсечение граней, зазор ядер): имя упавшей в логе CI обязано называть сломанный путь. PR B закрыл **гейт 4**: фильтр `layer`+`mask` с ДВУСТОРОННИМ согласием (обе стороны обязаны видеть друг друга) отсекает пару в широкой фазе, тела-триггеры строят манифольд, но до решателя не доходят, события `begin`/`stay`/`end` доставляются ПОСЛЕ шага (решатель гоняет контакты `VELOCITY_ITERATIONS` раз, реакция игры из середины сдвинула бы мир под остальными итерациями) и в порядке, заданном слиянием отсортированных списков кадров, а не обходом хеш-множества; у потока событий СВОЙ накопительный голден `0x2c191ca82be0defc` рядом с голденом состояния — состояние сходится и при потерянном событии. Ревью PR B вскрыло, что «доставка после шага» и «СБОРКА после шага» — разные вещи: точка хранится плечом от центра тела, и сложение её с позицией ПОСЛЕ интеграции относило место удара на пройденный за кадр путь (десять пикселей на 600 юнитах в секунду — точка уезжала вглубь зоны мимо ударенной грани). Наблюдения за этим не было вовсе: прежний голден потока пинил смещённую точку и был зелен, а сцена гейта ехала по юниту за шаг, где ошибка неотличима от геометрии. Теперь `begin` сверяется с полосой перекрытия на момент обнаружения — она уже пути за кадр, и дофиксовый код из неё выпадает. Запросы (raycast/shapecast — `framework_physics_query_test`, overlap — своя цель: у списка свой контракт, порядок по ключу и очистка приёмника) считают консервативным продвижением, где луч — вырожденный случай свипа формы, и сверяются с таблицей, посчитанной РУКАМИ из раскладки, включая касательные (луч ровно по плоскости грани) и вырожденные (нулевая длина, старт изнутри) входы. PR C вертикали 3 закрыл **гейты 3 и 5**, и каждый из трёх шагов к ним вынужден замером, а не вкусом. Спекулятивный контакт (пара рождается уже при зазоре до 1/16, ширина взята равной `CONTACT_SLOP` — допуск уже объявляет полосу `|глубина| <= 1/16` касанием, поэтому полоса зазора той же ширины достраивает понятие симметрично, а не расширяет его): без него башня, выставленная впритык, проваливалась сама в себя на 7.3 юнита за восемь кадров — контакта нет, значит нет опоры, и этаж падает свободно за кадр. Симметричный проход Гаусса—Зейделя (чётная итерация вперёд, нечётная назад): Гаусс—Зейдель решает точки по очереди, первая всегда получает импульс по нескорректированной скорости, и на грани об грань это момент одного знака КАЖДЫЙ кадр — башня уезжала вбок на 2.53 юнита; что причина в порядке, а не в геометрии, показано зеркалом (обратный обход дал те же 2.53 в другую сторону), попеременный оставляет 0.035, а баланс держится на ЧЁТНОСТИ числа итераций. Правило покоя вместо доказанной неподвижной точки: прежняя редакция усыпляла остров по повтору отпечатка состояния, но посылка НЕ ВЫПОЛНЯЕТСЯ — за 12000 кадров отпечаток не повторился ни разу ни у башни, ни у одинокого ящика (решатель оставляет ±2 raw скорости, позиционная коррекция при одной итерации переталкивает стопку на 13–20 raw за кадр), то есть критерий был мёртвым кодом, а гейт на нём — вакуумно зелёным; теперь неподвижная точка не отыскивается, а СОЗДАЁТСЯ — остров, удержавшийся в круге 1/1024 юнита от ЯКОРЯ восемь кадров, замирает с РОВНО нулевой скоростью, и цена названа: остановить оно вправе только ход медленнее пикселя за две минуты. Сон — решение ОСТРОВА (статика в связность не входит: пол — общий сосед всей сцены, через него весь уровень стал бы одним островом), пробуждение по пересечению AABB заведомо раньше контакта, плюс неконстантный `body()`. Гейт 5 вскрыл при этом настоящий дефект, невидимый голдену состояния: кеш пишется в конце шага манифольдами, посчитанными в его начале, поэтому запись РЕШЁННОЙ пары описывает позиции до собственной позиционной коррекции — на первом замершем кадре она консервировалась навсегда, и прогон со сном расходился с прогоном без сна на 1 raw глубины, ТОЛЬКО по `event_hash`, ни на волос не сдвинув ни одно тело; `recall` теперь отдаёт лишь записи уже замерших пар. Голден состояния поэтому перепечатан осознанно (`0xad1ec96e2dbbcc32` → значение выше) — четыре причины по построению меняют числа, и эталон снят с прогона, где прошли ВСЕ проверки поведения. Четвёртая причина найдена ревью PR C и лежит ВНЕ физики: `fix32::operator*` округлял арифметическим сдвигом вправо, то есть к минус бесконечности, и произведение меньше кванта давало −1 raw на отрицательном знаке против 0 на положительном той же величины — асимметрия ЗНАКА в ядре движка. Физике она стоила обоих гейтов сразу (стопка ползла вверх, окно покоя не закрывалось никогда; переключение одной строки в обе стороны даёт `FAIL → PASS`), а перепечатала ДВА чужих голдена — ядра и ввода, — при шести неподвижных; решение принять принято владельцем явно. Дефект дожил до вертикали 3 потому, что `preflight.sh` сверял ОДИН голден, физики: дыра закрыта этапом `core_goldens` (семь из восьми локально, имя восьмого печатается). Ревью того же PR вскрыло ТРИ двери пробуждения замершего острова, из которых две были закрыты наглухо: замерший остров не читает ни тяготения, ни геометрии вокруг, поэтому перевёрнутая гравитация и телепортированный пол не делали с ним НИЧЕГО — не «позже», а никогда. Третья дверь, неконстантный `body()`, была открыта слишком широко: перегрузку выбирает КОНСТАНТНОСТЬ МИРА, а не намерение, и цикл отрисовки, читающий трансформации, сбрасывал бы окно покоя всем телам каждый кадр — правило оказалось бы выключено молча, при зелёном гейте, читающем мир константной ссылкой. Разобраны и отвергнуты оба очевидных ответа: будить по факту выдачи выключает покой молча, а размораживать «на кадр» делает ЧТЕНИЕ мира меняющим его траекторию — наружу не видно (`at_rest` всё время `true`, число замороженных кадров то же), а хеш расходится с 44-го кадра и к 600-му ящики разъезжаются на 5–35 raw. Поэтому ручка не делает с покоем НИЧЕГО, а правка замечается сверкой замершего тела с копией целиком, снятой `settle` на переходе в замершее: отпечаток из выбранных полей пришлось бы дописывать вслед за каждым новым полем, и забытое не падало бы, а молча переставало будить. Та же инъекция вскрыла ВАКУУМНУЮ сцену внутри самого гейта — толчок мерился на башне без трения, которая не замирает и за 30 секунд, то есть обе ветки давали одинаковый ход и при наглухо выключенном пробуждении. Там же: узкая фаза для триггерной пары зовётся с НУЛЕВЫМ спекулятивным полем — зона отвечает на тот же вопрос, что `overlap` («кто здесь сейчас»), и на общем поле давала бы `begin` телу, ещё не вошедшему в неё, с отрицательной глубиной, пока запрос отвечает «никого». PR D закрыл **гейт 8 машинно** — и вместе с ним машинную половину вертикали 3; ручная половина гейта (живые Linux и Windows) открыта и ждёт владельца, поэтому ADR 0015 остаётся *Proposed*. Утверждается РАБОТА, а не время: счётчики шага детерминированы и СОВПАЛИ на трёх ОС до единицы (прогон CI 31393763850, ubuntu/macos/windows, Release и Debug), а стенные часы на общем раннере гуляют, и красный шаг «кадр занял 3.1 мс вместо 2» отключили бы первым же — вместе со всем гейтом. Сцен нагрузки три, и третья заведена ЗАМЕРОМ, а не симметрией: разлёт задумывался худшим случаем широкой фазы и оказался ЛУЧШИМ (538 кандидатов против 11972 у кучи — разъехавшиеся тела сортировка по X разделяет идеально), то есть двумя сценами худший случай sweep-and-prune не измерялся вовсе. Вырождает SAP не разброс, а СОВПАДЕНИЕ координаты по оси сортировки: колонна из 500 тел на одном X даёт ровно полный перебор 124750 = 500·499/2. Замер опроверг и предпосылку раунда: цифра «≤ 2 мс» была назначена до измерения, честная — **3.64 мс** среднего кадра на худшей сцене (Apple M3 Pro), то есть 22% бюджета кадра; она и записана (2.25 мс и 14% — до подъёма числа итераций решателя с 8 до 16 в раунде 2026-08-12). Тем же замером закрыт вопрос об окончательном broadphase, и ответ обратен ожидаемому: колонна со 124750 кандидатами стоит 0.23 мс, а куча с 11951 — 3.64 мс, значит цену шага определяет РЕШАТЕЛЬ, а не широкая фаза, и менять SAP на сетку незачем; подъём числа итераций вдвое подтвердил это прямым опытом — широкая фаза осталась на месте, а кадр вырос в полтора раза. Ревью PR D вскрыло, что первая редакция сцены разлёта мерила ПЕРЕХОДНЫЙ процесс: пола у неё нет, но полом ей работал кламп `WORLD_HALF` в конце шага, и к трёхсотому кадру 282 тела из 500 стояли полосой у границы мира — «максимум движения при минимуме контактов» мерился в момент минимума движения, а числа, снятые с такого окна, лежали НИЖЕ установившейся сцены. Разлёт получил нулевое тяготение, прогрев удлинён втрое, и настройка мира уехала внутрь сцены. Эталон утверждается точным равенством и живёт константами ВНУТРИ бинаря, а не грепом по литералу в шаге CI (греп в workflow не проверяется локально ничем и всплывает только после пуша); рядом с ним — СТРУКТУРНЫЕ потолки, где каждая ступень шага ограничена предыдущей, а не числом из замера: они переживают перепин эталона, проверяются ПОКАДРОВО (пик по каждому полю берётся независимо, и сравнение пиков между собой слабее заявленного) и доказаны двойной инъекцией — сломать решатель И перепинить эталон под сломанное, потолок обязан упасть в одиночку. Доля полного перебора рядом с ними взята ИЗ ЗАМЕРА и структурной не является, о чём сказано прямо. Второй проход ревью вскрыл, что сетка кучи не помещалась между стенками ямы: отступ применялся только слева, и два правых столбца из 23 стартовали ВНУТРИ правой стенки — сорок два тела из пятисот начинали прогон в перекрытии со статикой, при комментарии рядом, утверждавшем обратное; числа при этом были устойчивы и выглядели честным эталоном. Ширина проверяется теперь `static_assert`. Там же: `active_bodies` считает тела, к которым применено тяготение, а НЕ «которые шаг подвинул» — кинематические он интегрирует, не считая, и доки утверждали обратное. Эталон сверен Release против Debug до единицы. Там же доведён гейт 6: перехват аллокаций получил ВЫРАВНЕННЫЕ формы — перегрузку выбирает компилятор по `alignof`, и до PR D над-выравненное выделение шло мимо счётчика, а его `delete` уходил в `free` от указателя, которого `malloc` не выдавал. Позитивный контроль на них пришлось пропустить через непрозрачную границу: clang на -O3 выбрасывал пару new/delete целиком, не оставляя в объектнике даже ССЫЛКИ на `operator new`, и наивный контроль был зелен вакуумно. Раунд 2026-08-12 поднял `VELOCITY_ITERATIONS` с 8 до 16: спека обещала «стопка ящиков стоит», не называя высоты, а замер назвал — башня заваливалась с ДЕВЯТОГО этажа при штатном трении и с ТРЕТЬЕГО без трения вовсе. Шестнадцать дают десять и три, цена — кадр худшей сцены с 2.25 до 3.64 мс. Глубина теперь ГЕЙТ, а не обещание (`framework_physics_depth_test` утверждает ОБЕ границы: башня заявленной высоты стоит И башня на ящик выше валится — односторонний ассерт прошёл бы и на сорока итерациях, тихо заплатив кадром), и там же утверждается ЧЁТНОСТЬ числа итераций, на которой стоит попеременный обход точек манифольда. Подъём вскрыл, что гейт стопки утверждал не то, что думал: он мерил ПОЗИЦИЮ верхнего ящика допуском ОДНОГО контакта, хотя позиция k-го ящика есть сумма k+1 проседаний под ним, — теперь сверяются проникновения поконтактно. Тем же вскрылся гейт пробуждения: толчок доезжает (+27 raw на первом шаге), но трение съедает его за два кадра, и знак суммы за 300 кадров был знаком остатка решателя, а не толчка) — [`specs/2026-07-26-physics-core.md`](specs/2026-07-26-physics-core.md) · [`decisions/2026-08-08-physics-core.md`](decisions/2026-08-08-physics-core.md) |
| 16 | Движение персонажа и тайлмап-коллизии | **Закрыта** (ADR 0016 **Accepted** 2026-09-02: машинная половина закрыта целиком, гейт 8 закрыт владельцем: 2026-08-30 словом, 2026-09-01 перезакрыт С УЛИКАМИ — запись прогона, стартовая строка (Intel UHD 620, Vulkan, `gamepad: evdev`) и ответы на все семь вопросов §6. Живая игра дала ТРИ находки, невидимые всем машинным гейтам раунда, потому что те ходят сценарными маршрутами: уровень без левого края, платформа, проезжающая сквозь персонажа сбоку, и перенос опорой, объявлявший раздавленным пассажира, упёршегося вбок. Все три починены и пришпилены гейтами, красными до фикса; маршрутный хеш `0xfead7a87477a9258` не двинулся ни разу — он и не мог, маршрут туда не ходит, **owner-половина гейта 7 закрыта 2026-09-01** — стенное время тика против бюджета кадра на самой медленной машине; прецедент #15 прямой: там owner-замер изменил заявленное число с 500 тел на 350, здесь такого числа не нашлось. Intel UHD 620 под Nobara: `worst` 0.2600 мс и `mean` 0.0242 — 1.56% и 0.15% кадра; та же коробка под Windows 11/MSVC: `worst` 0.2338–0.6174 и `mean` 0.0304–0.0310, а счётчики двух тулчейнов совпали до единицы (`queries=5345 scanned=38794`, `hash=5b3bcf0fc03ada62`, `allocs=0`) — инвариант 4, ответивший на третьей ОС. Замер оказался чувствителен к загрузке машины (3.7349 мс при трёх сборках рядом против 0.19–0.28 на тихой), и различить два прогона по выводу нельзя — счётчики пинятся в бинаре, поэтому требование мерить на незанятой машине выписано в оба runbook'а. лестница приехала отдельным РЕЖИМОМ движения (`MoveState::Ladder`, альтернативный тик с хуком до шага 0), `MOVE_VERSION` 4. вертикаль 1 закрыта машинно 2026-08-23: кинематический контроллер `engine/framework/character/` — гейт 1 golden траектории `0xb8a964e87679bce4`, гейт 2 табличные окна прощения в тиках + профиль с нулевыми окнами, гейт 3 нет туннелирования с контролем-телепортом, инвариант 5 zero-alloc счётчиком кучи; ценой прыжка была починка `nearest_on_core` — ответ опорной плоскостью грани вместо ближайшей точки, отдельным коммитом с регрессом и перепечаткой голдена состояния физики `0xf238d0bc34325db6`. перед вертикалью 2 отдельным раундом `perf(physics)` закрыто известное ограничение спеки: запросы обходили ВСЕ тела мира, теперь у мира персистентный sweep-and-prune по X с полосой из двух двоичных поисков (второй — по префиксному максимуму правых краёв), а шов протухания ИМЕНОВАННЫЙ — неконстантный `body()` удалён, правка идёт через `mutate()`, и читателю дверь правки теперь физически недоступна. Граница у шва названа ревью и записана рядом с ним: индекс метится по факту ВЫДАЧИ ручки, поэтому ссылка, пережившая запрос между выдачей и записью, пишет мимо всех дверей — ручка действительна до ближайшего запроса или шага, кешировать её на кадр нельзя. Цена мерится счётчиком рассмотренных тел, а не часами: `framework_physics_index_test` требует 1 из 192 на узком зонде и все 192 на луче через всю сцену — второе утверждение антивакуумное, без него гейт прошёл бы и на счётчике, всегда пишущем ноль. Полоса расширяется симметрично с обеих сторон: односторонний вариант был измеренным дефектом этого раунда — `overlaps` строгий, а свип принимает зазор ровно в `CONTACT_SLOP` включительно, и зажатый впритык контроллер переставал видеть стену (`framework_character_tunnel_test` покраснел). Голдены физики и траектории не двинулись: индекс — ускорение, а не смена ответа. Следом отдельным раундом `fix(physics)` закрыт дефект, найденный при подготовке тайлмапа и существовавший с самого свипа: нормаль касания бралась у перебора вершин, а тот на паре «угол в угол» называет диагональ между углами вместо грани, которой мувер вот-вот коснётся. Теперь её отдаёт УЗКАЯ ФАЗА — та же функция, которой отвечает шаг мира. Цена названа числом: голден траектории перепечатан в `0xb8a964e87679bce4` (голден физики не двинулся — дефект жил только на пути запроса), расхождение траекторий заперто в тиках 196..309 из 600 и снято потиковым замером, а не рассуждением. Гейт `framework_physics_corner_test` держит позитивный контроль на сам свидетель и краснеет в день, когда посылка решения исчезнет. вертикаль 2 начата: шаг A закрыт машинно 2026-08-23 — модуль `engine/framework/tilemap/` (плоская плотная сетка + три запроса с семантикой #15, геометрия НЕ переписана: тайл разворачивается в `physics::WorldShape` и отвечает тем же `cast_shape`, что и мир). Голден ответов `0xb19419157787b1c3` в Release и в Debug на трёх ОС, инвариант 4 пинится счётчиком: тот же зонд сканирует 10 тайлов и на карте 12×12, и на 200×200. Отсечение внутренних граней СНЯТО по замеру, а не по вкусу: 9773 случайных свипа и 490 выровненных сцен дали ноль расхождений «с отсечением/без» на обоих правилах нормали, а цена его была ненулевой — тело целиком внутри тайлов теряло все грани разом и получало «путь свободен» (2360 из 20000); теперь оно получает долю ноль, как от `physics::World`. Инвариант 3 держит геометрия, и позитивный контроль гейта сделан парой сцен из ДАННЫХ, отличающихся одним тайлом. Контроллер к тайлам ещё НЕ подключён — это шаг вертикали 3. Шаг B закрыт машинно 2026-08-24: карта уровня едет ДАННЫМИ — свой текстовый формат (`tilemap.txt`, пекарь остаётся чистым парсером, поэтому секция сверяется `--verify-game` на трёх ОС без единого внешнего инструмента), zero-parse таблица в бандле и читатель `TileMapTable` прямо из mmap-региона. Гейтов три, и роли между ними разделены СЛОМАННЫМИ реализациями, а не рассуждением: байтовый голден `0xcd034f07f57b5f8b` один видит согласованную перестановку полей в пекаре и читателе, `--verify-game` один видит отставший от исходника бандл, а читательский тест на лежащем в git `game.bundle` один видит дефект ЧТЕНИЯ — байтовая сверка читателя не запускает вовсе. Бандл перепечён: `bundle_hash 0xb86719612225e36f`, пять ассетов, четыре tool-free секции. Склейка тайлов в полосы отложена ДО ЗАМЕРА (решение владельца: полосы назначает счётчик `scanned` на живом уровне, а не соображение), стриминг (гейт 6) отложен по-прежнему. Потребителя в рантайме пока нет — подключение приезжает вертикалью 3 вместе с контроллером. вертикаль 3 начата: шаг A закрыт машинно 2026-08-24 — контроллер переехал на СЦЕНУ (`character/collision.hpp`: `CollisionScene{world, grid}`, оба указателя опциональны), слияние ответов двух источников идёт по МЕНЬШЕЙ доле, на равной доле — по более встречной грани, на полной ничьей — телам; порядок пинится отдельной целью `framework_character_collision_test`, и каждое из трёх правил проверено сломанной реализацией, роняющей ровно свой случай. Голден траектории перепечатан в `0x7801049c168bb301`: сцена гейта 1 теперь «тайлы + одно тело» (пол и стена сеткой, потолок телом) при геометрии ЮНИТ В ЮНИТ прежней, поэтому расхождение измеряет ровно одно — насколько ответ сетки отличается от ответа мира тел на той же грани. Замер потиковый: расходятся тики 2..408 из 600, максимум 0.0136 по X и 0.0621 по Y — обе величины МЕНЬШЕ `CONTACT_SLOP`, ни один дискретный признак (`state`, `on_ground`, `hit_wall`, `hit_ceiling`) не разошёлся ни разу, конечная позиция побитово та же. Переезд вскрыл дефект тайлмапа, живший с шага A вертикали 2: на равной доле порядок решал ИНДЕКС тайла, и персонаж, садящийся на плоский пол ровно на стыке двух тайлов, получал вместо нормали пола диагональ — скольжение обнулялось, и он вставал на ровном месте. Теперь осевая грань бьёт диагональ (`tilemap/query.cpp`, `axial()`), случай пинится `test_landing_on_a_seam` с контролем прямым падением, и позитивный контроль подтверждён: на старом правиле краснеют оба гейта — и тайлмапа, и угла в `framework_character_tunnel_test`. Вторым коммитом шага A приехало геометрическое прощение: `corner_correction` и `ground_snap` полями профиля (решения владельца — «только голова» и «стоял + не поднимается»), формат поднят до версии 2 с полями В КОНЕЦ, бандл перепечён `bundle_hash 0xecf9af92663a002c`, голден байтов профиля `0x6fc67dbed5d4c5e9`. Голден траектории при этом НЕ СДВИНУЛСЯ, и это находка, а не спокойствие: в сцене гейта 1 не срабатывает ни один из приёмов, то есть три ОС и Debug/Release про них не утверждают ничего — всё доказательство несут две новые цели — `framework_character_corner_test` и `framework_character_snap_test` (таблицы окон в ЮНИТАХ на своей геометрии, промахи дробные, чтобы ответ приходил от окна, а не от допуска свипа; целей две по границе раскладки, а не по длине файла: у сдвига она про потолок, у притяжения про пол). Прогон сломанными реализациями поймал четыре из пяти; пятая — снятая проверка знака пути в `corner_correct` — не ловится ничем, потому что наблюдаемое держит проверка НОРМАЛИ, и знак оказался ранним выходом, а не условием поведения: записано в `assist.hpp` вместо прежнего утверждения. Независимое ревью нашло то, чего этот прогон не искал, и это цена самого метода — сломанная реализация проверяет уже сделанные утверждения и молчит про случай, которого в таблице нет: (1) прилипание СЪЕДАЛО прыжок из-под низкого потолка, потому что условие «не поднимается» читалось после движения, а касание потолка гасит `velocity.y` тем же тиком — знак теперь снимается снимком ДО движения, что верно решению владельца («не поднимается» — про намерение тика), и пинится замером `apex=3.875` при тике подъёма 6.533; (2) два сторожа отказа по нормали стояли на осевой стене вплотную и были ВАКУУМНЫ — вертикальный свип к вертикальной грани не приближается, обе проверки проходили и на снятой проверке нормали, раскладка переписана на клин из `physics::polygon` под 63°; (3) окна клампились в `sanitize`, то есть у зовущего, — кламп переехал внутрь `assist.cpp`) Шаг B вертикали 3 начат, нарезан на три коммита (решение владельца: склоны → односторонние → движущиеся, PR один в конце). B1 закрыт машинно 2026-08-24 — геометрия склона: `TILE_SLOPE` плюс два бита зеркал живут ВМЕСТЕ с телесным флагом (склон — модификатор формы, а не вид тайла), ориентация названа сплошным углом, потому что «вверх направо» под +Y ВНИЗ читается двояко; формы кешируются по ориентации (`tile_shape.hpp`, четыре на окно запроса), разбор отвергает два слова склона в одной строке и склон без тела; гейт `framework_tilemap_slope_test` парный — склон обязан отвечать пусто там, где коробка отвечает, — а набор отказов пекаря оказался вакуумен наполовину (сверял только номер строки, пекарь с `slope_words > 9` оставлял его зелёным). B1b закрыт машинно 2026-08-24 — склон стал ПОЛОМ для контроллера: порог проходимости `SURFACE_NORMAL_Y` заменён полем профиля `max_slope` и сравнением ОТНОШЕНИЯ подъёма к пробегу, потому что тайл выражает ровно 45°, а `cos 45°` в Q16.16 — округлённое число, и разряд округления решал бы судьбу самой частой грани карты; потолок `MAX_SLOPE = 2` выведен из `ground_snap`, а не выбран. Формат профиля поднят до версии 3 полем В КОНЕЦ, голден байтов `0x5633becd833be880`, бандл перепечён `bundle_hash 0xd1455f9139d79913`. Незапланированная находка гейта: склон работал ТРАМПЛИНОМ — `slide_along` проецирует на грань и скорость, поэтому 340 юнит/с горизонтали давали −320 вертикали, и персонаж вылетал на две клетки над верхом; владелец выбрал «гасить подъём на опоре», но буквальная форма этого варианта ползёт (3.5 юнита подъёма против 92 спуска: проекция каждый тик делит горизонталь пополам, а шаг 2 возвращает лишь `ground_accel * dt`), поэтому гашение стоит в момент СХОДА с опоры, где та же скорость перестаёт быть движением по поверхности. Второй частью починки сторож притяжения к полу стал ПОИМЕННЫМ (`jumped` шага 4 вместо снимка `was_rising`): знаком этот подъём не отличить от прыжка ни после движения, ни до него — идущий вверх по склону поднимается скольжением, и на верхнем краю уходил в воздух там, где притяжение обязано было его удержать. Гейт `framework_character_slope_test` мерит высоту и опору, а не хеш (голден траектории `0x7801049c168bb301` НЕ сдвинулся — в его сцене нет ни одного склона, то есть про них он не утверждает ничего); подброс меряется профилем с выключенным `ground_snap`, потому что на штатном его не видно вовсе — первая раскладка этого не знала и была вакуумной; сломанных реализаций три, и третья (сторож, снятый целиком) доказывает, что условие осталось несущим после переписывания. Шаг B2 — односторонние платформы: бит `TILE_ONEWAY` живёт ВМЕСТЕ с `TILE_SOLID` (решение владельца), иначе тайл выпадал бы из каждого уже написанного запроса с `require = TILE_SOLID`, а решает ЗАПРОС на каждом хите; держит только грань ВВЕРХ и только если низ формы на СТАРТЕ свипа был не ниже верха тайла, и обе части несущие — без первой тайл держит сбоку, без второй держит того, кто уже внутри. Спуск («вниз» + ФРОНТ прыжка на односторонней опоре) вышел САМООГРАНИЧЕННЫМ: за тик с `exclude = TILE_ONEWAY` гравитация уводит ноги на 0.667 при зазоре 0.125, поэтому счётчика тиков в профиле нет и версия формата осталась 3. Проба земли докладывает, односторонняя ли опора, — иначе прыжок молча съедался бы на каменном полу; спуск гасит буфер и coyote (одно нажатие — одно событие). Пара `oneway slope` отвергается парсером: держащая грань склона это гипотенуза, и правило «пришёл сверху» на ней не выражается — то есть общий бит со склоном НЕ комбинируется. Оба гейта нашли по дыре в себе: у тайлмапного снятая проверка направления грани не роняла набор вовсе (закрыто пятым подходом — по диагонали в бок), у персонажного снятое гашение буфера не наблюдаемо ни на какой раскладке при штатных шести тиках (закрыто платформой прямо над полом и окном в 30 тиков). Голдены не сдвинулись, и это утверждение: нового бита нет ни в одной их сцене. ШАГ B3 (движущиеся платформы) закрыт машинно: платформа — ТЕЛО и только тело (у `TileGrid` начало координат лишь читается, значит движущийся тайловый пол невыразим, а движущаяся платформа не может быть односторонней). Четыре решения владельца: перенос двигает только ПОЗИЦИЮ, прыжок наследует только ГОРИЗОНТАЛЬ (унаследованная вертикаль отменяла бы `jump_height` профиля), порядок кадра `world.step` → `character::step` контрактом, раздавливание — флаг `crushed` наружу и ничего больше. Перенос идёт ПЕРВЫМ шагом тика, БЕЗ свипа и АТОМАРНО: свип из-под опоры отвечает долей ноль с нормалью ПОЛА, то есть про саму опору, а перенос «сколько влезло» вдавливал бы персонажа в потолок по кусочку за тик. Проверка «влезаю ли» — свип НУЛЕВОЙ длины: `overlapped_at_start` считается до цикла, длина в него не входит. Неподвижность спрашивается предикатом интеграции мира (статика по типу, замершее по правилу покоя), иначе статическая плита с записанной скоростью увозила бы с себя персонажа. Опора помнится через окно coyote и умирает вместе с ним; `SceneHit` получил `BodyId`, пробы — `GroundInfo*`. Гейт `framework_character_platform_test` — шесть парных вопросов на своей сцене-заголовке, семь сломанных реализаций, каждая роняет свою строку; потолок `controller.cpp` поднят 205 → 230 осознанно. Голдены не сдвинулись: сцена голдена не держит тел и не шагает мир ни разу. **Шаг D вертикали 3 — гейт 7 закрыт машинно 2026-08-30**: `framework_character_perf_test` гоняет один маршрут по двум порождённым картам, 256x32 и 1024x256 (в 32 раза по площади), и счётчики совпадают побитово — `queries=5345`, `scanned=38794`, худший тик 20 запросов / 156 тайлов (0.06% целевой карты), хеш `0x5b3bcf0fc03ada62`, 0 аллокаций; Debug и Release согласны, сломанное окно (`window()` во всю карту) роняет гейт тремя строками разом. Уровень порождается гейтом, а не читается из ассетов игры: гейт движка на ассете краснеет от чужой правки. Тем же числом закрыты два отложенных вопроса — **склейки тайлов в полосы не будет** (окно и так 21.6 тайла на тик, а полосы — второй источник геометрии), **гейт 6 (стриминг чанков) снят** (цена от площади не зависит, стриминг покупает мегабайт ценой шва, двигающего хеш; порог возврата — карта свыше ~4 млн тайлов). Стенное время в кадре остаётся владельцу: раздел 5 `docs/owner-verification.md`, пункт L `docs/owner-setup.txt`, замер — `scripts/owner_perf.sh`. **Владельческий прогон §6 от 2026-09-01 (Linux) дал две находки, и обе невидимы гейтам раунда по одной причине — скриптованный маршрут трогает только то, ради чего написан**: за левым краем карты образца нет ни пола, ни стены, а стена в три тайла перепрыгивается (герой уходил жить ВНЕ уровня живым и управляемым — закрыто клампом в `platformer_scene.cpp` по тому же прямоугольнику, к которому уже прибита камера, и гейтом края в `game_platformer_sim_test`, идущим влево С ПРЫЖКОМ: ходьба упирается в стену на 24.125 и проходила бы вакуумно); тик персонажа смотрел на движущиеся тела только через опору, на которой СТОИТ, поэтому платформа проезжала сквозь висящего — за полсекунды на пятьдесят юнитов, — а отход `move_and_slide` на `SKIN` вдоль оси меньшего погружения читался владельцем как «выжимает наверх, будто пасту из тюбика» (закрыто `character/push.hpp`: тело, въезжающее в персонажа, сносит его вдоль СВОЕЙ нормали атомарно и отдаёт `crushed`, когда на новом месте есть что-то кроме толкателя; гейт `framework_character_push_test`, на коде до фикса первый случай меряет проникновение 53.985). Ни один из фиксов не двигает ни маршрутный хеш `0xfead7a87477a9258`, ни восемь голденов ядра — это то же утверждение с другой стороны. — [`specs/2026-07-26-character-tilemap.md`](specs/2026-07-26-character-tilemap.md) · [`decisions/2026-08-30-character-tilemap.md`](decisions/2026-08-30-character-tilemap.md) |
| 17 | Графика из коробки: спрайты, анимации, камера, частицы | **Закрыта** (ADR 0017 **Accepted** 2026-09-02) — вертикаль 1 (данные и тик, headless) **закрыта**: шаги A (клипы, шкала, метки), B (стейт-машина), C (камера с политиками), D (нарезка атласа именем, секция `atlas_regions` бандла) и E (отладочный оверлей: линия/рамка/текст → квады); вертикаль 2 — шаги A (спрайты, слои, батчинг по материалу, 9-slice; гейт 6 числом draw-call'ов, гейт 8 счётчиком аллокаций) B (тайлмап с culling'ом; гейт 7 числом обойдённых тайлов на двух картах) C (частицы обоих классов, разделённые ТИПОМ эмиттера; гейт 3 — sim-хеш не зависит от декораций, гейт 4 — голден состояния эмиттеров) и D (зум камеры: развёртка мира в экран, пиксельная сетка привязкой на ЭКРАНЕ; зум живёт в `Viewport`, а не в камере, чтобы разрешение окна не попало в тик), голдены `0x8db6e6730e0c446f`, `0x15028037bdca9f22`, `0xd6c2f15de2ab50f8`, `0xa9b9b74a0152af52`, `0x922ccf9cf4b0aecc`, `0xbda7b692a87b1155`, `0x599044fa7aabfddc`, `0xab81c90241aaa65a` и `0x5bcea5ecad651b07` на 3 ОС и в Debug; вертикаль 3 — шаг A (платформер-образец переехал на фреймворк: свои кламп камеры, окно culling'а и сборка квадов удалены, тон тайла стал таблицей на КЛАСС, порядок задан слоями; гейт 10 числом — маршрут `0xfead7a87477a9258` не двинулся), шаги B1/B2 (нарезка шутера читается из секции бандла по именам, а спрайт адресует регион ЧИСЛОМ через `RegionSlot` — список имён остаётся один) и B3 (частицы шутера на `DecorEmitter` фреймворка: описания данными в `fx_table.hpp` с пересчётом секунд в тики функцией, шов до инстансов `sprite_out.cpp`, накопитель кадра `instance_stage.cpp` отделён от `SpriteBatch` вынужденно — гейт 8 требовал утверждения НА ЗАГРУЗКЕ, а гейт на `SpriteBatch` был бы гейтом на видеокарте раннера. Голден `0xfd9ca7d2936ad48a`, гейт 8 на обеих половинах кадра, яркость 1.9 стала экспозицией МАТЕРИАЛА — в восемь бит канала она не кладётся; личный поворот частицы снят и оплачен утверждением о симметрии звезды по обоим зеркалам И по транспонированию, потому что четырёхлучевая искра переживает оба зеркала). Шаг C — сценарий гейта 9 написан построчно и разложен на ДВЕ половины с разным вопросом: у платформера кадр обязан совпасть, у шутера — нет и не совпадёт (float-LCG → `fix32`, трение до шага вместо после, конус вместо корреляции с местом рождения), и все три расхождения названы ДО команд сборки. Гейт 2 (попиксельный эталонный кадр) закрыт машинно 2026-08-31: допуск судит РАЗМЕР СВЯЗНОГО ПЯТНА разошедшихся пикселей (12), а не пик — на lavapipe пик уже 0.73, потому что краевой пиксель, округлённый растеризатором на другую сторону обводки, отличается на КОНТРАСТ этой обводки, и никакой порог по пику не отделит его от дефекта; дефект двигает ПЛОЩАДЬ. Ценой стало слепое пятно, выписанное словами: яркий артефакт меньше 12 связных пикселей этот гейт от шума растеризатора не отличит. Три живых драйвера 2026-09-01 подтвердили допуск на железе, которого нет ни у одного раннера: Intel UHD 620/Mesa `blob 1`, NVIDIA MX150/Windows `blob 1`, тот же UHD 620 под windows-драйвером `blob 0` — два стека над одним кремнием разошлись по пику в сорок шесть раз и оба зелёные, то есть допуск размерен по стеку, а не по кремнию; AMD не отвечал никто. Гейт 9 закрыт владельцем 2026-09-02. Требование «ручные `art/batch/draw/fx` удаляются» **переформулировано 2026-08-31**: три из четырёх файлов держат не дубль слоя, а содержимое конкретной игры (процедурная печать пикселей, композиция со спрайт-текстом, таблица описаний эффектов), и удалять там нечего — снят дубль, а не файлы — [`specs/2026-07-26-graphics-framework.md`](specs/2026-07-26-graphics-framework.md) · [`decisions/2026-08-30-graphics-framework.md`](decisions/2026-08-30-graphics-framework.md) |
| 18 | Материалы и шейдеры | **В работе** (ADR 0018 **Proposed** 2026-09-03) — вертикаль 1 (материал как ассет) закрыта шагами A–D: таблица материалов печётся и читается zero-parse, библиотека из семи материалов компилируется в ТРИ пайплайна (ключ — пара «точка входа + смешивание», а не имя материала), оба потребителя — golden-харнесс движка и игра-образец — рисуют ею. Вертикаль 2 (валидация и hot-reload) закрыта машинно 2026-09-03: гейт 2 — пекарь поднимает Metal/Vulkan/D3D12 по одному и собирает НЕ модуль, а все пайплайны таблицы (разбор WGSL у naga общий, генерация кода своя, отказывает она на пайплайне), недоступный бэкенд назван поимённо и посчитан числом; гейт 3 — драйвер горячей замены живёт рядом с кэшем, потребителей у него двое (игра через `LIKENES_FX_WGSL` и панель редактора), набор пайплайнов меняется целиком, битая правка не трогает ни одного объекта и оставляет прежний вариант рисовать. Обе половины гейта 3 машинные: `material_hot_reload` на трёх ОС (плюс второй прогон с `LIKE_NES_WATCH=poll` на Linux) и `editor_shell --gate3`, который судит ПИКСЕЛЯМИ — хеш превью меняется на верной правке и совпадает бит в бит после битой. Гейты 4, 5, 6 и 8 зелёные на трёх ОС. Вертикаль 3 (2D-освещение) закрыта машинно 2026-09-03 тремя шагами: свет как ДАННЫЕ (таблица `lights.txt` печётся zero-parse и едет в `library.bundle`, ambient — в заголовке таблицы), проход освещения ВЫКЛЮЧАЕМ и при выключении возвращает прежний кадр бит в бит, нормали и мягкие тени читаются из СЛОТОВ материала, а не из отдельного канала. Машинно у спеки не осталось ничего: ждут владельца только гейты 1 (golden-кадр библиотеки на трёх бэкендах) и 9 (живой прогон, запись экрана) — [`owner-verification.md`](../docs/owner-verification.md) §§9–11 — [`specs/2026-07-26-materials-shaders.md`](specs/2026-07-26-materials-shaders.md) · [`decisions/2026-09-03-materials-shaders.md`](decisions/2026-09-03-materials-shaders.md) |
| 19 | README и документация en/ru | **Draft** — [`specs/2026-07-26-docs-en-ru.md`](specs/2026-07-26-docs-en-ru.md) |
| 20 | Релизные инсталляторы и скрипт сборки релизов | **В работе** — вертикали 1 и 2 закрыты. Вертикаль 1 (релиз собирается и сверяется): компонент установки `engine` (`cmake/install_engine.cmake` — редактор, пекарь, рантайм wgpu, 7 лицензий, `version.txt` со штампом версии/коммита/тройки), `scripts/release.sh` (версия из тега либо `--version`, отсутствие — отказ кодом 2, а не «0.0.0»; `--only windows` отказывает кодом 3, называя задачу CI следующей вертикали, `--only linux` уходит в контейнер), детерминированный архив (сортированный список, mtime коммита, uid/gid 0, `gzip -n`) и `release/<версия>/` с манифестом и `SHA256SUMS`. Гейт `scripts/check_release.sh` **упаковывает пакет ДВАЖДЫ из одной сборки** (свой каталог `build-release`: релизная конфигурация, залитая в общий, переживает в кеше следующий прогон и молча подменяет ему набор опций) и утверждает двенадцать вещей — состав поимённо (равенство множеств, не подмножество), непустые лицензии, имя рантайма против НЕЗАВИСИМОГО источника (`WGPU_RUNTIME_LIB` из кеша CMake), поля штампа с тройкой, сверенной с именем пакета, совпадение архива со своим манифестом, нормализацию самого архива (владелец 0, порядок `LC_ALL=C`, `gzip` без времени — двух прогонов на неё не хватает: они идут из одного стейджа, где uid и порядок совпадают по построению), воспроизводимость содержимого и суммы, строку в `SHA256SUMS`, игнор `release/`, уборку каталога прогона за собой и отсутствие следов в дереве. Позитивных контролей 40 в двух наборах, разведённых по предмету: `check_release_selftest.sh` (26 — содержимое пакета) зовёт внешней командой `check_release_pack_selftest.sh` (14 — форма архива и гигиена прогона), и среди них пять ПОДМЕННЫХ реализаций — проверка составом-подмножеством, штамп «файл существует» и три подмены архива — без зануления владельца, без сортировки и со временем, вписанным прямо в заголовок gzip (снятый `gzip -n` фикстурой быть не может: у GNU gzip из stdin флаг — пустышка, время пишет только gzip Apple, и подмена проверяла бы платформу вместо утверждения), — которые набор обязан отбить. Вертикаль 2 (пакет Linux с машины macOS): `scripts/release_linux.Dockerfile` с базой, пиннутой ДАЙДЖЕСТОМ (тег `ubuntu:24.04` переезжает молча, и «та же команда» перестала бы давать тот же пакет), `scripts/release_container.sh` — контейнер даёт линукс-ХОСТ, а внутри работает тот же `release.sh`, поэтому второго упаковщика, который разъедется с оригиналом, нет по построению. Гейт `scripts/check_release_container.sh` собирает пакет в контейнере ДВАЖДЫ и гоняет по нему те же одиннадцать утверждений о пакете (целевая ОС названа явно: состав, посчитанный по хосту, ждал бы `libwgpu_native.dylib`), а без `--live` — одни правила без демона: пин базы, делегирование упаковки, монтирование дерева `:ro`, тег с архитектурой, живость движка по ответу `info` (а не по наличию в PATH: Docker Desktop с погашенным демоном иначе падал бы посреди сборки), коды отказов 3 и 4, кеш вне дерева и снос обломка упавшей конфигурации. Позитивных контролей 37 на macOS (35 на линукс-хосте, где делегирование и отказ по linux — своя сборка и пропускаются вслух) в трёх наборах, разведённых по предмету (файлы контейнерного пути, подменённые реализации библиотеки, ветвление `release.sh` на диспатче), и три из них — про сам набор: промах копирования обязан валить его, нетронутая копия обязана ПРОХОДИТЬ утверждение, которое валит её сломанный близнец, и каждое имя `assert_*`, которое зовёт гейт, обязано быть определено. Первые два появились после того, как набор напечатал `PASS`, не создав ни одной копии (`local a="$1" b="$a"` на bash 3.2 macOS), третье — после `assert_pack_normalized: command not found` в ветке `--live`, которой этап правил не касается. Ревью вертикали нашло четвёртое: ранняя резолюция версии, поднятая над диспатчем ради контейнерной ветки, отобрала у `--only windows` его код 3 на HEAD без тега, а утверждение этого не видело, потому что само подставляло `--version` — контракт «код 3 независимо от версии» проверялся версией, которую сам же и дал. Гейты 3–8 (чистые системы) остаются владельцу — §15 `docs/owner-verification.md`, пункт O `docs/owner-setup.txt` — [`specs/2026-07-26-release-installers.md`](specs/2026-07-26-release-installers.md) |
| 21 | Глобальный аудит кодовой базы (2 независимых ревью) | **Draft** — [`specs/2026-07-26-global-audit.md`](specs/2026-07-26-global-audit.md) |
| 22 | Детерминированный сетевой слой: сессия, откат, авторитетный прогон | **В работе** — [`specs/2026-09-02-deterministic-net.md`](specs/2026-09-02-deterministic-net.md). Строка была placeholder'ом «нетворкинг/rollback, planned» и стала спекой 2026-09-02. Скоуп сужен решением владельца: вертикаль 1 — **peer-hosted lockstep с откатом** (хостит один из игроков, по сети идёт ВВОД, а не мир), вертикаль 2 — тот же хост **без окна** плюс верификация реплеев переигрыванием потока `InputFrame` со сверкой `state_hash`. **MMO из формулировки убрано**: персистентность, шардинг, интерес-менеджмент и аккаунты — бэкенд-сервис, а не движок, и столп «только движок» это запрещает. Долг по строке оплачен раньше и другими раундами: ADR 0004, 0005, 0006 и 0015 обосновывают свои решения именно netplay'ем, детерминизм доказан на arm64 против x86_64 и на трёх компиляторах, `InputFrame` уже per-tick и воспроизводим, состояние — плоские вектора POD'ов, а граница «декор вне симуляции» пришпилена гейтом 3 спеки #17. Вертикаль 1 закрыта 2026-09-03 (гейты 1–5 и 7): `framework::rollback::Session` с окном `LEAD = 16` и откатом на подтверждённый кадр, `net::Link` с окном отправки и переупорядочиванием поверх шва `platform::net::Socket`, два ПРОЦЕССА по петле сходятся к одному `state_hash` — гейт 3 закрыт дважды (инъектор поверх шва в `net_link_test` и живая петля в `net_loopback_test`), потому что инъектор доказывает реакцию на потерю, а не то, что сокет вообще ходит. Терпение пира к дыре меряется СТЕННЫМ временем: две тысячи проходов с `sleep_for(1ms)` — это 2.4 с на macOS и 31 с на Windows, где сон округляется до ~15.6 мс, и гейт 7 краснел на windows-раннере именно так. Вертикаль 2 закрыта 2026-09-03 (гейт 6): поток `InputFrame` с заявленным `state_hash` переигрывается и подделка отбивается С НОМЕРОМ ТИКА, запись ведёт тот же `--peer` через `RecordingSim`. Гейты 8 и 9 — владелец: 8 вооружён в тот же день (пир мерит свой кадр ДВУМЯ цифрами — шаг симуляции с откатами и записью против обслуживания сокета, `scripts/owner_net_budget.sh` поднимает пару и печатает их, процедура — §13 `docs/owner-verification.md`; на M3 Pro худший кадр 1.3% и 1.9% бюджета), а 9 вооружён НАПОЛОВИНУ 2026-09-04: адресация соседа была долгом ДВИЖКА — оба пира адресовали `ADDRESS_LOOPBACK` и знакомились файлом на одной ФС, поэтому «живая сессия» вышла бы петлёй под другим именем. Теперь сосед называется аргументами `--listen <port>` и `--at <host:port>`, заданная НАПОЛОВИНУ адресация — отказ кодом 3, а не тихий откат на файлы (иначе гейт двух машин зеленел бы на одной), и `game_platformer_net_direct_test` держит два структурных контроля: марки сходятся у пары по названным адресам И файлов `.port` после такого прогона не существует. Сходимость по проводу владелец гоняет с этого дня; субъективное «играется ли» остаётся долгом движка — окна у headless-пира нет |

> Бывшая строка «Боль 2D-разработчиков — дополнительные фичи» раскрыта спеками **#14–#18**:
> пресеты ввода и шаблоны контроллеров, физика, платформерный контроллер с окнами прощения,
> спрайты/анимации/камера/частицы, библиотека шейдерных эффектов.

## Порядок раундов #11–#22 (и почему такой)

```
#11 реорганизация  ─┬─→ #12 Windows-ядро ──→ #13 паритет среды разработки
                    │                              │
                    └──────────────────────────────┴─→ #14 каркас фреймворка + ввод
                                                          │
                                    #15 физика-ядро ──→ #16 контроллер + тайлмапы
                                                          │
                                    #17 графика ──────→ #18 материалы и шейдеры
                                                          │
                                              #22 сетевой слой
                                                          │
                                              #19 документация en/ru
                                                          │
                                              #20 инсталляторы
                                                          │
                                              #21 глобальный аудит
```

- **#11 первым** — все последующие раунды пишут код в новую раскладку; переезд позже стоил бы
  переписывания их путей.
- **#12–#13 до фреймворка** — иначе слой #14–#18 пишется в POSIX-предположениях, и паритет
  превращается в дорогой retrofit. Сейчас на Windows не собирается ассет-пайплайн, аудио,
  hot-reload, изоляция плагинов и весь инструментальный слой IDE (15 блоков `if(NOT WIN32)`,
  24 POSIX-only шага CI).
- **#15 → #16 и #17 → #18** — жёсткие зависимости внутри пар; сами пары друг от друга не зависят
  и могут идти в любом порядке.
- **#22 между #18 и #19** (решение владельца 2026-09-02) — по тем же двум аргументам, что стоят
  ниже: сетевой слой добавляет шов в `platform/`, API снапшота состояния и меняет публичную
  поверхность фреймворка, поэтому документация и аудит до него писались бы по коду, который
  заведомо изменится. Раньше #18 не ставится, потому что #18 мал, замыкает графическую арку и с
  сетью не пересекается вовсе: шейдеры в симуляцию не входят.
- **#20 раньше #19** (решение владельца 2026-09-04) — при том, что спека #20 наследует #19
  «инструкция установки». Порядок обратный записанному, и это осознанно: текст установки пишет сам
  раунд #20 по тому, что в нём собрано, а #19 документирует уже закрытое — писать его до того, как
  пакет существует, значило бы документировать намерение.
- **#19 после фреймворк-серии** — документировать то, что ещё не закрыто гейтами, значит писать
  документацию дважды.
- **#21 последним** — аудит до завершения серии проверял бы код, который заведомо будет переписан.

## Модельная стратегия работы (Claude Code)

- Спеки/интервью/архитектура: **Opus 4.8**.
- Лёгкие/рутинные секции: **Sonnet 5** (экономит недельный лимит Max 5x).
- Тяжёлые автономные прогоны реализации: **Fable 5** — но жжёт лимиты ~2× быстрее Opus,
  для активной разработки рассматривать Max 20x ($200) или API pay-as-you-go.
- План $100 (Max 5x) достаточен для фазы спек.
