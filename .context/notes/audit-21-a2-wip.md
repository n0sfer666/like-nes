# Аудит #21, срез A·2 + гейт 9 — состояние для продолжения (2026-09-15)

Ограничение владельца: самостоятельных решений нет, всё через интервью (AskUserQuestion).
Стоп-точка: весь срез A·2 (находки 1–10) + гейт 9, затем СТОП. B1 и B5 — в гейт 10.

## Пачка 1 (A·2·1, A·2·1b, A·2·6, попутная по конверту бандла, A·2·2) — ЗАКОММИЧЕНА 2026-09-17

Восемь коммитов кода и тестов в `dev` (девятым идёт этот же docs-коммит с вердиктами), каждый
собран и прогнан сам:

| SHA | коммит |
|---|---|
| `6f08e44` | `refactor(input)` — счёт логических осей одной функцией |
| `feef73b` | `test(input)` — гейт читателя секции на ТОЙ реализации, которая была |
| `95469d1` | `fix(input)` A·2·1b — потолки строк и имён |
| `f94ab5c` | `fix(input)` A·2·1 — курсоры-срезы и граница пары |
| `08a69c1` | `fix(framework)` A·2·6 — база у четырёх секционных читателей |
| `88073fb` | `fix(asset)` — база у конверта бандла (попутная находка) |
| `5732e69` | `fix(plugin)` A·2·2 — песочница одна, хозяин отвечает за своё состояние |
| `961fcc0` | `test(asset)` — align-гейт пинит ТРЕБОВАНИЕ формата, а не свою же константу |

Вердикты с этими SHA — `.context/notes/audit-verdicts.md` (секции A·2·1, A·2·1b, A·2·6 и четыре
круга ревью). Дальше по роадмапу: A·2·3 … A·2·10, гейт 9, затем README строка 21, статус спеки #21
и dev-log — всё через интервью, как и раньше.

Ниже — история сборки этой пачки, она нужна для продолжения среза.


Снапшоты шагов лежат ВНЕ репозитория: ~/.claude/projects/-Users-n0sfer--dev-like-nes/a2-steps/
(в `.claude/a2-steps/` их держать нельзя — line_budget.py считает рабочее дерево целиком).
`build_check` PASS, lint PASS, тесты PASS — ПОСЛЕ правок по ревью.

Правки по первому ревью СДЕЛАНЫ (см. ниже), пачка пересобрана, все проверки зелёные, запущено
повторное ревью. Решения владельца по находкам первого ревью:
- **pair_axis**: граница — ЧИСЛО ЛОГИЧЕСКИХ ОСЕЙ пресета, а не строк (`pair` в пекаре —
  логический номер, `preset_validate.cpp:127`). В `open()` посчитать логические оси (≤64 строк,
  ≤2016 strcmp) и требовать `pair < logical`. Фикстура: два пресета + пресет, где строк больше, чем
  осей; отказ при pair = logical, позитивный контроль при logical−1; поправить текст сообщения
  (не «last row»). Идёт в коммит A·2·1.
- **MAX_NAME = 64**: `open()` требует ноль в пределах 64 байт у name_offset строк пресетов,
  действий и осей (memchr); пекарь отказывает длинное имя с номером строки (`preset_parse.cpp`
  :42, :61, :82). Тесты: отказ на 65, позитивный контроль ровно на 64 (фикстура порчи — затереть NUL
  имени, которое НЕ последнее в блобе). Идёт в коммит A·2·1b.
- Устаревшие комментарии refusal-теста («шесть строк…», ~:72 и ~:97) — обновить.
- `framework_tilemap` в линковке align-теста — СДЕЛАНО (порядок character, tilemap, platform_core —
  иначе ld duplicate warning).
- `BundleView::open` не проверяет выравнивание base — чинить ОТДЕЛЬНЫМ коммитом + тест сдвига, и
  записать попутной находкой в audit-verdicts.md.
- taste: `static_assert(MAX_AXIS_ROWS >= ::input::MAX_AXES)` в preset_validate.cpp; перенос строки
  110 колонок в preset_validate.cpp:87.
Потом: повторное ревью, blast-radius (framework_preset_test :112-118, game.bundle, golden
`preset-input-hash = 0x416969081dcf230d`, framework_golden_test, framework_rt_test).

Серия коммитов: refactor(input) split → test(input) reader-тест отдельной целью → A·2·1b (потолок
строк + имён) → A·2·1 (курсоры + pair) → A·2·6 (4 читателя + align-тест) → BundleView align.
Вердикты A·2·1/1b/6 + попутная — отдельным docs(context) с SHA.

## Решения по остальным находкам (уже получены)

- A·2·2: store limiter 16 МиБ, 1 инстанс; топливо 1 000 000 на каждый apply(), расход печатать;
  исчерпание = trap = плагин выключен. Гейт памяти: grow сверх лимита → −1 → unreachable → TrapOnCall
  (в C-API v26 нет trap_on_grow_failure). Третий гейт — бесконечный цикл → trap. Голден
  `0x7d9a6e60cbed4156` обязан воспроизвестись; гейт локальный (wasmtime только в deps/).
  Снять/уточнить слово «untrusted» в engine/plugin/CMakeLists.txt:71 по факту.
- A·2·3: `MAX_PLAYERS = 64` новой константой в platformer_replay_io.hpp, проверка players первой
  для обеих веток size_matches.
- A·2·4: потолок 4096 на КАЖДЫЙ список переноса (stats и unlocked), сверх — отброс; число — через
  аксессор `dropped_count()` рядом с `carried_count()`, сигнатура restore не меняется.
- A·2·5: сверять bundle_hash в BundleView::open при trusted==false, диагностика «file corrupted»;
  проверить штамп example_ugly_game/assets/game.bundle.
- A·2·7: api_version манифеста до dlopen, abi() оставить.
- A·2·8: отказ всей сцены на неудачном from_json, диагностика entity · component · line.
- A·2·9: по предписанию отчёта. A·2·10: nullptr, потолок n, noexcept try/catch на thunks, дубль
  asset-codec.
- Гейт 9: свой детерминированный seeded-мутатор над реальными файлами дерева, 3 ОС, ASan/UBSan
  (Linux), ASan (clang-cl Windows); читатели — конверт бандла, секции LNFI LNTM LNAR LNMT LNAM LNAP
  LNFM, сцена IDE, манифест плагина, сейв достижений, реплей; позитивный контроль — сломанный
  читатель обязан пойматься. Файла гейта ещё нет — завести `.context/gates/<гейт>.md`.

После среза: README строка 21, статус спеки #21, dev-log, СТОП.


## Серия коммитов — раскладка по файлам (составлена после повторного ревью)

Каждый коммит собирается и прогоняется САМ: промежуточные версии файлов пишутся из снимков в
скрэтчпаде, потому что `git add` стейджит файл целиком, а `ci.yml`, `preset_open.cpp` и оба
тест-файла меняются в нескольких коммитах.

1. `refactor(input)` — вынос открытия таблицы в свой TU + общий счёт логических осей.
   `preset_open.cpp` (тело старого `open()` дословно + `view<T>`), `preset_axes.hpp`, `presets.cpp`,
   `presets.hpp`, `preset_validate.cpp` (только замена копий на общую функцию),
   `input/CMakeLists.txt` (исходник библиотеки), `framework_preset_test.cpp` (согласие трёх
   счётчиков), `line_budget_allow_code.py` (снять `presets.cpp`, выписать `framework_preset_test.cpp`).
2. `test(input)` — отказы КОНВЕРТА секции своей целью. `framework_preset_reader_test.cpp` (только
   утверждения, верные для старого `open()`: магия, версия, `total_size`, границы смещений,
   терминатор блоба), `input/CMakeLists.txt` (цель + шапка «ТРИ цели»), `ci.yml` (цикл framework).
3. `fix(input)` A·2·1 — курсоры-срезы и граница пары. `preset_open.cpp` (`slice_fits`, `slices_fit`,
   `pair < logical`, самопара), `preset_validate.cpp` (отказ пекаря на самопаре),
   `framework_preset_reader_test.cpp`, `framework_preset_refusal_test.cpp`.
4. `fix(input)` A·2·1b — потолки. `preset_format.hpp`, `preset_open.cpp` (`name_fits`/`names_fit`,
   потолок пресетов, потолок строк внутри `slices_fit`), `preset_validate.cpp` (`static_assert` +
   потолок строк), `preset_parse.cpp`/`.hpp` (`preset_name_fits`, потолок пресетов),
   `preset_pad_parse.cpp`, оба теста, `line_budget_allow_code.py` (reader-тест).
5. `fix(framework)` A·2·6 — выравнивание базы у четырёх секционных читателей. `profile_read.cpp`,
   `atlas_read.cpp`, `map_read.cpp`, `preset_open.cpp`, `framework_section_align_test.cpp`,
   `framework/CMakeLists.txt`, `ci.yml`.
6. `fix(asset)` — выравнивание базы у конверта бандла. `bundle_view.hpp`/`.cpp`,
   `asset_align_test.cpp`, `asset/CMakeLists.txt`, `tools/assetc/verify_game.cpp`, `ci.yml`.
7. `fix(plugin)` A·2·2 — лимит памяти и топливо. `wasm_host.hpp`/`.cpp`, `plugin_wasm_test.cpp`,
   `plugin/CMakeLists.txt`.
8. `docs(context)` — вердикты A·2 с настоящими SHA (черновик в скрэтчпаде `verdicts-a2.md`).

Перед КАЖДЫМ коммитом — перештамповать `head` в `.claude/.verify-state.json` и
`.claude/.review-state.json` отдельным вызовом.

## Второй круг ревью — куда лёг каждый файл серии

Решения владельца (интервью): фикстуры потолков — НА ГРАНИЦЫ; `tables` и `memories` — обе
фикстуры; бак топлива поднят до 10 000 000 при том же правиле порога «расход < 1% бака»; цена
`open()` линеаризована общей картой `row→ось` + тест согласия; `framework_preset_test` добавлен в
шаг ASan; asset-прогоны в CI грепают вердикт; комментарий про заворачивание uint32 переписан.

- Коммит 3 (`fix(input)` A·2·1): `logical_axis_map()` в `preset_axes.hpp`, её вызов в
  `slices_fit`, тест согласия карты в `framework_preset_test.cpp`, ALLOW этого файла 225 → 242.
  Именно здесь появляется обход пар, то есть здесь же и его цена — оптимизация не отстаёт от
  находки на коммит.
- Коммит 4 (`fix(input)` A·2·1b): комментарий у проверки смещения имени пишется СРАЗУ верным —
  оба конца отбивает `offset >= size` в `name_fits`, до арифметики `size - offset` дело не доходит.
- Коммит 5 (`fix(framework)` A·2·6): в новом шаге ASan сразу три сборки (reader, preset, align) и
  оговорка про рукописный `engine/input/action_map.cpp` (каталог `engine/input` целиком не
  собирается без GLFW и evdev, а `input_core` — ровно один TU).
- Коммит 6 (`fix(asset)`): `tee` + `grep` вердикта для `asset_align_test`, `asset_test_san` и
  `asset_align_san`.
- Коммит 7 (`fix(plugin)` A·2·2): границы фикстур, две новые фикстуры лимитера, бак 10 000 000,
  `~Impl`, `delete p_` на отказе `make_sandbox`, `fuel_used_ = 0`, `HostError` на отказе `set_fuel`.

## Третий круг ревью — куда лёг каждый файл серии

Ревью вернуло FAIL (2 medium, 6 low). Обе medium проверены пробой на wasmtime v26 до правок.

- Коммит 3 (`fix(input)` A·2·1): предусловие буфера в `preset_axes.hpp`; блок согласия карты в
  `framework_preset_test.cpp` переписан на ДВА набора ровно в `MAX_AXIS_ROWS` (сплошные тройки и
  чередование `ax(i % 5)`); запись ALLOW поднята 242 → 245 и обоснование переписано честно — оно
  называло юнит-тест шаблона «путём манифест → InputFrame».
- Коммит 6 (`fix(asset)`): охрана `[ -n "$L" ]` перед запуском `asset_align_test` в `ci.yml`.
- Коммит 7 (`fix(plugin)` A·2·2): заправка бака внутри `make_sandbox` (иначе `(start)` трапает на
  инстанцировании и приезжает `LinkError`); единая политика отказов `init()` — Impl не удаляется
  нигде, поэтому `apply()` не затирает причину; `init_wat()` и третий аргумент `fueled` у
  `wasm_run_escape` как входы для двух новых фикстур; четыре фикстуры в `plugin_wasm_test.cpp`
  (конечный и бесконечный `(start)`, песочница без счётчика → `HostError`, расход трапнувшего
  вызова = 0); комментарий про умолчания лимитера разведён на элементы и штуки; четыре C-style
  каста заменены на `static_cast`. Чтобы влезть в бюджет, `read_file` (обёртка в три строки вокруг
  `platform::read_text`, один вызов) заинлайнен, а неиспользуемые `<sstream>`/`<vector>` убраны.

## Четвёртый круг ревью — две новые цели в серии

Ревью вернуло FAIL (2 medium, 2 low, 2 taste). Все шесть подтверждены чтением кода до правок;
обе medium и обе low — развилки, закрытые интервью, обе taste владелец взял целиком.

- Коммит 3 (`fix(input)` A·2·1): блок согласия счётчиков осей уезжает из `framework_preset_test.cpp`
  в НОВУЮ цель `framework_preset_axes_test` — у файла было две ответственности, а это сильнее
  вопроса о запасе строк. Запись ALLOW опускается 245 → 225, обоснование называет вынос. В `ci.yml`
  новая цель добавлена и в цикл `for T in framework_…`, и в шаг ASan отдельной сборкой: под
  санитайзером обязан быть ТОТ файл, который пишет в стековый буфер шириной ровно в потолок
  формата, а не интеграционный тест, собираемый ради него.
- Коммит 7 (`fix(plugin)` A·2·2): Impl создаётся ДО чтения файла, а вход сторожит `ready_`, а не
  `p_` — седьмая ветка отказа `init()` больше не теряет причину, а провалившаяся инициализация
  повторяема (`reset_impl()` + `init_module()`); `bool fueled` заменён на `Fuel::On/Off` БЕЗ
  умолчания (режим без топлива не ограничивает время гостя ничем, и забытый аргумент повесил бы
  прогон навсегда); добавлен исход `WasmOutcome::NoExport` — под общим `LinkError` опечатка в имени
  экспорта красила бы гейт побега; `fuel_used_ = 0` поднят первой строкой `apply()`.
  Контракт объекта `WasmGravity` вынесен в НОВУЮ цель `plugin_wasm_host_test` (вердикт
  `plugin-wasm-host`): гейт побега спрашивает у чужого модуля, что ему разрешили, а этот — у
  хозяина, что он говорит о себе. Туда же уехала фикстура `TWICE_WAT`. Обе цели локальные: им
  нужен wasmtime C-API из `deps/`, которого на раннерах нет.

## Четвёртый круг ревью — разобран (2026-09-17)

Вердикт: FAIL (1 medium, 5 low, 3 taste). Все девять закрыты; развилки прошли интервью (две пачки),
механическое применено прямо.

| находка | что сделано |
|---|---|
| [medium] `fuel_used()` = 0 после трапа | `read_fuel()` зовётся и на пути отказа `apply()`. Проба на v26: после трапа `get_fuel` отвечает штатно, бесконечный гость оставляет 0 (расход = весь бак), `unreachable` — почти полный бак. Два новых ассерта в host-тесте |
| [low] ci.yml «три вызывающих пути» | вызывающих ДВА (`preset_open.cpp:81` и тест согласия); пекарь и рантайм зовут только счётчики. Обе строки переписаны |
| [low] безымянный FAIL в axes-тесте | красная строка называет набор, строку, имя оси и обе цифры |
| [low] `reset_impl()` оставляет `p_` висеть, если `new` бросит | `delete p_; p_ = nullptr; p_ = new Impl();` |
| [low] `take_error()` выброшен на пяти отказах побега | `wasm_run_escape(..., std::string* why = nullptr)`; таблица из 12 побегов печатает `cause:` у красной строки |
| [low] `err_` не чистится на успехе | `err_.clear()` рядом с `ready_ = true` и перед `return true` в `apply()`; ассерт в host-тесте |
| [taste] `Fuel` в глобальном пространстве | переименован в `WasmFuel` |
| [taste] `wasmtime_extern_delete` не зовётся | зовётся на всех выходах обоих файлов (контракт `extern.h`) |
| [taste] две ответственности в `wasm_host.cpp` | раскол на три сущности: `wasm_sandbox.{hpp,cpp}` (песочница и потолки), `wasm_host.{hpp,cpp}` (хозяин), `wasm_escape.{hpp,cpp}` (зонд побега) |

Правило 18 (спорная находка ревью — подтвердилась): `PLUGIN_WASM=OFF` во ВСЕХ шести конфигурациях
`ci.yml`, то есть `plugin-wasm` не гоняется в CI нигде, а `scripts/check_goldens.sh` утверждал
«только в CI». Строка исправлена; сценарий обоих гейтов с построчным ожидаемым выводом — новый
раздел Q в `docs/owner-setup.txt`, абзац-указатель — в `docs/owner-verification.md` («Beyond the
gates», а не новый раздел-гейт: гейт там имеет статус open/closed и попадает в перечень владельца,
а эти два гоняются машиной, просто не раннером).

Неваккуумность новых ассертов — три одиночные мутации, каждая красит свою строку:
`read_fuel()` убран с пути отказа → «trapped call names its OWN spend» и «drains the tank» NO;
`err_.clear()` убран → «a successful init clears the previous reason» NO; `say()` сделана пустой →
«the refusal names itself» пусто, FAIL. Фикстура `TWICE_WAT` при этом переписана: без витков перед
трапом расход трапнувшего вызова РАВЕН расходу удачного (9 и 9), и ассерт не отличал бы свой расход
от застрявшего чужого; с тысячей витков — 6011 против 9.

Состояние гейтов на момент остановки: `build-check` PASS (ноль предупреждений), вся цепочка `lint`
PASS (`line-budget` — 929 файлов, 38 разрешений, 0 находок), `plugin-wasm`, `plugin-wasm-host`,
`framework-preset`, `framework-preset-axes`, `framework-preset-reader`, `framework-preset-refusal`
PASS. Голдены целы: WASM 0x7d9a6e60cbed4156, топливо 9499, `assetc --synthetic` 0xf2255dc74fbdb6bc.
`scripts/check_goldens.sh` целиком требует каталогов `build-full`/`build-ci`, которых в этой сессии
нет — проверено поштучно.

**Коммит 7 серии вырос**: к нему теперь идут `wasm_sandbox.{hpp,cpp}`, `wasm_escape.{hpp,cpp}`,
`docs/owner-setup.txt` (раздел Q), `docs/owner-verification.md` и `scripts/check_goldens.sh`.
Серия НЕ начата: `.claude/.verify-state.json` и `.claude/.review-state.json` не обновлялись,
рабочее дерево не коммичено.

## Пачка 2 (A·2·3 … A·2·5) — в работе 2026-09-17

| SHA | коммит |
|---|---|
| `1836477` | `fix(game)` A·2·3 — потолок ШИРИНЫ прогона у читателя и писателя + раскол гейта на две цели |
| `639fffd` | `fix(game)` A·2·3 — потолок РАЗМЕРА файла реплея: `read_bytes_capped` в шве, `MAX_TICKS`/`MAX_FILE` у читателя и писателя |
| `f1b3bb2` | `fix(achievements)` A·2·4 — потолок 4096 на каждый список переноса, `dropped_count()` рядом с `carried_count()` |
| `a1a4916` | `fix(asset)` A·2·5 — штамп конверта сверяется в `open()`, и отказ говорит, какого он рода |

Раскол гейта — решение владельца по ревью: `game_platformer_replay_io_test.cpp` после правок вырос
до 245 строк, и цена разделения (дубль записи прогона) не платится — вторая цель эталон СОБИРАЕТ,
а не записывает, обстановка вынесена в `platformer_replay_fixture.hpp`.

Вторая половина A·2·3 (потолок РАЗМЕРА файла) закрыта отдельным коммитом, как решил владелец:
`platform::read_bytes_capped` в шве + `MAX_TICKS = 480000` / `MAX_FILE` у читателя И писателя
реплея. Потолок назван в тиках, чтобы граница была ЗАКОННЫМ файлом — иначе гейту нечего утверждать
о самом крае; писатель сверяет его БАЙТАМИ (`bytes_for`), потому что тиковая проверка совпадает с
байтовой только при ширине 1. Потолки выделения вынесены в свою цель
`game_platformer_replay_ceiling_test` (разрез — решение владельца, жёсткий лимит длины).

**Попутная находка (класс, не одна строка): `platform::read_bytes` без потолка.** Шов читает файл
целиком у КАЖДОГО потребителя, и размер выделения диктует тот, кто файл прислал. Непокрытыми
остаются: `engine/asset/codec.cpp:23` (бандл), `tools/assetc/bakers.cpp:17`,
`tools/assetc/bakers_table.cpp:132`, `tools/assetc/verify_game.cpp:87`,
`example_ugly_game/platformer_peer_result.hpp:69`. Реплеи закрыты обе штуки: файл с диска
(`replay_io::read_file`) и реплеи ребёнка в `example_ugly_game/platformer_net_runs.hpp` — тем же
коммитом. Свой файл дерева (испечённый бандл) — не тот же случай, что присланный: разбирать по
потребителям, решение владельца не запрашивалось. Сообщить после среза.

### A·2·7 — вердикт о версии ABI в разборе манифеста

Решение владельца: `parse_manifest` сверяет `api=` с `PLUGIN_API_VERSION`, гейт `abi()` в
`host.cpp:63` остаётся. Проверка стоит там же, где поле разбирается: это единственное место, где
«до dlopen» держится по построению, а не по дисциплине вызывающего. Поле разбиралось и
выбрасывалось — читатель видел объявленную версию и принимал её за работающую проверку.

Развилки, закрытые интервью по двум кругам ревью:

- переполнение `std::stoi` получило СВОЮ причину отказа: цифры, не влезшие в `int`, — это число, и
  звать их «не числом» значит посылать владельца файла искать опечатку там, где её нет;
- вторая строка `plugin` и повторный ключ `api=` — отказ, а не «побеждает последний»: молчаливое
  правило приоритета обходило вердикт одной лишней строкой.

Находка второго круга, которую не поймал первый: `ls >> m.id >> m.version` съедал первый
`key=value`, если поля версии в строке нет, — `plugin id api=1 api=2` принимался как версия 2, и до
счёта повторов доезжал один ключ. Ключи снимаются со строки независимо от позиции; `Manifest::version`
в дереве не читает никто, радиус ограничен `id` и `api_version`.

Фикстур одиннадцать вместо двух: причин отказа шесть, и каждая чинится по-своему.
`empty_api` пинит кавычки вокруг значения, `overflow` — единственный вход в `catch` вокруг
`std::stoi` во всём дереве, `dup_plugin` несёт ЧУЖУЮ версию (так наблюдаем порядок причин),
`dup_api` — без поля версии (так наблюдаем разбор строки идентичности), `no_panels` держит ранний
структурный выход, `api_typo` — форму, которую разбор не читает.

### Неваккуумность A·2·7

| мутация | что покраснело |
|---|---|
| вердикт о версии убран целиком (поведение до A·2·7) | 17 из 21 |
| ранний структурный выход убран | `a manifest with an identity but no panels is refused` |
| ветка дубля перенесена в конец цепочки | `and a doubled identity is said BEFORE the version…` + 3 |
| позиционные поля снова снимаются до ключей (прежний обход) | `a manifest that names api= twice on one line is refused` + 2 |
| переполнение слито с «не число» | `and a number too big is not called 'not a number'…` |
| кавычки вокруг значения убраны | `…quotes included`, `…instead of trailing off after the colon` |
| найденный токен больше не цитируется | `and the refusal quotes the token it did find…` |
| вторая строка `plugin` терпится | `and a doubled identity is said BEFORE the version…` |
| повторный `api=` терпится | `a manifest that names api= twice on one line is refused` + 2 |
| путь убран из новых текстов | `every refusal names the file it is talking about` |
| `!=` ослаблено до `>` | `a manifest declaring a foreign api version is refused` + 2 |

Блэст-радиус правила о дубле: в дереве нет ни одного манифеста с двумя строками `plugin` или
повторным `api=`, кроме новых фикстур; манифест-врезок в `docs/` и `scripts/` нет; производственный
потребитель `parse_manifest` один — `engine/plugin/ui_shell_main.cpp:60`, он читает `ok` и печатает
`error`. Живые `inspector`/`console` подняты до `api=2` и принимаются.

Шаг ASan/UBSan теперь ассертит СТРОКУ, а не только код возврата, и зовёт гейт с
`UBSAN_OPTIONS=halt_on_error=1`: до этого отчёт санитайзера на новом пути через `catch` печатался бы
в stderr мимо `tee` и оставлял шаг зелёным.

### A·2·8 — отказ загрузчика сцены редактора

Решение владельца: битый файл отказывает ЦЕЛИКОМ, сцена остаётся пустой, диагностика называет
строку, сущность и компонент. Форма — `bool deserialize(Scene&, const std::string&, std::string*
why = nullptr)`. Причина: сцена, собранная из части файла, выглядит целой — половина сущностей на
месте, у второй половины позиция (0,0), — и редактор показал бы не «файл битый», а «кто-то
передвинул объекты в начало координат». Первое же сохранение закрепило бы потерю.

Развилки этого захода:

- строка, которой в формате нет вовсе (`garbage`, `E20` без пробела), — тоже отказ (решение
  владельца): иначе в формате осталась бы строка, которую читатель молча выбрасывает;
- `restore_entity` при отказе СНОСИТ сущность (решение владельца) — то же правило, что «пустая
  сцена»: полусобранная сущность выглядит целой, а история при этом уже считает шаг отменённым;
- гейт — своя цель `scene_refusal_test` (решение владельца): у `scene_roundtrip_test` предмет
  «целый текст читается байт-в-байт», здесь «битый не читается вовсе».

Своя находка сверх четырёх форм интервью: `strtoull` на `E 20abc` молча останавливался на букве и
отдавал 20 — сущность рождалась под номером, которого в файле нет. Проверка усилена до «цифрой
обязана быть КАЖДАЯ», это та же названная причина, а не седьмая.

Заход ревью (`code-reviewer`: 1 high, 2 medium, 5 low, 1 taste — исправлено всё), три развилки в нём
закрыты решением владельца:

- **седьмая причина отказа** (`high`): все символы — цифры, но число не влезает в `uint64_t`.
  `strtoull` насыщался до `ULLONG_MAX` и выставлял `ERANGE`, которого никто не читал, — сохранение
  записывало `E 18446744073709551615` вместо строки файла. Теперь `std::from_chars` и своя причина
  `entity id does not fit`: опечатка чинится правкой символа, переполнение — другим номером вовсе
  (то же решение, что у `api=` в A·2·7). Причин отказа стало **семь**;
- **`CommandBus::last_error()` убран** (решение владельца): канал причины наружу завёлся бы без
  читателя и без гейта, а `std::function<void()>` вернуть её всё равно не может. На шину сегодня
  опирается ровно то, что отказ снимка оставляет сцену БЕЗ сущности, а не с полусобранной;
- **свой префикс `snapshot line N`** (решение владельца): строка снимка и строка файла сцены —
  разные тексты, и номер под общим именем `line N` увёл бы искать правку в произвольное место сейва.

Остальное из ревью: `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1` на все три san-сборки IDE;
цитата битой строки режется по потолку 64 байта с откатом назад по продолжающим байтам UTF-8
(разрезанный посередине символ — мусор в панели там, где ждали имя объекта); имя причины
приклеивается к КАЖДОМУ утверждению цикла (`[needle]`), иначе красная строка не говорит, какая из
семи сломалась.

Разбиение файла (решение владельца): `serialize.cpp` перевалил мягкий лимит 200 строк, и обоснования
из закрытого списка (`data`/`text`/`single`) честного не нашлось — у разбора своё имя и своя
ответственность. Запись осталась в `serialize.cpp` (56 строк), разбор уехал в `deserialize.cpp`
(167 строк), заголовок один. `scene_core` в CMake и пять рукописных san-сборок в `ci.yml` получили
новую единицу трансляции: рукописный список исходников равняется ЦЕЛИ, а не «что зовёт прогон».

#### Второй заход ревью (после разбиения файла) — решения владельца

Ревью на уже разбитом коде дало ещё четыре развилки, все закрыты интервью:

- **разбор идёт во ВРЕМЕННУЮ сцену**: `deserialize` сперва собирает `Scene probe`, и только когда
  весь текст прошёл, чистит сцену вызывающего и собирает её заново. Прежний порядок звал `clear()`
  до первой проверки — открытый документ погибал от чужого файла, выбранного по ошибке. Гейт
  утверждает про СЕРИАЛИЗАЦИЮ (`serialize(live) == before`), а не про наличие сущности: пережить
  отказ обязано и значение;
- **не-NULL от `from_json` ≠ «значение прочиталось»**: `const char* flecs::world::from_json<T>`
  отдаёт указатель на ОСТАТОК текста, поэтому `{"n":"hero"}` вместо `{"value":"hero"}`, `{}` и
  хвост мусора после json принимались молча, а компонент оставался `T{}` — та же тихая (0,0), ради
  которой находка заведена. Теперь значение переизлучается `to_json<T>` и сверяется с исходным
  текстом: одна проверка на все типы, она же ловит хвост. Причин отказа стало **двенадцать**, их
  список и порядок — одним местом в `serialize.hpp`;
- **сверка с каноном игнорирует ПРОБЕЛЫ**: писатель ставит пробел после запятой, правленный руками
  файл обычно не ставит, и отказ за это был бы отказом за форматирование. Вычерк огульный, вместе с
  пробелами внутри литералов: сверка сравнивает текст с переизлучением ЭТОГО ЖЕ текста, внутри
  литерала переизлучение сохраняет байты как есть — значит вычерк симметричен. Прежняя версия
  `squeeze` умела различать литералы; мутационный прогон показал, что отличить её от огульной
  нечем ни одной фикстурой, то есть те десять строк были кодом без проверки, и они сняты;
- **исход отмены ВОЗВРАЩАЕТСЯ шине**: `Command::undo` теперь `std::function<bool()>`, а
  `CommandBus::undo()` не уводит транзакцию в `undone_`, если отмена не состоялась целиком. Прежде
  `restore_entity` звали ради побочного действия: снимок, который не разобрался, оставлял сцену без
  сущности, а история шагала вперёд — шина считала шаг отменённым, и redo предлагал повторить то,
  чего не было. `redo` исхода не возвращает и не должен: отменить шаг можно только из текста
  снимка, который мог написать другая сборка, а повторить — тем же кодом, что уже отработал.

Разбиение гейта (решение владельца): один файл гейта перевалил жёсткий лимит 250. Предметов в нём
два — ФАЙЛ сцены (двенадцать причин, своя шапка) и ТЕЛО СНИМКА undo (без шапки, без строк `E`,
исход уходит в шину), — и общий гейт не сказал бы, который из текстов сломался. Итог: две цели
(`scene_refusal_test` 200 строк, `scene_undo_refusal_test` 74) плюс общая фикстура
`refusal_fixture.hpp` (74) — счёт провалов, поиск причины в тексте и сами фикстуры, взятые У
СЕРИАЛИЗАТОРА. Последнее — тоже находка ревью: шапка была написана руками как `C Name {"n":"hero"}`,
текста, которого `serialize` не пишет НИКОГДА (член зовётся `value`), то есть контроль «целое
читается» стоял на битом тексте и держал ровно тот дефект, против которого заведён гейт.

`deserialize.cpp` (241 строка) выкуплен в `scripts/line_budget_allow_code.py` причиной `single`:
сам ПОРЯДОК двенадцати проверок есть часть формата (шапка до первой `E`, номер до дубля, имя
компонента до его значения), и у выделяемого куска нет собственного имени.

`IDE_CORE` в `.github/workflows/ci.yml` (три san-шага) получил маркер
`# ci-lint: mirrors-target tools/ide/CMakeLists.txt scene_core` — правило `list-drift` следит, что
рукописный список равен ЦЕЛИ. Маркер проверен неваккуумным в ОБЕ стороны: и убранный файл, и
лишний дают свою строку FAIL.

#### Третий заход ревью — решения владельца

Ревью после второго захода вернуло **1 high, 4 medium, 8 low, 1 taste**, и high я внёс сам этим же
заходом: `CommandBus::undo()` на отказе ТЕРЯЛ транзакцию — группа оставалась разобранной наполовину,
и повторить отмену было уже нечем, `can_undo()` про неё не знал. Восемь развилок закрыты интервью:

- **отмена целиком или не тронута**: отказ ломает цикл на первой же неудаче, уже отменённые команды
  группы накатываются назад через `redo()`, транзакция возвращается в `done_`. Иначе drag из
  пятидесяти `set` отменялся бы наполовину и оставался в таком виде навсегда — состояние, которого
  пользователь не создавал никогда;
- **`restore_entity` тоже разбирает во временную сцену**: прежде разбор шёл прямо в сцену, а
  `Scene::create` на существующей сущности зовёт `destruct()` — то есть неудачная отмена СНОСИЛА
  живую сущность на занятом guid и оставляла на её месте огрызок. Теперь занятый guid переживает
  битое тело целиком, и гейт утверждает про `serialize_entity`, а не про наличие;
- **дубль имени компонента — тринадцатая причина** (в итоговом порядке одиннадцатая): второй
  `set<T>` молча перетирал первое значение, файл с двумя `C Position` грузился как файл со второй
  строкой, а пересохранение писало одну. Набором владеет вызывающий: у файла сцены он чистится на
  каждой `E`, у тела снимка живёт один на всё тело;
- **шапка формата — одной константой**: `SCENE_HEADER`/`SCENE_HEADER_PREFIX` в `serialize.hpp`.
  Прежде её текст был написан руками тремя несвязанными копиями (запись, точное сравнение, префикс
  версии) — та же болезнь, против которой заведён сам список причин;
- **`[[nodiscard]] bool undo()`, вызывающих привести**: выброшенный исход — ровно тот дефект, ради
  которого возврат заводился. Одиннадцать вызывающих правлены: в `command_test` исход стал
  утверждением, в `perf_test` — явным `(void)` (предмет замера время), в `editor_gate6` — новым
  `check(undo_ok, …)` (гейт читает позицию СРАЗУ после отмены, и молча не состоявшаяся отмена дала
  бы ему позицию после drag под видом позиции до), в кнопке `Undo` редактора — `(void)` с причиной;
- **ведущий ноль — своя причина** (четвёртая): все символы тут цифры, и чинится он не правкой
  символа, а стиранием нуля. `E 007` разбирался в 7, пересохранение писало `E 7` — два текста
  одного номера, тот же дубль, что у причины 6, только невидимый глазу;
- **два прохода остаются, цена измеряется**: `perf_test` получил Bench E — загрузка файла
  10k-сцены. Инвариант «пусто или целое» стоит ровно одного лишнего прохода, и цена обязана быть
  ИЗМЕРЕНА, а не предположена. На этой машине 32 мс при пороге 400 мс (порог держит самая медленная
  машина CI, не эта). Недостижимая ветка отказа второго прохода оставлена и подписана: она стоит за
  ИНВАРИАНТ, а не за случай;
- **разрез по уровням**: восемь решений подняли `deserialize.cpp` до 264 строк при жёстком лимите
  250, а жёсткий лимит не выкупается причиной вовсе — гейт прямо велит спросить владельца. Шов —
  «строка» против «текста»: `parse_component.{hpp,cpp}` (111+25 строк) знает ОДНУ строку
  `C <Name> <json>` и причины 9–14, `deserialize.cpp` (170) — оба ТЕКСТА и причины 1–8. Заголовок
  приватный: снаружи у разбора строки читателей нет и быть не должно. Оба файла под мягким лимитом,
  записи в ALLOW не понадобилось — прежняя запись для `deserialize.cpp` снята.

Причин отказа стало **четырнадцать**; порядок в списке — порядок проверок в коде. Механические
находки ревью закрыты там же: `flecs::string::c_str()` может быть NULL (`std::string(nullptr)` —
UB, теперь под явной проверкой), фикстура «чужой член» съехала на свою сущность (у сущности 10
`Name` уже есть шапкой, и её первой ловила бы новая причина 11 — гейт покраснел на этом сразу и
показал, что находка настоящая), `says(reason, "line ")` требует за словом ЧИСЛО, а не подстроку,
добавлены фикстуры CRLF обоим текстам.

### Четвёртый заход ревью A·2·8 — куда лёг каждый пункт

Вердикт: 0 critical, 0 high, 4 medium, 6 low, 1 taste. Закрыты все одиннадцать.

| находка | как закрыта |
|---|---|
| [med] контроль Bench E утверждал ВРЕМЯ (`load_ms > 0`), а под санитайзером порог времени выключен вовсе | утверждение о ЧИСЛЕ сущностей через `check`, а не `check_time`; проверено подменой `text_10k` |
| [med] `Scene probe` жила до конца функции — пик памяти на открытии удваивался | проба в своей области видимости, обе — и в `deserialize`, и в `restore_entity` |
| [med] `quoted()` держал рез по границе символа, но не UTF-8 самих байтов | санитайзер: всё, что не печатный UTF-8, идёт в цитату как `?`; строгость та же, что у проверки гейта |
| [med] гейт наката группы пинил n=1 — обратный порядок наката проходил | группа из трёх команд: две правки Position ловят ПОРЯДОК, Velocity — ДЛИНУ хвоста |
| [low] `valid_utf8` лежал в общем заголовке при одном потребителе | второй потребитель настоящий: у гейта снимка своя фикстура сырых байтов |
| [low] у гейта снимка пинилась только `snapshot line 1` | тело из двух строк, сломана вторая, ждём `snapshot line 2` |
| [low] `command.hpp:60` звал `restore_entity` с `why = nullptr` — Undo молчал | причина уходит в `stderr`; гейта нет и быть не может — ветка недостижима через публичный путь, см. ниже |
| [low] `#` и пустые строки принимаются и теряются, а обоснование причины 4 это отрицало | решение владельца: не отказывать, обоснование сужено, потеря записана как осознанная |
| [low] `perf_test` 249/250 — чинить вакуумный контроль было нечем | решение владельца: харнесс уехал в `perf_fixture.hpp`, файл 195 строк, запись в ALLOW снята |
| [low] таблица неваккуумности обещала 28 строк при 23 и цитировала снятое решение | таблица перегенерирована прогоном: 35 строк, числа из вывода |
| [taste] `perf_test` не включал `serialize.hpp` явно | включает |

**Одна строка осталась без гейта осознанно.** `fprintf(stderr, "undo refused: …")` в `command.hpp`
недостижим через публичный путь: снимок пишет `serialize_entity`, и он разбирается ВСЕГДА — по той
же причине, по которой ложная ветка `undo()` пинится собранной руками командой. Дойти до этой
строки может только снимок из ЧУЖОЙ сборки, ради которого `restore_entity` и умеет отказывать.
Мутация на неё была бы мутацией на недостижимый код.

**Ещё одна — `quoted(name)` в сообщении о дубле компонента.** Сырое имя туда не доходит: `seen`
пополняется ДО `apply_component`, то есть первая же строка с неизвестным именем валит разбор, и до
второй дело не идёт. Вызов оставлен не для случая, а ради правила «ни один байт из файла не попадает
в `*why` без `quoted`»: правило это проверяется чтением, а доказательство недостижимости придётся
переделывать каждому, кто переставит две строки местами.

### Пятый заход ревью A·2·8 — куда лёг каждый пункт

Вердикт: 0 critical, 0 high, 0 medium, 4 low, 2 taste. Закрыты все шесть.

| находка | как закрыта |
|---|---|
| [low] `quoted()` на 64+ продолжающих байтах отдавала пустую цитату `'...'` | откат, упёршийся в ноль, возвращается к MAX; заведена фикстура и мутация под неё |
| [low] шапка `serialize.cpp` всё ещё говорила «двенадцать причин» при четырнадцати | счёт исправлен; это ровно та пересортица, против которой единый список и заведён |
| [low] заголовок обещал «сцена НЕ ТРОНУТА» и на отказе ВТОРОГО прохода, где это неверно | контракт записан двухуровневым: первый проход — не тронута, второй (недостижим) — пусто / guid снесён |
| [low] `UBSAN_OPTIONS=halt_on_error` стоял в одном санитайзерном шаге CI из трёх, что трогает дифф | тот же `export` в шаги perf и editor data-path: без него UBSan пишет в stderr и возвращает 0 |
| [taste] комментарий в `command.hpp` ссылался на `break`, которого в цикле нет | «выход по отказу» |
| [taste] декодер UTF-8 продублирован в `refusal_fixture.hpp` | оставлено: это НЕЗАВИСИМЫЙ оракул гейта, общий код сделал бы проверку проверкой самой себя |

Отдельно ревью подтвердило три осознанных пробела (недостижимый `fprintf` в `command.hpp`,
недостижимый `quoted(name)` на ветке дубля, потеря оформления при пересохранении) и добавило к
третьему два входа того же класса: пустые строки ДО шапки и пробелы внутри значения. Оба
записаны в обоснование при ветке `#` в `deserialize.cpp` — иначе следующий заход перерешает это
заново.

### Неваккуумность A·2·8

Прогон сделан шестью заходами: до первого ревью и после каждого из пяти. Ниже числа ПОСЛЕ
ПЯТОГО: **36 мутаций, вакуумных ноль**. Причин четырнадцать, разбор разъехался на два файла
(`parse_component.cpp` — строка, `deserialize.cpp` — тексты). Против прошлого захода прибавились
мутации на санитайзер цитаты (сырые байты, управляющие, пересортица и суррогаты), на сырое имя
компонента в сообщении, на счёт строк снимка и на ДВА способа сломать накат группы — порядок и
длину хвоста. Соседние гейты (`scene-roundtrip`, `command-undo`) остаются зелёными везде, кроме
трёх строк, где мутация ломает `restore_entity` или `undo()` глобально: там сосед краснеет законно.

Замер Bench E (`perf_test`) в сводку не входит — он не гейт отказа, и прогоняется отдельно. Его
контроль проверен той же дисциплиной: подмена `text_10k` на `"garbage"` красит именно
`the load bench loaded the whole scene, not a refusal on line one`, тогда как прежний `load_ms > 0`
эту подмену пропускал (замер показал 1.9 ms — время у отказа тоже положительное).

| мутация | что покраснело (первая строка) | всего |
|---|---|---|
| шапка не проверяется вовсе | `a file that is not a scene at all is refused before a single entity is born` | 8 |
| чужая версия не отличается от чужого файла | `the refusal says which cause it is [unsupported format version]` | 1 |
| цифрой проверяется только первый символ | `digits followed by letters are refused, not silently read as 20` | 4 |
| ведущий ноль снова проходит | `an entity id with a leading zero is refused, not renumbered on the next save` | 4 |
| переполнение номера не читается | `the refusal says which cause it is [entity id does not fit]` | 1 |
| дубль номера снова молча сносит первый блок | `an entity id used twice is refused, not silently collapsed onto the first block` | 4 |
| неизвестная строка снова пропускается | `the refusal says which cause it is [unrecognized line]` | 1 |
| компонент до первой E снова принимается | `CRASH(exit -11): гейт упал, не назвав строки` | 1 |
| файл сцены перестал съедать CR | `a scene file with CRLF line ends is taken` | 2 |
| разбор снова идёт прямо в сцену вызывающего | `and the scene it was given back is byte-identical to before` | 1 |
| строка C без значения снова проходит | `the refusal says which cause it is [component line has no value]` | 1 |
| пустое имя компонента снова уходит в таблицу | `the refusal says which cause it is [component line has no name]` | 1 |
| дубль компонента снова молча перетирает значение | `the same component twice on one entity is refused, not silently overwritten` | 4 |
| исход from_json снова выбрасывается | `the refusal says which cause it is [bad json]` | 1 |
| сверка с каноном снята | `a member the format does not have is refused, not read as an empty value` | 8 |
| сверка стала побайтовой (пробелы значимы) | `a file written without the writer's spacing is still taken` | 2 |
| squeeze перестал вычёркивать пробелы | `a file written without the writer's spacing is still taken` | 2 |
| цитата больше не обрезается | `a line too long to quote whole is cut short` | 1 |
| рез цитаты больше не ищет границу символа | `and a whole character is kept, not swapped for a marker the file never had` | 1 |
| цитата отдаёт байты файла как есть | `and the refusal is valid UTF-8, not the bytes the file handed over` | 4 |
| рез цитаты отдаёт пустую цитату на сплошных продолжающих | `and its quote still shows the bytes as markers, not an empty pair of ticks` | 1 |
| цитата пропускает управляющие байты | `and no escape byte from the file reaches the terminal as a command` | 2 |
| цитата принимает пересортицу и суррогаты | `and the refusal is valid UTF-8, not the bytes the file handed over` | 1 |
| имя компонента снова идёт в сообщение сырым | `a refusal names the line, the entity and the component` | 1 |
| снимок называет номер строки как файл сцены | `and a snapshot says which text its line number belongs to, counting its own lines` | 1 |
| снимок принимает строку не из формата | `and it says so instead of quietly restoring what it could read` | 1 |
| снимок перестал съедать CR | `a snapshot body with CRLF is read back` | 2 |
| снимок разбирается прямо в сцену вызывающего | `and the entity that was standing there is byte-identical to before` | 4 |
| снимок требует шапку файла | `and a snapshot says which text its line number belongs to, counting its own lines` | 10 |
| восстановление снимка всегда отказывает | `and a snapshot says which text its line number belongs to, counting its own lines` | 10 |
| исход отмены снова выбрасывается шиной | `control: and the entity is back` | 6 |
| отказ отмены снова теряет транзакцию | `and the step that was not undone is still there to be undone` | 3 |
| номер строки снимка всегда единица | `and a snapshot says which text its line number belongs to, counting its own lines` | 1 |
| накат группы идёт с конца, а не с начала хвоста | `and the commands that had already been undone are rolled forward, in order, byte for byte` | 1 |
| накат группы теряет первую из успевших | `and the commands that had already been undone are rolled forward, in order, byte for byte` | 1 |
| отменённые команды не накатываются назад | `and the commands that had already been undone are rolled forward, in order, byte for byte` | 1 |

Шесть строк этой таблицы появились по итогу ПРОГОНА, а не по замыслу — первый заход показал у них
ноль красных строк, и каждая оказалась настоящей находкой против моего же кода:

1. **«компонент до первой `E`»** — вакуума нет, гейт падает с segfault: снятый guard уводит
   `apply_line` в `set<T>()` на нулевой сущности. Вакуумным был ХАРНЕСС, считавший красными только
   строки `FAIL:`. Решение владельца: падение — тоже сигнал, харнесс научен считать ненулевой
   выход красным, продакшен-код не трогаем (второй guard был бы защитой от недостижимого
   состояния, то есть опять кодом без проверки).
2. **«`squeeze` перестал видеть литералы»** — вакуум настоящий и неустранимый: сверка симметрична.
   Решение владельца — упростить `squeeze` до огульного вычерка пробелов; мутация заменена на
   «`squeeze` перестал вычёркивать пробелы», и она краснит.
3. **«исход отмены снова выбрасывается шиной»** — ложная ветка `CommandBus::undo()` была не
   запинена: снимок, который пишет сам редактор, разбирается ВСЕГДА, и через публичные
   `destroy_entity`/`undo` до отказа не дойти. Решение владельца: в `scene_undo_refusal_test`
   собрать `Command` руками (`undo = []{ return false; }`) и прогнать через настоящие
   `bus.execute`/`bus.undo` — пинит и `return false`, и «шаг не уехал в redo-хвост».
4. **«рез цитаты больше не ищет границу символа»** — вакуум ПОЯВИЛСЯ от собственной же починки
   четвёртого захода: санитайзер UTF-8 заменяет оборванный хвост на `?`, и цитата остаётся
   валидной с резом по границе и без него. Различает их только то, откуда взялся `?`: строка
   фикстуры целая, значит вопросительным знакам взяться неоткуда. Добавлено утверждение
   `a whole character is kept, not swapped for a marker the file never had`.
5. **«цитата принимает пересортицу и суррогаты»** — вакуум был в ФИКСТУРЕ, а не в коде: сырые
   байты в ней были только «очевидные» (`FF`, `FE`, одиночный `80`), а проверка «по старшим битам»
   пропустила бы пересортицу `C0 80` и суррогат `ED A0 80`, и гейт этого бы не заметил. В фикстуру
   добавлены оба, по одному байту на каждый способ соврать про кодировку.
6. **«рез цитаты отдаёт пустую цитату на сплошных продолжающих»** — находка ПЯТОГО ревью, и мутации
   под неё не было вовсе: откат по границе символа не имел нижней опоры, доходил до нуля, и на теле
   из 64+ продолжающих байтов цитата выходила `'...'` — пустая, то есть искать правку не по чему.
   Фикстура заведена у гейта снимка (`scene_undo_refusal_test`), а не у файлового: тот стоит на
   248 строках при жёстком потолке 250, а жёсткий потолок не покупается.

Блэст-радиус (`regression_risk: yes` — сменились сигнатуры `deserialize`, `restore_entity` и
`Command::undo`): `grep` по всему дереву даёт вне новых гейтов ровно двух вызывающих, оба правлены —
`tools/ide/command.hpp:60` (путь undo и сама шина) и `tools/ide/scene_roundtrip_test.cpp:47` (гейт 1
теперь проверяет, что загрузка СОСТОЯЛАСЬ: без этого отказ давал бы пустую сцену, а гейт падал бы на
сравнении текстов, не назвав причины). `tools/ide/editor/property_grid.cpp` линкуется со
`scene_core`, но ни одной из трёх сигнатур не зовёт — пересобран и прогнан вместе с остальными.
Голден гейта 1 цел: `0x2de54a36e54e0684`.

Блэст-радиус четвёртого захода (`regression_risk: yes` — санитайзер цитаты стоит на пути ВСЕХ
четырнадцати причин): `grep` по дереву по текстам сообщений даёт единственного потребителя вне
самого разбора — `scene_refusal_test.cpp:196`, где утверждение о трёх координатах ждало
`component Position`, а теперь ждёт `component 'Position'`; правлено. Оба гейта отказа, гейт 1,
`command_test`, `editor_selftest` и `perf_test` пересобраны и прогнаны, в том числе под
ASan+UBSan. Голден гейта 1 цел: `0x2de54a36e54e0684`.

Второй `regression_risk: yes` того же захода — комментарии `#` и пустые строки после шапки. Решение
владельца: **не отказывать**, а сузить обоснование причины 4 и записать потерю как осознанную
(`deserialize.cpp`, ветка `line[0] == '#'`). Ветвление разбора не тронуто вовсе, поэтому
перепроверять было нечего сверх обычного прогона: фикстуры `with()` строятся на шапке, пустой файл
и CRLF-файл прошли, голден цел.

Блэст-радиус третьего захода (`regression_risk: yes` — `[[nodiscard]]` у `CommandBus::undo()` и
разрез файла): `grep` по дереву даёт одиннадцать вызывающих `undo()`, все приведены поимённо
(`command_test` ×7, `perf_test` ×2, `editor_ui.hpp`, `editor_gate6.cpp`); зеркала списка исходников
обновлены обоим держателям — `tools/ide/CMakeLists.txt` (цель `scene_core`) и три копии `IDE_CORE`
в `.github/workflows/ci.yml`, сверку которых держит маркер `ci-lint: mirrors-target`. Голден гейта 1
цел: `0x2de54a36e54e0684`.

## A·2·9 — потолок уровня KTX2 у транскодера

Находка: `bc7.resize()` в `engine/asset/transcode.cpp` шёл по `info.m_total_blocks` — числу из шапки
ЧУЖОГО файла — без единой нашей проверки. Всё, что стояло между файлом и выделением памяти, было
внутренней валидацией basisu, исходников которой в дереве нет (тянется FetchContent'ом), то есть
проверить её было нечем.

Насколько это не теория, показала разведка на настоящем атласе из `example_ugly_game/assets/game.bundle`:
подменяем в шапке две четвёрки байт (`pixelWidth`/`pixelHeight` по смещениям 20 и 24) — и basisu
принимает файл на всех трёх своих шагах, честно пересчитывая под новые стороны число блоков:

| подменённая сторона | `init` | `start_transcoding` | `get_image_level_info` | `m_total_blocks` | во что превратился бы `resize` |
|---|---|---|---|---|---|
| 8196 | ok | ok | ok | 4 198 401 | 67 МБ |
| 16384 | ok | ok | ok | 16 777 216 | 268 МБ |
| 32768 | ok | ok | ok | 67 108 864 | 1,07 ГБ |
| 65535 | ok | ok | ok | 268 435 456 | **4,29 ГБ** |

Решения владельца (интервью, два захода). Первый: потолок плюс сверка заявленного числа блоков;
гейт — на чистой функции; возврат остаётся `bool`; шов «вызов действительно сделан» закрепить
враждебным KTX2. Второй, после второго ревью: мерить потолок БЛОКАМИ, а не площадью, и убрать
проверку площади совсем — она доказуемо следует из проверки блоков и потому непокрашиваема.

Куда лёг каждый пункт:

| пункт | где |
|---|---|
| потолок блоков `MAX_TEXTURE_BLOCKS = 1<<22` | `engine/asset/transcode.hpp:25` |
| решение «принять или отказать», один исход | `level_blocks()`, `transcode.hpp:48` |
| потолок МЕЖДУ чужим числом и `resize` | `transcode.cpp:25`, размер буфера берётся из возврата |
| таблица границ на чистой функции | `transcode_test.cpp`, 10 утверждений |
| положительный контроль на испечённом атласе | там же, `control: the baked atlas transcodes` |
| враждебный KTX2 + контроль отличимости | там же, `control: basisu itself accepts…` → `refused before it buys a single byte` |
| прогон на 3 ОС | `ci.yml`, шаг «Asset — gate #2 …» |
| прогон под ASan/UBSan | `ci.yml`, шаг «Asset — ASan/UBSan (gate #2 …)» |

### Почему потолок мерится БЛОКАМИ

Этот раздел переписан дважды, и оба раза — по находке ревью. Полезнее оставить след, чем итог.

Первая редакция мерила потолок ПЛОЩАДЬЮ (`MAX_TEXTURE_TEXELS = 1<<26`) и объясняла это так:
«тратится память, а память растёт по площади; предел по площади прямо называет то число байт,
которое мы согласны выделить чужому файлу». Вторая половина этой фразы неверна. Блоки округляются
вверх ПО КАЖДОЙ СТОРОНЕ, поэтому тонкий уровень держит площадь на пределе и учетверяет число
блоков: `1 × (1<<26)` — ровно 1<<26 текселей, потолок пропускал, а блоков `1 × 16 777 216`, то есть
**256 МиБ** вместо обещанных 64 — ровно столько же, сколько у отвергаемой фикстуры 16384×16384.
Проверено зондом против отгружаемых `libasset_gpu.a`/`libbasis_transcoder.a`, не рассуждением.
Таблица гейта этого не видела по построению: все её входы были квадратными
(`golden-middle-hides-boundaries` из памяти проекта).

Итог: ограничивать надо то, что тратится, а тратятся БЛОКИ — по 16 байт каждый, ровно.
`MAX_TEXTURE_BLOCKS = 1<<22` = 4 194 304 блока = **64 МиБ** BC7, и это же ровно квадрат 8192×8192,
так что старая граница на квадратных уровнях не сместилась ни на блок. Самый большой атлас
`assetc` — 512×256 = 8192 блока, запас **×512**.

Проверки площади рядом НЕТ намеренно, хотя ревью предлагало оставить её «дешёвым ранним отсевом».
При 16 байтах на блок `blocks >= texels/16`, поэтому проверка блоков влечёт проверку площади, и
строка про площадь никогда не сработала бы первой — то есть её нечем покрасить. Это тот же критерий,
по которому из `level_blocks` выброшена ветка нулевой стороны. Переполнения, ради которого её стоило
бы держать, тоже нет: стороны 32-битные, каждый сомножитель не больше 2^30.

Потолок по СТОРОНЕ отвергнут отдельно и по другой причине: он строже, чем нужно, на несимметричных
уровнях. 16384×4096 — те же 1<<22 блока и те же 64 МиБ, но предел по стороне 8192 отказал бы, не
спросив цену. Довод записан не только словами: строка `a lopsided level costing exactly the ceiling
is taken, not judged by its longest side` появилась в таблице именно потому, что без неё подмена
потолка на «сторона > 8192» не красила НИЧЕГО — единственная вакуумная мутация этого раунда.

### Почему у нулевой стороны нет ни ветки, ни строки в таблице

Нулевая сторона даёт ноль блоков, то есть тот же самый ноль, которым здесь называется отказ.
Явная ветка `if (w == 0 || h == 0) return 0;` была написана и УБРАНА: ни одна мутация не отличала
её присутствия от отсутствия — то есть это был код, который нечем покрасить. Вместе с ней убраны и
два утверждения о нулевых сторонах.

### Почему уликой служит пустой буфер, а не возврат `false`

Первая редакция враждебной фикстуры утверждала `!ktx2_to_bc7(...)` — и оказалась вакуумной: на
подменённом файле basisu возвращает `false` и сам, просто уже ПОСЛЕ того, как выделены 256 МБ.
Отказ как таковой ничего не доказывает. Доказывает пустой `bc7`: он означает, что до `resize` дело
не дошло, то есть отказали мы и ДО выделения памяти. Оба утверждения схлопнуты в одно —
`a header claiming 16384x16384 is refused before it buys a single byte`.

Ради той же отличимости фикстура сперва обязана ПРОЙТИ все три шага basisu. Эти четыре контроля
(`control: basisu itself accepts…`, `…starts transcoding…`, `…reports a level for it`,
`…block count follows the patched sides`) не краснеют ни на одной мутации нашего кода — и не
должны: они сторожат саму ФИКСТУРУ, на случай если версия basisu из FetchContent перестанет такой
файл принимать и гейт начнёт проверять чужой отказ вместо нашего. Что они живые, показывает
мутация фикстуры: сдвиг смещения подмены на один байт красит четвёртый из них.

### Третий независимый оракул округления

`honest(w,h)` в гейте считается циклами — нарочно другим выражением, чем `level_blocks`: оракул,
списанный с проверяемого кода, подтвердил бы любую его ошибку округления вместе с ним. Третьим
подтверждением служит уже лежавшая в дереве строка `example_ugly_game/bundle_atlas.cpp:66`, которая
независимо считает `want = ceil(w/4)*ceil(h/4)*16` — то же округление вверх.

### Мутационный прогон A·2·9

| мутация | какие именованные строки покраснели |
|---|---|
| потолок снят | `a level past the block ceiling is refused even though its block count is honest`<br>`one row of blocks past the ceiling is already past it`<br>`a thin level within the texel count but past the block count is refused`<br>`a header claiming 16384x16384 is refused before it buys a single byte` |
| потолок мерится ПЛОЩАДЬЮ, а не блоками (прежняя редакция) | `a thin level within the texel count but past the block count is refused` |
| потолок стоит по СТОРОНЕ | `a lopsided level costing exactly the ceiling is taken, not judged by its longest side` |
| граница потолка сдвинута на один блок внутрь | `and the largest square level the ceiling does allow is taken`<br>`a lopsided level costing exactly the ceiling is taken, not judged by its longest side` |
| сверка с заявленным числом блоков снята | `a block count above what the sides need is refused`<br>`and a block count short of the sides is refused too` |
| сверка ослаблена до «не меньше» | `a block count above what the sides need is refused` |
| округление стороны вниз вместо вверх | `a side that is not a multiple of four rounds up`<br>`and a level of a single pixel is one whole block` |
| уровень не принимается никогда | `and the largest square level the ceiling does allow is taken`<br>`a lopsided level costing exactly the ceiling is taken, not judged by its longest side`<br>`control: a level that adds up is taken`<br>`a side that is not a multiple of four rounds up`<br>`and a level of a single pixel is one whole block`<br>`control: the baked atlas transcodes`<br>`control: and the bytes it produced are exactly its blocks` |
| ШОВ: `ktx2_to_bc7` берёт число блоков из файла мимо потолка | `a header claiming 16384x16384 is refused before it buys a single byte` |
| размеры уровня не выдаются зовущему | `control: the baked atlas transcodes`<br>`control: and it reports its size` |
| ФИКСТУРА: подмена ложится не на те байты шапки | `control: whose block count follows the patched sides, so the tally has nothing to catch` |

**11 мутаций, вакуумных ноль.** Три из них правят не `level_blocks`, а его окружение: шов вызова в
`transcode.cpp`, выдачу размеров зовущему и саму фикстуру. Одиннадцатая («потолок стоит по СТОРОНЕ»)
в первом прогоне после перехода на блоки была ВАКУУМНОЙ — её покрасить стало нечем, пока в таблицу
не добавили несимметричный уровень 16384×4096.

Первый прогон этого набора дал три «падения сборки», которые я сперва записал как доказательство
неснимаемости проверки. Это было неверно: падал `-Werror=unused-variable` на осиротевшей переменной,
то есть артефакт мутации, а не свойство кода. Мутации переписаны так, чтобы собираться, и три из них
после этого дали настоящие покраснения. Заодно выяснилось, что заявление «снять проверку нельзя —
не соберётся» ложно и в комментариях `transcode.hpp`/`transcode.cpp`; оба исправлены.

Блэст-радиус (`regression_risk: yes` — на пути ВСЕХ транскодов появился отказ, которого не было):
`grep` по дереву даёт двух вызывающих `ktx2_to_bc7` вне гейта. `example_ugly_game/bundle_atlas.cpp:66-68`
уже считал ожидаемый размер сам (`want`, строка 66) и при несовпадении чистил `bc7` (строка 69) —
мягкий откат на процедурный атлас, путь отказа у него был и раньше. `engine/asset/asset_render_main.cpp:93` возвращает `fail("ktx2->bc7
transcode")` — тоже готовый путь. Ни один не передаёт картинок за потолком: самый большой атлас,
который печёт `assetc`, — 512×256 = 8192 блока, то есть в 512 раз меньше потолка. Оба TU пересобраны
в общем прогоне `build_check.sh`; голдены целы.

### Ревью A·2·9: 1 high, 5 medium, 7 low — и что за ними стояло

Ревью вернуло FAIL. Три находки оказались не про стиль, а про то, что заявленное не соответствовало
действительному; их стоит записать отдельно, потому что каждая пережила мой собственный прогон.

**Сверка блоков сторожит не файл, а шов с basisu.** Комментарий утверждал модель угроз с двумя
способами вранья: «невозможные размеры» и «честные размеры при завышенном числе блоков». Второго не
существует. `basisu_transcoder.cpp:19379` считает `m_total_blocks = num_blocks_x * num_blocks_y`,
где `num_blocks_* = ceil(сторона / сторона_блока)` из тех же `pixelWidth`/`pixelHeight`, которые
читаем и мы, — значит `declared` не может ПРЕВЫСИТЬ `need`, только совпасть. Сверка при этом
остаётся нужной, но по другой причине: она сторожит расхождение НАШЕГО округления с округлением
basisu при смене версии транскодера или формата выпечки (у ASTC-6×6 сторона блока не 4). Обоснование
переписано в `transcode.hpp`, сама формула не тронута.

**Шаг CI не собрался бы на Linux вовсе.** Санитайзерная сборка вендорного `basisu_transcoder.cpp`
без `-DNDEBUG` падает на g++ 13 и g++ 15 — `safe_shift_left` неоднозначен в отладочных ветках
`basisu_containers.h`. CMake этого не показывает: `like_nes_vendored_target` собирает цель в Release.
Проверено в контейнере `ubuntu:24.04` (тот же компилятор, что на раннере): с `-DNDEBUG` собирается и
проходит ASan+UBSan при `halt_on_error=1` начисто. Заявление «санитайзеры локально чисты» про
CI-компилятор действительно ничего не говорило — на macOS/AppleClang проблема не видна.

**Подмена шапки шла по смещениям 20/24 без сверки длины.** Гейт про «не индексируй буфер
непроверенным числом» сам этого не делал: `check` прогон не прерывает, поэтому упавший контроль выше
довёл бы до записи за конец копии — под ASan это heap-buffer-overflow вместо честного FAIL. Сверка
длины добавлена внутрь `poke_u32`. Редакция этого фикса переписывалась дважды, и оба раза по
находке: сперва `poke_u32` обернули в `check(...)` — вакуумное утверждение, истинное по построению;
потом рядом завели ещё и ранний отказ у зовущего — дубль, который не красит ни одна мутация, не
покрасив заодно сам `poke_u32`. Итог: сторож ОДИН, внутри хелпера, и зовущий обрывает на нём прогон
через `fail`, а не засчитывает утверждение.

Остальное: `w`/`h` при отказе не чистятся и контракт этого не говорил (записан в `transcode.hpp`);
сторона блока 4 зашита без фиксации допущения (записано); таблицы basisu поднимались неявным
порядком (`basisu_transcoder_init()` теперь зовётся явно); `BINC` брался без сторожа (добавлен);
«два байта» вместо восьми в трёх местах; оракул `honest()` и раскладка шапки слиплись в один
комментарий над чужим объявлением; неиспользуемый `<string>`.

`UBSAN_OPTIONS=halt_on_error=1` поднят в НАЧАЛО шага: раньше он стоял перед третьим прогоном, и
первые два (`asset_test_san`, `asset_align_san`) шли в режиме «напечатать и вернуть 0», то есть
проглотили бы находку UBSan молча. Это усиление существующих гейтов, поэтому проверено, а не
предположено: оба прогнаны в контейнере на g++ 13 со строгим режимом — `[asset_test] PASS`,
`asset-align: PASS`.

### Второе ревью A·2·9: 1 high, 3 medium, 2 low — потолок мерил не то

Ревью после первого круга правок снова вернуло FAIL, и главная находка была настоящей: потолок по
ПЛОЩАДИ не ограничивает то, что тратится (разобрано выше, «Почему потолок мерится БЛОКАМИ»).
Проверено зондом против отгружаемых `.a`, а не рассуждением: `init=1 start=1 blocks=16777216`,
`ktx2_to_bc7=0 bc7bytes=268435456`. Итог — потолок переведён на блоки, проверка площади убрана,
в таблицу гейта добавлены две строки (тонкий уровень и несимметричный), и на прежней реализации
тонкая строка краснеет — проверено мутацией.

**Контракт отказа был записан неверно.** В `transcode.hpp` стояло «`bc7` НЕ трогается». Это правда
только для отказов ДО выделения: `resize` в `transcode.cpp:27` идёт перед `return
t.transcode_image_level(...)`, поэтому отказ самого транскода оставляет вектор перерезанным и
записанным частично. Тот же зонд это и показал (`bc7bytes=268435456` при возврате 0). Решение
владельца (интервью): написать правду в комментарии, а не добавлять `bc7.clear()` — строку `clear()`
нечем покрасить, заставить basisu упасть ПОСЛЕ прохода нашего потолка фикстурой я не умею, а оба
зовущих и так защищены (`bundle_atlas.cpp:69` чистит сам, `asset_render_main.cpp:93` возвращает
`fail`).

**Мёртвая модель угроз выжила в тесте.** Правка предыдущего круга доехала до `transcode.hpp` и до
этих заметок, но не до `transcode_test.cpp`, где остался комментарий «второй способ соврать:
размеры честные, а блоков заявлено больше». Два файла одного коммита противоречили друг другу.
Переписан под ту же формулировку: сверка сторожит шов с basisu, а не файл.

Остальное: устаревшее обоснование проверки длины в `poke_u32` (оно ссылалось на защиту, которой
после раннего отказа больше не является); в таблице «куда лёг каждый пункт» два адреса из трёх
указывали не на те строки, а `bundle_atlas.cpp` назывался то `:66`, то `:67` — выправлено.

### Третье ревью A·2·9: 1 medium, 4 low — про то, что гейт сторожит сам себя

Третий круг FAIL'ов был уже не про потолок, а про его обвязку. Две находки касались правды
комментариев, три — того, что рукописные списки CI никем не сверялись.

**Контракт отказа перечислял не все точки отказа до выделения.** В `transcode.hpp` было написано,
что `bc7` не трогается «при отказе basisu на `init` или `start_transcoding` и при нашем отказе»;
`get_image_level_info` (`transcode.cpp:20`) из перечня выпал, хотя это тоже отказ ДО `resize`.
Дописан.

**Отсутствие `bc7.clear()` не было обосновано в коде.** Решение владельца прошлого круга жило только
в этих заметках, то есть правящий `transcode.cpp` увидел бы «путь отказа, забывший почистить буфер»
и починил бы это добросовестно — и молча убил бы единственную улику гейта: пустота `evil_bc7`.
Обоснование перенесено в комментарий рядом с объявлением, с явным указанием, КАКУЮ мутацию такой
«фикс» сделает незаметной.

**Утверждение называло себя неверно.** Строка `and the largest level the ceiling does allow is
taken` на 8192×8192 лгала соседней строке: 16384×4096 стоит ровно столько же блоков, то есть
«самого большого» уровня не существует — есть самый большой ПРИ РАВНЫХ СТОРОНАХ. Переименовано в
`and the largest square level the ceiling does allow is taken`.

**Три рукописных списка исходников в одном шаге несли три разных подмножества цели.** Это та же
поломка, что вскрылась при прогоне (см. следующий раздел), только увиденная как класс: `asset_test_san`
нёс `asset_manager bundle_view`, `asset_align_san` — `bundle_view bundle_writer`,
`transcode_san` — `asset_manager bundle_view`, и ни один не нёс `codec.cpp`. Решение владельца
(интервью): привести все три к ОДНОМУ составу, равному цели `asset_core`, и повесить маркер
`ci-lint: mirrors-target engine/asset/CMakeLists.txt asset_core` — дальше рецидив сторожит гейт
`list-drift`, а не дисциплина. Цена решения записана честно: `codec.cpp` тянет за собой
`platform_process_posix.cpp` (внешний пекарь) и весь шов `PLAT_FS`, поэтому платформенных TU в шаге
стало восемь вместо трёх. Две из трёх новых переменных попали в уже существующие группы
(`seam-fs`, `seam-args`), то есть сверяются тоже; третья (`PLAT_IO`) осталась без маркера намеренно —
группа из одного списка сверяется сама с собой, и гейт справедливо считает такое находкой.
Непустоту обеих сверок проверил мутацией: убранный из `ASRC` `codec.cpp` даёт
`ASRC ↔ asset_core: engine/asset/codec.cpp — есть в эталоне, нет в списке`, убранный из нашей копии
`PLAT_FS` `platform_path_posix.cpp` — `есть в остальных списках группы, нет здесь`.

**UBSan стоял на вендорном TU.** `basisu_transcoder.cpp` собирался в общей команде с
`-fsanitize=address,undefined`, а `halt_on_error=1` этого же шага превращает ЛЮБУЮ находку UBSan в
красный гейт — в том числе находку в чужом коде, который тянется FetchContent'ом и чинить который мы
не вправе. Решение владельца (интервью): собирать вендорный TU отдельным объектом с одним ASan.
Перепроверено в контейнере `ubuntu:24.04` на g++ 13: все три прогона шага линкуются и проходят
(`asset-align: PASS`, `[transcode_test] PASS atlas=512x256 bc7=131072 ceiling=4194304 blocks`).

Заодно проверено, ЧТО именно ASan на этом TU ловит, — и прежнее объяснение оказалось неверным.
В комментарии шага (с прошлого круга) стояло «выход за конец появляется на мутации «`resize` идёт
по `m_total_blocks`» — и вот её ASan ловит». Ни одного санитайзерного сообщения эта мутация не даёт:
её красит именованное утверждение враждебной фикстуры, а не ASan. Хуже того, зеркальная к ней
мутация «в `transcode_image_level` уходит `m_total_blocks` вместо `blocks`» не краснеет ВООБЩЕ —
и это не дыра гейта, а эквивалентный мутант: после сверки `declared == need` оба числа
доказуемо равны, ровно по тому же критерию, по которому из `level_blocks` выброшена ветка нулевой
стороны.

Настоящая мутация нашлась и измерена: `bc7.resize(blocks * 16 - 16)` — буфер на блок короче, чем
обещано транскодеру. Три прогона в контейнере:

| сборка вендорного TU | что напечатал прогон |
|---|---|
| `-fsanitize=address` (как в шаге) | `AddressSanitizer: heap-buffer-overflow … in memset`, rc=1 |
| без санитайзера вовсе | ни строки санитайзера, только `FAIL: control: and the bytes it produced are exactly its blocks`, rc=1 |
| чистое дерево, `-fsanitize=address` | `PASS`, rc=0 |

То есть запись за конец делает САМ чужой TU, и инструментация именно его — весь вердикт шага о
памяти; без неё остаётся лишь сверка размера, которая про выход за конец ничего не говорит.
Комментарий шага исправлен по этим замерам.

### Четвёртое ревью A·2·9: 3 medium, 4 low — и главная находка про мой же комментарий

**«Заметить было негде» — ложь, и урок из неё выходил неверный.** Я написал в шаге CI, что разъезд
списка увидеть было нечем, раз сборочный гейт идёт через CMake. Ревью проверило по `gh run list`:
`dev` красная ПЯТЬ прогонов подряд (`a1a4916`, `fd992a2`, `d2306aa`, `c009607`, `f825349`), и лог
последнего даёт ровно `engine/asset/asset_test.cpp:62: undefined reference to asset::write_bundle`
в задаче `build+determinism (ubuntu-latest)`. Проверено мной заново тем же способом. CI это видел —
не смотрел никто. Комментарий страховал от повторения хуже, чем мог бы: настоящий урок не «CI
такого не видит», а «красный прогон надо открыть» (память `branch-never-ci-validated`), плюс гейт
`list-drift`, который краснеет на этой машине ДО пуша — а не вместо прогона CI.

**`-DNDEBUG` тёк на все наши TU третьего прогона.** Он висел на общей команде вместе с
`-DBASISD_SUPPORT_KTX2*`, то есть накрывал `asset_core` и все восемь платформенных швов — те же
файлы, что в двух соседних прогонах ТОГО ЖЕ шага собираются без него. Сегодня безвредно (`assert(`
в `engine/asset/*` и в posix-TU платформы нет), но первый же `assert()` в `asset_core` оказался бы
выключен ровно там, где линкуется транскодер. Решение владельца (интервью): развести объекты —
`transcode.cpp` и `transcode_test.cpp` собираются отдельно с `-DNDEBUG` (как уже сделан вендорный
TU), остальные исходники линкуются без него. Команд компиляции в шаге стало четыре вместо трёх.

**Сторожей на подмену шапки было два.** Ранний отказ у зовущего и сверка длины внутри `poke_u32`
дают один и тот же `fail`, и ни одна мутация не красит первый, не покрасив второй; половина условия
(`a.data == nullptr`) вдобавок недостижима после `wait_ready`. Решение владельца (интервью): убрать
дубль, оставить единственного сторожа в `poke_u32` и записать рядом ПОЧЕМУ. Тот же критерий, что у
выброшенной ветки нулевой стороны.

Остальное: докстрока правила `list-drift` в `scripts/ci_lint_lists.py` называла три копии группы
`seam-fs`, а их стало четыре (текст гейта я выправил, исходник правила — нет); в разделе про три
списка выше было написано, что `asset_test_san` нёс `bundle_writer` — он его как раз и не нёс, и
соседний раздел этих же заметок говорил обратное; `ZINC`/`ZLIB` остались без сторожей, хотя
`SINC`/`BINC` их получили, а пустая подстановка после `-isystem` съедает следующий флаг как свой
аргумент; ссылка на `bundle_atlas.cpp:66-69` в заголовке движка дана без пути и номерами строк —
заменена на путь целиком и имя сущности (`want`).

Что ревью проверило и подтвердило самостоятельно, по вендорному исходнику: `ktx2_transcoder::init`
(`basisu_transcoder.cpp:18923`) отбивает нулевые стороны, поэтому отсутствие ветки нулевой стороны
у `level_blocks` корректно; `m_total_blocks` — **uint32**, и на сторонах порядка 2^32 произведение
переполняется, то есть порядок «сперва потолок по `need` в uint64, потом `declared == need`»
держит и этот случай — переставить две проверки местами значит вернуть дыру; `init` принимает
`KTX2_FORMAT_ASTC_6x6_SFLOAT_BLOCK` с `get_block_width() = 6`, и на нём файл отвергается сверкой,
а не недо-выделяется, — комментарий про ASTC-6×6 точен.

### Пятое ревью A·2·9: 1 medium, 5 low — про то, что контроль обязан обрывать прогон

**Три контроля отличимости не сторожили друг друга.** `check()` прогон не прерывает, поэтому
провалившийся `probe.init()` не мешал коду звать `start_transcoding()` и `get_image_level_info()`
на полуинициализированном транскодере. `m_pData` basisu выставляет РАНЬШЕ проверок сторон и
формата, так что сторож `if (!m_pData)` такой объект пропускает, и дальше нас спасали бы чужие
сторожа внутри зависимости, которая тянется FetchContent'ом. Сегодня безвредно, но сценарий
«новая версия basisu перестала принимать подменённую шапку» — ровно тот, ради которого эти
контроли и написаны. Первый из них сделан обрывающим; тот же класс, что круг назад закрыли у
`poke_u32`.

**Явный `basisu_transcoder_init()` был no-op.** Таблицы поднимает `ktx2_to_bc7` положительного
контроля строкой выше, а basisu идемпотентна и на повторный вызов выходит сразу. Ни одна из 11
мутаций эту строку не красила — тот самый критерий, по которому из `level_blocks` выброшена ветка
нулевой стороны. Решение владельца (интервью): убрать строку вместе с комментарием.

**Три комментария в шаге CI обещали больше, чем делали.** (1) «CMake собирает вендорную цель в
Release и потому этого не показывает» — неверная атрибуция: `like_nes_vendored_target` типа сборки
не трогает вовсе, `NDEBUG` приезжает из того, что ВСЕ конфигурации с basisu — Release или
RelWithDebInfo, а Debug-шаг CI собирает только `STATE_TARGETS` и до basisu не доходит. (2) Развод
UBSan от чужого кода назван полным, а он частичный: наши два TU собираются с `-fsanitize=…,undefined`
и включают `basisu_transcoder.h`, то есть шаблоны и инлайн-функции basisu инструментируются вместе
с нами; полностью развести это можно только обёрткой над транскодером. (3) «До ревью A·2·9 три
прогона несли три разных подмножества» — в приземлённой истории прогонов было ДВА, третий
добавляется этим же изменением; читатель проверил бы по git и не подтвердил. Часть про `a1a4916` и
пять красных прогонов при этом точна и проверена независимо.

**Новая цель не получала `-Wno-unknown-warning-option`.** `asset_gpu` несёт его ровно за
`basisu.h` с GCC-прагмой, опция PRIVATE и по линковке не наследуется, а `transcode_test.cpp`
включает `basisu_transcoder.h` напрямую. Решение владельца (интервью): повторить опцию на
`asset_transcode_test`, а не снимать её с `asset_gpu` как мёртвую — снятие задевает macOS/AppleClang
и clang-cl-джоб, а clang-cl на этой машине не проверить, улику дал бы только прогон CI.

Ревью самостоятельно подтвердило то, на чём держится порядок проверок в `level_blocks`: при
`pixel_width = 0xFFFFFFFF` basisu считает `(level_width + 3) / 4` В UINT32 и заворачивается в ноль,
то есть `declared` приходит нулевым, а наш `need = 2^60` отбивается потолком ПЕРВЫМ. Переставить
потолок и сверку местами значит вернуть дыру. Мелочь: ссылка на `init` указывала на строку
комментария, проверка сторон — строкой ниже (`basisu_transcoder.cpp:18923`).

Дублирование `guid_of`/`wait_ready` с `example_ugly_game/bundle_atlas.cpp` ревью пометило как вкус
и оставлено как есть: общий тест-хелпер — рефакторинг, а он только по явному запросу.

### Посторонняя находка: CI-шаг `asset_test_san` не линковался с `a1a4916`

Не связано с A·2·9, вскрылось при прогоне шага в контейнере. Коммит `a1a4916` этого же раунда
добавил в `engine/asset/asset_test.cpp:62` вызов `write_bundle`, но рукописный список исходников
шага «Asset — ASan/UBSan (gate #2 memory-safety, Linux)» не обновил — `bundle_writer.cpp` в нём нет.
Линковка падает с `undefined reference to asset::write_bundle`, то есть весь шаг красный на Linux с
того коммита. Это ровно правило `handwritten-link-list-needs-target`: рукописный список равняется
ЦЕЛИ, а не тому, что звалось вчера. Причина, по которой не заметили сразу, — `ci_watch.sh` на
`a1a4916`/`c009607`/`f825349` не гонялся.

Починено здесь же (`bundle_writer.cpp` добавлен в список) и проверено прогоном в контейнере, потому
что поломка блокирующая: без неё нечем было бы проверить и новый третий прогон в том же шаге.

## A·2·10 — граница C-ABI плагина: чужой кадр принимался на веру

**Находка.** Восемь входов `HostApi` — единственное место, куда чужая `.so` подаёт указатели и
счётчики. Танки в `registry.cpp` брали их как есть: `nullptr` вместо `id` доезжал до
`std::string(nullptr)` (UB), строка без терминатора читалась до первого случайного нуля, `n_after`
знакового типа не проверялся ни с одного конца, и ни один вход не был `noexcept` — исключение,
пересекающее кадр C-ABI, это UB по стандарту. Дубликат `fourcc` кодек принимал молча, хотя два
соседа из трёх дубликаты отбивают с именем владельца в логе.

Самый первый аргумент всех восьми входов — `ctx`, контекст хоста, — тоже приходил из чужого кадра
и тоже брался на веру: `static_cast<Registry*>(nullptr)` с последующим вызовом метода это UB,
которого барьер `guarded` поймать не может в принципе — оно не исключение.

**Что сделано.**

- `PLUGIN_MAX_SYSTEM_DEPS 64` в `plugin_api.h` — потолок объявлен ЧАСТЬЮ КОНТРАКТА, а не внутренним
  числом хоста: без него хост читает по чужому счётчику, а в логе стоит его собственный адрес.
- Граница вынесена в `engine/plugin/host_api.cpp` (решение владельца, интервью #6). Разрез по
  предмету, а не по счётчику строк: здесь кадр чужого кода и обязательство ничего не выпустить
  наружу, в `registry.cpp` — контейнер расширений, которому всё пришло проверенным. Поводом было
  превышение SOFT (224 строки), но закрытый список причин `line_budget_rules.py` его и не выкупает:
  `"single"` значит «у куска нет своего имени», а у границы имя есть.
- `PLUGIN_MAX_ID 128` — потолок на РАЗМЕР БУФЕРА любой строки из кадра плагина, терминатор входит
  в число. Терминатор ищется `memchr`'ом в пределах потолка, а не доверяется: `std::string(p)` по
  не-NUL-терминированному буферу читает чужую память. Тот же приём, которым аудит уже закрыл имена
  пресетов ввода (A·2·1b). Оговорка записана в коде: по совсем битому указателю это ограничивает
  ущерб, а не проверяет его — валидировать чужой указатель нечем в принципе.
- Сторож на `ctx` во всех четырёх собственных танках. Гейт зовёт с `nullptr` ВСЕ восемь входов,
  а не один: сторож, забытый в одном танке, был бы неотличим от поставленного во всех.
- Все восемь танков `noexcept`, барьер `guarded()` ловит оба вида исключений, `null_arg()` печатает
  отказ с именем поля — именем из объявления `HostApi`, а не параметра хоста: «extra is null»
  заставило бы автора плагина гадать, о каком из его же аргументов речь. Отказ по дыре внутри `after[]` — ЦЕЛИКОМ, а не пропуском элемента: система
  с тихо выброшенной зависимостью встанет в расписании не туда и разойдётся с задумкой плагина.
- `add_asset_codec` отбивает дубликат как оба соседа.
- Гейт `engine/plugin/plugin_abi_test.cpp` (177 строк). Утверждает не «не упало», а ЧТО РЕЕСТР НЕ
  ВЫРОС: `reg.count(kind)` до и после вызова — единственный способ отличить отказ от «принято и
  промолчало», раз танки возвращают void. Массив зависимостей ровно `PLUGIN_MAX_SYSTEM_DEPS` длиной,
  а счётчиком передаётся `size + 1`: снятый потолок читает за конец, и ассерту это НЕ видно, а
  санитайзеру видно. У КАЖДОГО из семи видов расширения есть и отрицательный случай, и позитивный
  контроль: без второго «сторож сработал» неотличимо от «вход мёртв», и мутанты «сделать танк
  пустым» проходили бы гейт зелёными — проверено, сначала они его и проходили.
- CI: свой шаг на 3 ОС (решение владельца, интервью #6) плюс сборка под ASan/UBSan на Linux. Три
  ОС не для симметрии: отказы зависят от рантайма (UB `%s` от nullptr, отрицательное → `size_t`).
- Два рукописных списка исходников `plugin_core` в `ci.yml` получили маркеры
  `# ci-lint: mirrors-target engine/plugin/CMakeLists.txt plugin_core`. Класс, который это же
  изменение и могло воспроизвести (новый `host_api.cpp` мимо списка), стал механическим.

**Мутационный прогон.** 28 мутантов по `host_api.cpp`, вакуумных 0. Разложены по предмету:
четыре сторожа `ctx`, два сторожа строк (`nullptr` и потолок терминатора), пять по счётчику
зависимостей (оба конца, контрактное `n > 0`, дыра внутри массива), восемь по обработчикам и
полям, четыре позитивных контроля «танк мёртв», два снятых `noexcept` и `old-impl`.
Мутант `old-impl` (танки из `HEAD:engine/plugin/registry.cpp` + беззащитный `add_asset_codec`)
даёт `exit=-11` — гейт краснеет на старой реализации, как требует DoD. Оба мутанта, снимающие
`noexcept`, краснеют на СБОРКЕ: чёрный ящик не умеет заставить чужой кадр бросить, поэтому половина
барьера закрыта `static_assert`'ом по всем восьми входам, а не ассертом теста.

Две ловушки прогона, обе исправлены и обе стоят записи:

1. `deps.reserve(static_cast<std::size_t>(n))` бросал `std::length_error` на отрицательном `n`,
   `guarded` его ловил — регистрация отказывалась ПО ЧУЖОЙ ПРИЧИНЕ, и сторож `n < 0` был
   непроверяемым (мутант «negative n allowed» зеленел). `reserve` убран, причина записана в коде.
2. Харнесс звал `cmake --build … | tail -5` через `shell=True`: код возврата приходил от `tail`,
   провалы сборки выглядели успехом, и прогонялся СТАЛЫЙ бинарь предыдущего мутанта. Тот же класс,
   что `fallback-makes-a-gate-vacuous`, только в инструменте проверки.

**Записанный, а не упущенный непокрытый сторож.** `thunk_log` подставляет `"(null)"` вместо
`msg == nullptr`. Его не красит НИ ОДНА мутация ни на одной из трёх ОС: libc всех раннеров печатает
за `%s` от nullptr то же самое `(null)`. Снять — значит оставить в коде UB ради строки; проверить —
значит перехватывать stderr, то есть заводить файловый ввод-вывод в тесте мимо шва. Остаётся как
обязательство перед стандартом, а не как поведение; сказано в трёх местах (код, тест, шаг CI).

### Блокирующая посторонняя находка: прогон владельца две недели не выполнял НИ ОДНОГО теста

`stale()` в обходе тестов спрашивал `ninja -n <цель> | grep -q 'no work to do'`. 2026-09-06
(`fbe6a6f`) в `docs/examples/CMakeLists.txt:11` появился `file(GLOB … CONFIGURE_DEPENDS)` — CMake
завёл цель `cmake.verify_globs`, которую помечает ВСЕГДА грязной, и сухой прогон стал обещать
работу для любой цели. С того дня все 149 тестов уходили в `STALE`, ни один не выполнялся, вердикт
был красным по причине, к дереву отношения не имеющей — ровно то, от чего эта корзина заводилась
2026-08-29.

Чинить фильтром вывода нельзя: сухой прогон обрывается на перегенерации `build.ninja` и о самой
цели не говорит ВООБЩЕ, так что фильтр объявил бы свежим всё — поломка в тихую сторону. Решение
владельца (интервью #7 — разбить скрипт, интервью #8 — собирать цель): `fresh()` зовёт
`ninja -C build <цель>` по-настоящему. Свежая цель — no-op (все 162 теста за 6 с), несвежая
становится свежей, несобираемая уходит в ту же корзину «тест НЕ ВЫПОЛНЯЛСЯ». Оба вердикта
проверены руками: тронутый исходник → бинарь пересобран до запуска; исходник с синтаксической
ошибкой → `STALE`, тест не запущен.

`scripts/owner_check.sh` при этом упёрся в ЖЁСТКИЙ лимит длины (250), который причиной не
выкупается: три строки ветки для `plugin_abi_test` съели последний запас. Обход тестов вынесен в
`scripts/owner_check_tests.sh` и ПОДКЛЮЧАЕТСЯ через `.`, а не запускается — вердикт внизу считает
по его массивам, а у дочернего процесса их не занять. Разрешение на 247 строк из
`line_budget_allow_tools.py` удалено: файл снова под мягким лимитом.

### Независимое ревью до коммита

`code-reviewer` по всему срезу: 1 high, 6 medium, 5 low, 1 taste. Исправлены ВСЕ ≥ low, taste —
тоже (переформулировка, без риска). Три находки меняли не код, а доказательство, и стоят записи:

1. **high — три мутанта проходили гейт зелёными.** Снятое контрактное условие `n > 0 &&`, мёртвый
   `thunk_render`, мёртвый `thunk_input`: мой прогон их просто не содержал. Причина одна и та же —
   у трёх видов расширения из семи не было ПОЗИТИВНОГО контроля, и «сторож сработал» было
   неотличимо от «вход мёртв». Добавлен случай «зависимостей нет вовсе, `after[] == nullptr`
   законен» и по позитивному контролю на каждый вид; мутанты теперь в прогоне и красные.
2. **medium — комментарий утверждал больше, чем делал код.** Было написано, что отрицательный
   `n_after` уводит чтение в чужую память: неправда, `for (int32_t i = 0; i < n; ++i)` даёт НОЛЬ
   итераций. Настоящий вред другой — система встаёт в реестр с ПУСТЫМ списком зависимостей, хотя
   плагин их объявил, и расходится с его задумкой молча, уже в расписании. Переписано в трёх
   местах (код, эти заметки, шаг CI).
3. **medium — сам сторож читал за буфер.** `memchr(p, '\0', PLUGIN_MAX_ID + 1)` по строке, поданной
   ровно в потолок, перечитывает ровно тот байт, ради которого заводился. Стало `PLUGIN_MAX_ID`,
   терминатор входит в потолок, формулировка контракта исправлена следом.

Ещё из ≥ low: `"$AT" | tee` терял stderr и код возврата (отказы печатаются в stderr) — стало
`2>&1 | tee` в обоих местах CI; отказ печатал имя параметра хоста вместо имени поля контракта
(`extra is null` вместо `title is null`) — заведён `extra_field`; новый `owner_check_tests.sh`
молча оказался вне гейта shellcheck.

Два мутанта (`named: id check gone`, `named: extra check gone`) краснели на СБОРКЕ по чужой
причине — снятый сторож оставлял `sid`/`sextra` неиспользованными, и `-Werror` ловил мутацию
раньше, чем её ловило поведение. Переделаны на присваивание: теперь оба `RED(run)`, падение
поведенческое. На сборке краснеют ровно два мутанта, и оба по делу — снятое `noexcept`.

### Решения владельца по срезу (интервью #9 и #10)

1. **shellcheck берёт семейство владельца шаблоном `scripts/owner_*.sh`**, а не списком: забытая
   строка списка не падает, она молча выводит скрипт из-под проверки — что и случилось при разрезе.
2. **Потолок на строки (`PLUGIN_MAX_ID`) заводится здесь, в этом срезе**, а не отдельной находкой:
   контракт с ограниченным счётчиком массива и неограниченной строкой написан наполовину.
3. **Асимметрия `add_named` ЗАПИСЫВАЕТСЯ комментарием, а не выравнивается.** Три соседа дубликат
   отбивают, named-слоты — нет, и это по предмету: у соседей имя — ключ поиска, у named-слотов —
   подпись в логе, а проходы вызываются все подряд. Отказ здесь менял бы счётчики четырёх чужих
   гейтов ради симметрии, а не ради дефекта.
4. **Файла в `.context/gates/` этот гейт НЕ получает, и причина записана.** `plugin_abi_test` — не
   отдельный гейт дерева, а регресс-тест одной находки аудита: у него нет своей самопроверки на
   сломанных фикстурах, он не зовётся из `preflight.sh` и в таблицу CLAUDE.md не идёт. Его
   устройство и позитивные контроли живут здесь, в заметках находки; заводить двадцатый файл гейта
   ради теста, который CI гоняет как обычный шаг, значило бы размыть смысл самой папки.
5. **Комментарий в `preflight.sh` сжат до одной строки** вместо разрешения в
   `line_budget_allow_tools.py`: 201-я строка появилась от объяснения, а не от логики, и выкупать
   бюджет объяснением — значит завести потолок, который следующий этап попросит поднять снова.

## Гейт 9 — решения владельца до кода (интервью #11 и #12)

Восемь решений, принятых ДО первой строки; каждое названо причиной, а не вкусом.

1. **Один бинарь `fuzz_readers` с таблицей читателей**, а не бинарь на семейство и не libFuzzer.
   libFuzzer отпадает по обеим осям требования разом: его нет на MSVC, и прогон недетерминирован
   по времени — «3 ОС + повторяемость» он не выполняет ни на одной. Таблица `{имя, семя, чтение}`
   делает нового читателя одной строкой, а не третьим шагом CI со своим дрейфом списка исходников.
2. **Валидное семя печётся В ПАМЯТИ теми же пекарями, что в бою** (`preset_bake`, `map_bake`,
   `atlas_bake`, `profile_bake`, `achievements/bake`, `bundle_writer`). Семя из файла молча
   протухает, когда файл перестают печь; семя из пекаря чинит себя само при смене версии формата.
   Валидность семени утверждается ДО мутаций — это позитивный контроль самого ВХОДА.
3. **Читателям, которые не берут байты, подаётся временный файл через шов `platform_fs`.**
   `parse_manifest` читает ПУТЬ, и гейт обязан проверять тот же код, что работает в бою, включая
   чтение файла. Разрезать боевую функцию ради удобства теста — рефакторинг, а не проверка.
4. **Позитивный контроль встроен в бинарь целью `canary` под флагом.** `--selftest` гоняет ТОЛЬКО
   её и ОБЯЗАН упасть под ASan; шаг CI красен, если она НЕ упала. Разовый мутационный прогон в
   скрэтчпаде такого не даёт: он не повторяется, и следующий регресс ловить нечем.
5. **Оракул: «принял» обязывает ОБОЙТИ ВСЁ СОДЕРЖИМОЕ.** Успешный `open()` без обхода не проверяет
   ничего — никто не трогает байты, о которых врёт заголовок, и ASan молчит. Именно в обходе живут
   находки A·2·6 и A·2·9: смещение и счётчик врут, а вскрывается это на чтении payload'а.
6. **Seed'ы — фиксированный список в коде**, не sha коммита. Мерцающий гейт нарушает то самое
   требование детерминизма, ради которого писался свой мутатор: красный раннер обязан
   воспроизводиться одной командой на машине владельца. Расширение поиска — дописать seed коммитом.
7. **Зависание ловится порогом на СЛУЧАЙ внутри гейта**, а не `timeout-minutes` у шага. Общий
   таймаут говорит «шаг висел» и не называет ни цель, ни seed, ни случай, а на машине владельца не
   говорит вообще ничего. К тому же `timeout` есть не на всех трёх ОС.
8. **Код живёт в новом корне `tests/fuzz/`.** Гейт пересекает шесть подсистем и не принадлежит ни
   одной; положить его в `engine/asset` значит завести зависимость `asset → tools/ide` и уронить
   гейт направления зависимостей. Цена признана: новый корень дерева вносится в `check_tree_roots.py`.

## Гейт 9 — что сделано и чем доказано

Устройство, решения и таблица мутаций живут в [`.context/gates/fuzz-readers.md`](../gates/fuzz-readers.md)
— это нормальный гейт дерева, со своим прогоном, своей самопроверкой и строкой в таблице CLAUDE.md.
Здесь остаётся то, что относится к АУДИТУ, а не к гейту.

**Десять целей, а не одиннадцать.** `framework_replay` цели не получил, и это не пропуск: у него
нет байтового формата вовсе — INTERFACE-библиотека без единого `.cpp`. Разбирать нечего, подавать
мутанта некуда. Появится формат — появится цель.

**Девять целей под `-DIDE_POC=OFF`.** Сцена IDE уходит под `#if` вместе с `scene_core`, и задача
Windows clang-cl ASan гоняется именно так. Поэтому число целей печатает сам прогон и закрепляется
грепом в шаге: `targets=10` на трёх ОС и `targets=9` под clang-cl — второе утверждает, что под
`#if` ушла РОВНО одна цель, а не половина таблицы.

**Гейт нашёл дефект в себе же на первом полном прогоне**, и это стоит записать: UBSan доложил
переполнение `int32_t` в `consume(prof.max_speed.raw + prof.gravity_fall.raw)` — фаззер складывал
два знаковых поля мутированного файла. Важнее самой правки два следствия:

1. Класс снят целиком, а не одной строкой: свёртка полей переехала в `consume_all`, которая гонит
   каждое поле через `uint64_t` по отдельности. Четырнадцать мест правились разом.
2. **Прогон при этом вернул НОЛЬ.** UBSan по умолчанию печатает находку и продолжает, и шаг CI
   зеленел бы с undefined-behavior в логе — тот же класс молчания, ради которого пишется весь этот
   аудит. Отсюда `-fno-sanitize-recover=undefined` в шаге: находка обязана быть кодом возврата.

**Линковка: `ld: warning: ignoring duplicate libraries`.** Список библиотек цели записан ОТ
ЗАВИСЯЩЕГО К ЗАВИСИМОСТИ, потому что CMake хранит заявленный порядок прямых зависимостей и, чтобы
архив стоял после того, кто из него тянет символы, дописывает его на линию второй раз. Сборочный
гейт не умеет считать предупреждения неважными — и правильно делает.

**Куда встал гейт.** `preflight.sh` (каталог `build-full`, иначе проверялись бы девять форматов из
десяти молча), релизный шаг на трёх ОС, шаг ASan/UBSan на Linux и урезанный по ГЛУБИНЕ (а не по
списку seed'ов) прогон в задаче Windows clang-cl. В обоих санитайзерных шагах стоит ОБРАТНОЕ
утверждение: `--selftest` обязан упасть, и зелёный его код возврата означает, что санитайзер в
этой сборке не вооружён вовсе.

Прогон НА МОМЕНТ СДАЧИ НА РЕВЬЮ: `targets=10 seeds=8 cases=160000 accepts=19120`; 33 с в релизе,
~2 мин под ASan/UBSan; непустота — 15 мутаций реализации, вакуумных 0. Оба числа устарели дважды,
разбор ниже; итог гейта — `accepts=20315` и 30 мутаций.

### Гейт 9 — что нашло независимое ревью (вердикт FAIL, 2 medium + 7 low + 2 taste)

Ревью читало рабочее дерево против `dd24883` и собирало цель само. Находки исправлены все, включая
обе `taste`; решения по четырём спорным приняты интервью #15.

1. **Порог не ловил зависание (medium).** `spent` считается ПОСЛЕ возврата `t.read()`: чтобы вызов
   сравнили с порогом, он должен сперва вернуться. Не сходящийся цикл не возвращается никогда, и
   гейт, который в трёх местах обещал назвать цель, seed и номер вместо общего таймаута, на самом
   опасном входе давал ровно общий таймаут. Закрыто сторожевым потоком (`fuzz_watchdog.hpp`) со
   сроком на секунду БОЛЬШЕ порога — чтобы два отказа не гонялись наперегонки.
2. **Цель могла уйти в вакуум молча (medium).** `accepts` было одним глобальным числом и ничего не
   утверждало: цель, у которой конверт начал отбивать всё на первом байте, осталась бы зелёной, а
   её ноль утонул бы в тысячах остальных. Теперь счёт на каждую цель, ноль принятых — отказ, и
   появился `--target <имя>` для повтора одной цели теми же буферами.
3. **`atoi` на `--cases` (low).** Выход за диапазон `int` — UB, и страж `<= 0` его не ловил:
   `--cases 99999999999999` уходил в многочасовой прогон, неотличимый от зависания. То есть гейт
   ломался ровно тем способом, против которого написан. Теперь `strtol` с потолком.
4. **Порядок байт в мутаторе (low).** `memcpy` первых `width` байт `uint64_t` — это младшие байты
   только на little-endian, а файл заявлял воспроизводимость «на любой машине». Переписано явными
   сдвигами. **`accepts=19120` не сдвинулось ни на единицу** — это и есть доказательство, что на
   LE байты те же; перегонять зафиксированные числа не понадобилось, вопреки оценке ревью.
5. **Заявление, которое не могло покраснеть (low).** Комментарий обещал подачу пустого буфера с
   НУЛЕВЫМ указателем, но `buf` всегда копия непустого семени, а `resize(0)` сохраняет capacity:
   `data()` не бывает нулевым ни разу. Заявление снято, а не подкреплено: настоящий случай
   `read(nullptr, 0)` потребовал бы чинить сами читатели (`std::string(nullptr, 0)` — UB).
6. Остальное: фиксированное имя `fuzz_manifest.tmp` (два прогона рядом затирали файл друг друга,
   а падение оставляло untracked-мусор под гейтом 10 спеки #20) → имя с `platform::process_id()` и
   строка в `.gitignore`; `consume(sizeof(src))` и `consume(1)` скармливали стоку КОНСТАНТУ
   КОМПИЛЯТОРА вместо того, что читатель отдал; `reinterpret_cast` в цели сцены без обоснования
   (`conventions.md`); путь `./build/fuzz_readers` в раннбуке против `./build-full/` в таблице
   CLAUDE.md и в preflight — разные конфигурации, то есть молчаливо другой гейт.

7. **Что этот страж нашёл сразу же (не находка ревью, а её следствие).** Поставленный по пункту 2
   `accepts == 0` уронил гейт на ЖИВОМ дереве: цель `ach-state` не приняла ни одного изменённого
   буфера из 16000. Причина в формате — `ach::decode` сверяет размер со счётчиками и запечатывает
   payload хешем FNV (`engine/achievements/state.cpp:62-65`), поэтому мутация payload'а ломает
   хеш, мутация счётчиков — размер, и цикл записей (`:67-76`) не исполнялся на враждебных байтах
   НИ РАЗУ за всю жизнь гейта. Прежние «63 принятых» были целиком копиями семени: мутатор вправе
   вернуть буфер без изменений («интересное» число легло поверх байт, где уже лежало; перестановка
   куска при `from == to`), а копию семени принимает и наглухо закрытый конверт. Исправлено двумя
   правками: принятым считается только изменённый буфер, и у `Target` появился шов `reseal`,
   перепечатывающий контрольную сумму. Первая редакция чинила её на ПОЛОВИНЕ случаев с обоснованием
   «иначе выключим хеш-сторожа» — второе ревью показало, что обоснование ложное (см. ниже), и шов
   зовётся на каждом случае. Стало 1600 принятых из 16000, итог прогона `accepts=20315`.

### Гейт 9 — второе независимое ревью (вердикт FAIL, 2 medium + 4 low + 1 taste)

Ревью читало дельту после первого вердикта: шов перепечатки, счёт только изменённых буферов,
разделение файла целей, общий `consume_str`, две правки доков. Решения по трём спорным — интервью
#17, исправлены все находки, включая `taste`.

8. **Обоснование «половины» обещало защиту, которой нет (medium).** Комментарий утверждал, что
   перепечатка на половине случаев сохраняет под обстрелом хеш-сторожа `ach::decode`. Это неправда:
   гейт судит падение, зависание и вакуум, а сторож РАЗМЕРА сам держит счётчики согласованными с
   длиной, поэтому снятая проверка хеша не краснит прогон ни при 0%, ни при 50%, ни при 100%
   перепечаток. Половина глубины уходила на путь, кончающийся одним `return false`. Решение
   владельца — перепечатывать ВСЕГДА (шов чинит только поле хеша; магию, версию и счётчики
   по-прежнему судит конверт): 786 → 1600 принятых, итог 19501 → 20315.
9. **Ни один мутант не отделял ценность шва (medium).** Ревью предложило различающий мутант — снять
   ОДИН сторож размера, хеш-сторож оставить: он якобы обязан краснеть только при перепечатке.
   Измерено — и предположение не подтвердилось: мутант красен и БЕЗ шва, потому что мутация
   счётчиков не трогает payload, сумма остаётся верной, и вход доезжает до цикла записей сам. В
   таблицу встали оба (одиночный и контроль с выключенной перепечаткой), а в файл гейта — честная
   формулировка: шов возвращает под обстрел СОДЕРЖИМОЕ формата, а не обнаружение этого дефекта; за
   обнаружение отвечает страж «ноль принятых».
10. **Третья запись одной константы (low).** Комментарий перепечатки обещал, что смещение хеша
    выведено из `STATE_HEADER_SIZE` и потому не разойдётся с `ach::encode`, — а `encode` и `decode`
    писали голый литерал `16`. По интервью введена `ach::STATE_HASH_OFFSET`, и все три места зовут
    её. Блок формата не изменился: `ach_state_test`, `ach_crash_test`, `ach_bundle_test` зелены,
    sim-hash `0x32a094e89eacf2f2` совпал во всех трёх режимах, `check_hash_seam.py` — 756 файлов,
    находок 0.
11. **Ложный отказ на мелкой глубине (low).** `--cases 5` давал `FAIL ach-manifest: of 40 cases not
    one changed buffer got past the envelope` на здоровом дереве: страж вакуума судил о размере
    выборки. Введён пол `MIN_CASES = 200` — ровно глубина самой медленной задачи CI, где запас
    каждой цели уже доказан прогоном. Побочный эффект поймал следующий же перегон: мутант
    `--target nosuch` гонялся с `--cases 1` и стал вакуумным — команда отбивалась разбором
    аргументов, не доходя до предмета. Команда в скрипте перегона исправлена на `--cases 200`.
12. Остальное: строка про самую тонкую цель называла `ide-scene`, которой в задаче clang-cl нет
    вовсе (`-DIDE_POC=OFF`) — теперь названы обе, и `ach-manifest` 46 из 1600 для той конфигурации;
    блок с `accepts=19120` и «15 мутаций» помечен как замер на момент сдачи; `taste` — в шапке
    `fuzz_targets_ach.cpp` сказано, что `asset/hash.hpp` заголовочный и границу групп по линковке
    не нарушает.

Файл драйвера при этом перевалил мягкий лимит (215 строк), и прогон одной цели выделен в
`fuzz_run.hpp`: в `fuzz_readers.cpp` остались командная строка и сборка вердикта по таблице.

Первый перегон после правок дал 3 вакуумных из 19, и ни один из трёх не был находкой гейта:
`material` гонялся по релизной сборке вместо санитайзерной (он краснеет по UBSan, а в релизе
чтение за границей просто не падает), у мутанта `--target` полярность вердикта в самом перегонном
скрипте была обратной (снятый страж даёт ровно НОЛЬ — это обнаружение, а не вакуум; теперь код
возврата сверяется с базовым, измеренным до мутаций), а `vacuum` не краснел по причине из пункта 7.
Четвёртый зелёный вылез уже после правки пункта 7: мутант «мутатор ничего не правит» гейт ловит
ТЕПЕРЬ другой строкой — не «канарейка выжила», а «ни одного изменённого буфера внутрь», потому что
дохлый мутатор отдаёт одни копии семени, а они больше не считаются принятыми. Скрипт узнавал только
первую строку. Итог после всех четырёх правок вердиктов: **20 мутаций, вакуумных 0** (санитайзерная
сборка, `-fno-sanitize-recover=undefined`); пять новых мутантов закрывают ровно новые утверждения —
вакуумная цель, шов перепечатки, не вернувшийся читатель, `atoi` вместо `strtol`, неизвестное имя
цели. Мутант зависания гоняется с таймаутом, и истёкший таймаут засчитан как ВАКУУМ, а не как
красный: не сработавший сторож выглядит в точности как зависший перегон.

### Гейт 9 — третье независимое ревью (вердикт FAIL, 1 high + 2 medium + 4 low)

Ревью читало дельту после второго вердикта: всегда-перепечатку, `STATE_HASH_OFFSET`, пол глубины и
обе правки доков. Решения по двум развилкам — интервью #18, исправлены все семь находок.

13. **Пол поставлен на НЕ ту величину (high).** Страж вакуума судит все случаи цели разом —
    `cases × seeds`, — а пол стоял на одной глубине. `fuzz_readers 1 --cases 200` проходил пол,
    оставлял один seed, ронял выборку в восемь раз, и `ide-scene` краснела вакуумом на здоровом
    дереве: ровно тот ложный отказ, ради которого пол и вводился, только через другую дверь. По
    интервью пол перенесён на ПРОИЗВЕДЕНИЕ (`MIN_SAMPLE = 1600`) и считается ПОСЛЕ разбора
    аргументов — раньше обе величины ещё не известны. Отказ называет обе:
    `FAIL: sample 200 = 200 cases x 1 seeds, floor is 1600`. Мутант «пол на глубине» в таблице.
14. **Позитивный контроль на санитайзер не работал ни в одной задаче CI (medium).** Ветка
    `if fuzz_readers --selftest; then echo "the canary survived"; exit 1; fi` недостижима с
    рождения гейта: `--selftest` возвращает 1 в ОБОИХ исходах, потому что зелёного вердикта у
    самопроверки не бывает. По интервью код возврата оставлен как есть, а судит теперь СТРОКА:
    `… --selftest … || true` и `grep -q "the canary survived"`. Измерено на мутанте `canary` в
    сборке без санитайзера: `exit 1` + строка на месте, старая ветка молчит, новая краснеет.
15. **Позиционный seed не проверялся вовсе (medium).** `fuzz_readers abc` уходил в прогон seed'ом
    0, и строка PASS называла число, которого никто не просил, — повтор упавшего раннера повторял
    бы не его. Разбор теперь тот же, что у `--cases` (`errno`/`end`/хвост), мутант в таблице.
16. **Третья копия числа `8` (low).** Размер поля хеша был записан литералом в трёх циклах
    (`encode`, `decode`, перепечатка). Введена `ach::STATE_HASH_SIZE`, а вместе с ней
    `static_assert(STATE_HASH_OFFSET + STATE_HASH_SIZE <= STATE_HEADER_SIZE)`: разъедься смещение
    с размером заголовка — запись ушла бы в полезную нагрузку, и гейт доложил бы о вакууме вместо
    причины. Это единственный мутант гейта, красный на СБОРКЕ, и так и записано.
17. **Комментарий пола называл не ту конфигурацию (low).** «200 — глубина самой медленной задачи
    CI» умалчивало, что запас доказан прогоном ВОСЬМИ seed'ов на этой глубине, то есть выборкой
    1600. Формулировка теперь называет обе величины.
18. **Цель плагина обвиняла формат в отказе окружения (low).** `read_manifest` возвращала `false`,
    когда не открывался временный файл: каталог только на чтение дал бы вакуум и вердикт «ни один
    изменённый буфер не прошёл конверт» — гейт искал бы дефект в разборе манифеста. Теперь причина
    называется своим текстом и прогон кончается на месте.
19. **Шапка `preflight_stage_lib.sh` называла одну группу из трёх (low).** Список поимённый:
    неполный хуже отсутствующего, читатель решил бы, что правка общего куска задевает один файл.

Итог перегона после правок: **25 мутаций, вакуумных 0** (три новых — seed, пол выборки,
`static_assert`). Команда перегона переведена на `--cases 1600`: один seed и прежние 400 случаев
пола выборки больше не проходят, и это первое, что новый пол поймал на своём же инструменте.

### Гейт 9 — четвёртое независимое ревью (вердикт FAIL, 3 medium + 7 low + 2 taste)

Ревью читало дельту после третьего вердикта: пол выборки, греп самопроверки в обеих задачах CI,
разбор позиционного seed'а, `STATE_HASH_SIZE` и новую группу `preflight_build_rules.sh`. Развилки —
интервью #19 (четыре вопроса), исправлены все находки, включая обе `taste`.

20. **Построчные вердикты терялись на КАЖДОМ аварийном выходе (medium).** Все документированные
    вызовы гейта пайпят stdout (`| tee` в трёх шагах CI, `out=$(…)` в группе продуктов сборки), а
    на пайпе libc буферизует полностью. `_Exit` сторожа зависания, `_Exit` цели плагина и `abort()`
    санитайзера на НАСТОЯЩЕЙ находке уносили с собой строки «какие цели уже прошли и на какой встал
    прогон». По интервью — `setvbuf(stdout, nullptr, _IOLBF, 0)` первой строкой `main`. Измерено
    мутантом зависания под пайпом: со строкой — 5 вердиктов в stdout, без строки — **stdout пуст**,
    stderr в обоих случаях одинаков.
21. **Единственная недетерминированная ось гейта судилась порогом разбора (medium).** Цель плагина
    пишет случай на диск (`parse_manifest` принимает путь), и запись с удалением входили в те же
    `CASE_BUDGET_SEC = 2.0`. Заминка ФС на загруженном раннере дала бы `FAIL … 2.1s > 2.0s budget`
    там, где читатель возвращается, — мерцание ровно того сорта, против которого зашиты seed'ы. По
    интервью дисковое время вынесено за порог: `fuzz_io_span.hpp` копит секунды по областям
    видимости, а сторожа на это время ставит на ПАУЗУ и на выходе сдвигает ему дедлайн на
    измеренное (пауза — правка ревью #5, пункт 33). Мутант «порог равен нулю НА ДИСКОВОЙ цели» в
    таблице: вычет порога не отменяет.
22. **Новая группа нарушила записанный критерий, не объяснившись (medium).** `preflight.md:24`
    было единственным местом, где критерий выделения группы записан словами: «групп две, и признак
    у них один — сборки и сети не нужно». `preflight_build_rules.sh` этому признаку ПРОТИВОПОЛОЖНА
    (судит продукты сборки и требует собранного дерева), а доки продолжали описывать «три проверки
    продуктов сборки по одной». Переписано в трёх местах: `preflight.md` (обе точки), `checks.md`
    (состав этапов и абзац про группы) — групп три, признак у третьей обратный, и условие «идти
    ПОСЛЕ сборки» записано один раз шапкой группы, а не у каждого этапа.
23. **`--target ""` молча вырождался в ПОЛНЫЙ прогон (low).** Пустая строка ниже неотличима от
    «`--target` не задан», и страж `ran == 0`, поставленный ровно затем, чтобы опечатка была
    громкой, такой опечатки не видел: оператор просил одну цель, а получал десять. Отбой в разборе,
    мутант в таблице.
24. **Запись случая на диск проверяла только `open` (low).** Углубление пункта 18: короткая запись
    и неудачное закрытие возвращали бы `false`, то есть обвиняли бы ФОРМАТ в отказе окружения.
    Теперь `fwrite` и `fclose` судятся тем же `die_io`, что и открытие.
25. **`STATE_HASH_SIZE` не был связан с типом, которым поле пишется (low).** Второй
    `static_assert`: `STATE_HASH_SIZE == sizeof(uint64_t)`. Первый стережёт размещение в заголовке,
    этот — что поле по-прежнему пишется и читается одним `uint64`.
26. **Комментарий пола называл не все пропускаемые конфигурации (low).** Порог 1600 пропускает и
    `fuzz_readers <seed>` (2000 × 1). Замерено: у самой тонкой цели там 20 принятых, то есть обе
    пропускаемые конфигурации доказаны прогоном, а не рассуждением.
27. **Очередной каталог сборки появился в джобе молча (low).** Шапка `ci.yml` обосновывала
    `timeout-minutes: 60` перечнем каталогов, в который `build-fuzz-san` не входил. По интервью
    лимит оставлен прежним, а перечень переписан честно (ревью #5, пункт 35): счёт каталогов в
    самой шапке был неверен и тогда.
28. **Компромисс глушения чужого лога нигде не записан (low).** `ecs_log_set_level(-4)` гасит и
    СОБСТВЕННУЮ диагностику flecs, упади он сам. По интервью цена записана словами в файл гейта
    вместе с процедурой разбора: снять глушение руками и гнать одну цель `--target ide-scene`.
29. **Шапка `preflight_stage_lib.sh` склеивала две мысли в 140-символьной строке (low).**
    Перевёрстана: поимённый список групп — отдельным абзацем от поведенческого свойства «этапы не
    останавливают друг друга».
30. **(taste) Список форматов в файле гейта называл девять для десяти целей.** Дописаны сцена IDE и
    манифест плагина — у обоих своей магии нет, и это сказано прямо.
31. **(taste) `fuzz_manifest_*.tmp` стоял ВНУТРИ группы `*.save*`,** между `*.save.tmp` и
    `*.save.ready`. Перенесён за неё: правило чужого гейта, вклиненное в чужую группу, читается как
    её часть.

### Гейт 9 — пятое независимое ревью (вердикт FAIL, 4 medium + 8 low + 1 taste)

32. **Вердикты целей терялись на аварийном выходе, а `setvbuf` этого не чинил (medium).** Строка
    `setvbuf(stdout, nullptr, _IOLBF, 0)` объявлялась гарантией, но UCRT документирует `_IOLBF`
    как «то же, что `_IOFBF`», и при нулевом буфере требует размер от двух — на Windows вызов и не
    обязан удаться, и удавшись ничего бы не изменил. По интервью («явный fflush на всех аварийных
    путях») сброс стоит ПОСЛЕ каждой строки вердикта, перед `_Exit` сторожа и перед `_Exit`
    дисковой цели; `setvbuf` остался подпоркой под чужой `abort()` санитайзера, под который своего
    сброса не подложить. Контроль перемерен моделью Windows (строка `setvbuf` снята в ОБОИХ
    прогонах, мутант — зависший читатель под пайпом): с явными сбросами — 5 вердиктов в stdout
    (`[fuzz] bundle: cases=1600 accepts=49` первой строкой), без них — **stdout пуст**, stderr в
    обоих одинаков.
33. **Сторож сдвигался ЗАДНИМ ЧИСЛОМ, а не вставал на паузу (medium).** `watchdog::extend`
    двигал дедлайн на ВЫХОДЕ из дискового куска, то есть срок, истёкший ВНУТРИ заминки ФС, убивал
    живого читателя с текстом «the reader never returned» — ровно то событие, ради которого вычет
    и делался. По интервью («пауза/возобновление») вход в кусок поднимает `g_io_depth`, и пока он
    ненулевой, сторож молчит; выход опускает счётчик и сдвигает дедлайн на измеренное. Цена
    названа прямо в шапке: зависание ВНУТРИ дискового вызова сторож теперь не ловит — под паузой
    живёт только шов платформы, а не читатель.
34. **Признак группы «своих сборок не делая» был ложен (medium).** `check_debug_golden.sh`
    конфигурирует и собирает СВОЙ `build-debug`. Переписано в трёх местах
    (`preflight_build_rules.sh`, `preflight.md`, `checks.md`): группа судит ПРОДУКТЫ сборки и
    потому идёт после её этапов; три этапа требуют готовых `build-ci`/`build-full`, а голден Debug
    собирает свой каталог сам — иначе уровни оптимизации не сверить.
35. **Перечень каталогов в шапке `timeout-minutes: 60` был неверен (medium).** Каталогов не пять:
    на всех ОС `build`, `build-audio`, `build-ui`, `build-debug`, на Windows ещё `build-asan`, на
    Linux `build-fuzz-san` и ЕЩЁ ДВЕ полные сборки шага воспроизводимости в `/tmp`. Замером теперь
    названа только база (Linux 33 мин, macOS 12, Windows 22), а добавка фаззинга под санитайзером
    честно названа неизмеренной: её назовёт первый же прогон этой ветки, до PR.
36. **Проверка СЕМЕНИ не была под сторожем (low).** Читатель, зависший на собственном валидном
    семени цели, не ловился ни порогом (он судит вернувшееся), ни сторожем — прогон вставал тем
    самым способом, который гейт обещал заменить. Сторож вооружён и там, номер случая `-1`
    называет именно семя. Мутант в таблице: обратное условие к мутанту `watchdog`.
37. **Часы случая закрывались после бухгалтерии (low).** `spent` снимался после `disarm`,
    сравнения буферов и инкрементов, то есть порог «скорости разбора» судил заодно обвязку. Замер
    закрывается первой строкой после `t.read`.
38. **Вычет дискового времени НЕПОЛОН, и это нигде не было сказано (low).** Под вычет попадают
    запись и удаление файла, а чтение ВНУТРИ `parse_manifest` остаётся под порогом — оно и есть
    предмет цели, и заминка ФС там по-прежнему способна дать ложный `FAIL … > 2.0s budget`.
    Записано словами и в шапку `fuzz_io_span.hpp`, и в файл гейта: развести это можно только швом
    внутри чужого разбора, и такой шов не оправдан.
39. **Решение «санитайзеры по ВСЕМУ дереву, включая вендоренное» не было записано (low).**
    Читатели зовут flecs (`ide-scene`), zstd (бандл) и stb (атлас), и подавление, поставленное
    заранее, погасило бы находку ровно на нашем шве. Записано в файл гейта вместе с порядком:
    файл подавлений заводится ТОЛЬКО по настоящей находке, одной функцией и со ссылкой на
    upstream, а не маской по каталогу.
40. **`die_io` дисковой цели уходил в `_Exit` без сброса stdout (low).** Вердикты уже прошедших
    целей терялись на отказе окружения. Частный случай пункта 32, исправлен там же.
41. **Заметки называли 22 мутации при 27 в таблице (low).** Число в строке итога поправлено;
    после этого раунда мутаций 30, вакуумных 0.
42. **Файл гейта называл только пайпы (low).** Два шага CI stdout ПЕРЕНАПРАВЛЯЮТ в файл
    (`> fuzz_self.txt`), и полная буферизация там ровно та же. Формулировка исправлена.
43. **Пункт 27 этих же заметок повторял неверный счёт каталогов (low).** Исправлен вместе с
    шапкой `ci.yml`.
44. **(taste) «Переписывание мутатора со сдвигов вместо `memcpy`»** читалось наоборот: переписан
    он НА сдвиги. Исправлено.

### Гейт 9 — шестое независимое ревью (вердикт FAIL, 1 medium + 4 low)

45. **Вердикт сторожа не был атомарен относительно срока, по которому сработал (medium).** Два
    достижимых переплетения. (а) После снимка истёкшего дедлайна главный поток успевал `disarm`,
    досчитать статистику и `arm` СЛЕДУЮЩИЙ случай раньше, чем сторож прочтёт цель/seed/номер, —
    тройка назвала бы не тот случай, и команда повтора не повторила бы красный прогон. (б) Между
    снимком срока и чтением `g_io_depth` успевал целиком пройти `io_leave`: дедлайн продлён,
    глубина опущена до нуля — сторож видел ноль и обвинял ЖИВОГО читателя, только что вернувшегося
    из медленного дискового вызова, то есть ровно то мерцание, против которого пауза и вводилась.
    Комментарии обещали гарантию, которой release на дедлайне не даёт. Починено счётчиком поколения
    (`g_gen`, инкремент в `arm` и `disarm`, release) плюс перепроверкой снимка после acquire-барьера:
    сдвинулось поколение — случай перевооружён, изменился дедлайн при том же поколении — его продлил
    `io_leave`; в обоих исходах вердикта нет. Это читающая половина seqlock'а, барьер обязателен,
    иначе relaxed-чтения описания вольно опустить ниже перепроверки. Blast-radius: единственный
    вызывающий — `fuzz_run.hpp`; перепроверены мутанты «читатель не возвращается», «пауза снята»,
    «пауза снята И дисковый вызов медленный», «пара: медленный вызов, пауза на месте» — поведение
    прежнее, вся таблица прогнана заново. На перепроверку снимка заведены ДВА новых мутанта (итого
    32): «снимок дедлайна не перепроверяется» краснеет ложным `FAIL plugin-manifest seed=1 case=-1:
    the reader never returned` на ЖИВОМ читателе, а пара «тот же зонд, перепроверка на месте» —
    зелена. Гонка сама не воспроизводится, поэтому окно раскрыто зондом: сторож, увидев истёкший
    срок при ненулевой паузе, спит 1.5 с и просыпается ПОСЛЕ `io_leave` (диск 4.0 с), но ДО конца
    случая (ещё 1.9 с разбора — ниже порога 2.0, иначе краснел бы порог, а не сторож). Поколение при
    этом ТО ЖЕ: проверку поколения мутантом не покрыть, она страхует от арматуры с иным бюджетом.
50. **Мутант `ach-manifest` перестал краснеть от снятого буфера stdout (попутно, найдено прогоном).**
    В обычной сборке он краснел ПАДЕНИЕМ, то есть удачей раскладки кучи: `_IONBF` убрал 4 КиБ
    буфера, куча сдвинулась, и чтение за границей попало в отображённую память. Дефект остался тем
    же — улика была негодной. Мутанты с уликой «чтение за границей» (`bundle`, `ach-manifest`)
    судятся теперь санитайзерной сборкой `build-san`: под ASan мутант даёт
    `AddressSanitizer: BUS ... in _platform_memmove` и код -6.
46. **Комментарий перед `_Exit` сторожа противоречил соседним строкам той же функции (low).** Он
    обещал, что вердикты уносит построчная буферизация из `main`, — ровно то утверждение, которое
    опровергнул пункт 32. Переписан на явный `fflush`.
47. **Записанная дыра «под чужой `abort()` санитайзера сброса не подложить» была устранима (low).**
    UCRT соблюдает `_IONBF`, в отличие от `_IOLBF`: буфера не заводит и размер игнорирует на всех
    трёх ОС. При десятке строк вывода цена нулевая, а на НАСТОЯЩЕЙ находке ASan под MSVC вердикты
    уже прошедших целей перестают теряться. `main` ставит `_IONBF`; явные сбросы остаются, потому
    что возврат `setvbuf` по-прежнему не проверяется. Контроль сбросов перемерен под пайпом.
48. **Цена неполноты вычета была занижена (low).** Заминка ФС внутри `parse_manifest` (вне паузы
    нарочно) даёт не только ложный `FAIL … > 2.0s budget`: перевалив за
    `CASE_BUDGET_SEC + WATCHDOG_GRACE_SEC`, та же заминка даёт ВТОРОЙ, более жёсткий вердикт —
    текст сторожа с `_Exit(1)`. Дописано в обе шапки.
49. **Итог гейта в этих же заметках всё ещё называл 27 мутаций (low).** Файл гейта и пункт 41
    говорят 30, пересчёт таблицы даёт 30. Повтор дефекта пункта 43; исправлено.

### Гейт 9 — седьмое независимое ревью (вердикт FAIL, 2 medium + 3 low + 2 taste)

50. **Перепроверка поколения, добавленная шестым ревью, оказалась ВАКУУМНОЙ (medium).** `g_gen`
    рос на каждом вооружении и разоружении, но отсеять этим ничего нельзя: `g_deadline_ns`
    принимает только 0 (`disarm`) и `now_ns() + budget` (`arm`) плюс монотонные приращения
    `io_leave`, то есть последовательность строго возрастает и ненулевым значением НЕ
    ПОВТОРЯЕТСЯ. Чтобы дойти до перепроверки поколения, нужен ненулевой снимок, просроченный на
    ≥ 3 с; любой последующий `arm` даст строго больший срок. Значит строка не отсеивала ничего
    сверх перепроверки дедлайна, и мутанта на неё не существовало — заголовок «вакуумных 0» был
    ложен. Решение владельца: **дописать пишущую половину seqlock'а**. Теперь `arm` поднимает
    счётчик ДО записи описания (нечётное — «запись идёт») и ещё раз после, `disarm` прибавляет
    два, а сторож отвергает нечётное поколение ПЕРЕД чтением описания. Это и сделало проверку
    непустой: пока `arm` не дописал срок, перепроверка дедлайна полузапись пропускает по
    построению. Мутант `watchdog: a half-written case is not detected` краснеет
    `FAIL plugin-manifest seed=1 case=-1: the reader never returned` на читателе, который ещё не
    начал читать; пара `pair: the same stall AND the parity check stays` при том же зонде даёт
    `PASS targets=1 seeds=8 cases=1600 accepts=918`. Blast-radius: `g_gen`, `arm` и `disarm`
    живут только в `fuzz_watchdog.hpp`, единственный вызывающий — `fuzz_run.hpp`, вся таблица из
    34 мутантов перегнана заново.
51. **LeakSanitizer на Linux не был решён, а был умолчанием (medium).** На Linux ASan включает
    LSan по умолчанию, шаг CI судится кодом возврата (`shell: bash` = `bash -eo pipefail`), и
    утечка на ветке ошибки уронила бы гейт безымянным `N byte(s) leaked` — тем самым отказом без
    имени цели и seed'а, ради отмены которого гейт писался. С машины владельца это не проверить:
    на macOS `detect_leaks` не поддержан. Решение владельца: **узнать прогоном**, LSan оставлен
    вооружённым, решение записано строкой в файл гейта, чтобы не оставаться умолчанием.
52. **Позитивный контроль на санитайзер утверждал любое упоминание «AddressSanitizer» (low).**
    Отчёт LeakSanitizer удовлетворяет этому грепу, не сказав ничего про выход за границу. Ужесточён
    до класса ошибки: `grep -qE "AddressSanitizer: (heap-buffer-overflow|container-overflow)"` в
    обеих задачах (Linux ASan/UBSan и clang-cl на Windows). Классов два — аннотации контейнеров
    libc++/MSVC STL дают `container-overflow` там, где голый буфер дал бы `heap-buffer-overflow`.
53. **Комментарий обещал seqlock, которого у писателя не было (low).** «Поколение пишется
    последним с release, читается первым с acquire» — это только ЧИТАЮЩАЯ половина; окно закрывали
    два незаявленных инварианта (парность `arm`/`disarm` и всегда-будущий срок). Снято пунктом 50:
    писатель стал настоящим seqlock'ом, комментарии переписаны под него.
54. **`timeout-minutes: 60` не учитывал добавленного каталога (low).** Linux-нога замерена в 33
    минуты ДО этой задачи, а получила `build-fuzz-san` со своим FetchContent, санитайзерную сборку
    замыкания читателей и два прогона. Превышение дало бы голый таймаут задачи без имени цели —
    ровно то, что гейт отменяет. Решение владельца: **поднять до 90**; маскировки нет, настоящее
    зависание убивает сторож за 3 с.

Оставлено как `taste`, без правки: строка `[fuzz] PASS …` идёт без явного `fflush` (следом
`return 0`, который сбрасывает поток, и ни одного `_Exit` после), и размер `.context/gates/fuzz-readers.md`
(274 строки против 86–142 у соседей; `.md` из `line_budget.py` исключён нарочно).
