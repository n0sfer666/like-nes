# Раунд #14: каркас gameplay-фреймворка + ввод из коробки

Спека: [`.context/specs/2026-07-26-framework-input.md`](../specs/2026-07-26-framework-input.md).
Скоуп согласован с владельцем: гейты 1–7 машинно, гейт 8 (owner-HW) — «ожидание проверки от
разработчика», как в #13. Пресеты — сразу ассетами через `assetc` (решение 4 спеки), без
промежуточной итерации «сначала кодом».

DoD: гейты 1–7 зелёные на трёх ОС; пресеты и профили лежат в бандле #5; игра-образец переведена на
фреймворк, sim-хеш `0x32a094e89eacf2f2` цел; ADR 0014 в Proposed.

## Что уже есть (разведка)

- `engine/input/` — закрытая спека #4: `DeviceState` (уровни), `ActionMap::resolve` → `InputFrame`,
  контексты, линейная мёртвая зона, `capture_source` для перебиндов. Здесь **не переписывается**.
- `engine/achievements/bake.hpp` + `manifest.hpp` + баркер в `tools/assetc/bakers.cpp` — готовый
  образец «текстовый манифест → zero-parse таблица в бандле → рантайм читает из mmap». Ввод
  повторяет ровно эту схему, а не изобретает свою.
- Расписания тика в ядре нет: игра-образец зовёт системы руками из `sim.cpp`. Каркас слоя обязан
  его ввести — иначе #15–#18 сложат четыре разных порядка.
- `example_ugly_game/controls.cpp` (32 строки) — ручная проводка ввода, которую раунд удаляет.

## План коммитов

- [x] 1 каркас слоя: `engine/framework/core` (Stage + Schedule с явными зависимостями и
      детерм. порядком), правила слоя в `engine/framework/README.md`, CMake-цель `framework_core`.
- [x] 2 математика стика: радиальная и по-осевая мёртвая зона, кривая отклика, порог триггера на
      fix32 + табличный тест (гейт 3).
- [x] 3 пресеты как данные: текстовый манифест → zero-parse таблица, баркер в `assetc`, загрузка из
      mmap в `input::ActionMap` + golden разрешения ввода (гейт 1).
- [x] 4 профили геймпадов: нормализованная модель, автоопределение по VID/PID, generic-фолбэк,
      hotplug (гейты 2, 5).
- [x] 5 перебинды: сессия «нажми клавишу», конфликты, профиль игрока в `LIKENES_SAVE_DIR` (гейт 4).
- [ ] 6 перевод игры-образца на пресеты, `controls.cpp` удаляется; sim-хеш обязан совпасть.
- [ ] 7 CI: гейты 1–7 на трёх ОС, направление зависимостей грепом (гейт 6), zero-alloc (гейт 7);
      ADR 0014 (Proposed), спека закрыта частично, dev-log.
