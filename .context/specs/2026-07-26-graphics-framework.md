# Спецификация #17: Графика из коробки — спрайты, анимации, камера, частицы

- **Дата:** 2026-07-26
- **Статус:** **В работе** — вертикаль 1 (данные и тик, headless) **закрыта**: шаги A, B, C, D и E; вертикаль 2 — **закрыта**: шаги A, B, C и D; вертикаль 3 — **закрыта**: шаги A, B1, B2, B3 и C. ADR — [0017](../decisions/2026-08-30-graphics-framework.md), **Accepted**: гейт 2 (эталонный кадр)
  закрыт машинно 2026-08-31, гейт 9 закрыт владельцем 2026-09-02.
- **Наследует:** [#2](2026-07-18-render-pipeline.md) (RHI, render-graph, батчинг, bloom),
  [#5](2026-07-19-asset-pipeline.md) (атласы, BC7/basis), [#14](2026-07-26-framework-input.md)
  (правила слоя), [#16](2026-07-26-character-tilemap.md) (состояния движения → анимации, тайлмапы).
- **Уровень проверки:** **T4** — golden-кадр + golden анимационного состояния, живой прогон.

## Контекст

Спека #2 дала render-graph и доказала уровень эффектов на вертикали PoC. Но между «RHI умеет рисовать
батч квадов» и «у меня на экране анимированный персонаж, который смотрит в нужную сторону и за
которым едет камера» лежит слой, который сегодня в игре-образце написан руками (`game/art.cpp`,
`batch.cpp`, `draw.cpp`, `fx.cpp`) и потому неприменим ни к чему другому.

Этот раунд переносит найденное в игре-образце в переиспользуемый вид и достраивает то, чего в ней
нет: анимационные клипы и стейт-машину, полноценную камеру, рендер тайлмапов, data-driven частицы.

## Ключевые решения (owner)

| # | Развилка | Решение |
|---|---|---|
| 1 | Источник | **Извлечение из игры-образца, а не написание с нуля.** Код `game/` уже прошёл живой прогон; задача — обобщить и покрыть гейтами, попутно удалив дубли. |
| 2 | Анимации | **Данные:** клип = ссылки на кадры атласа + длительности + флаги (цикл, пинг-понг) + событийные метки на кадрах. Стейт-машина анимаций — тоже данные (состояния, переходы, условия). |
| 3 | Детерминизм анимаций | **Анимационное состояние живёт в тике и детерминировано** (кадр = функция от номера тика), потому что события анимации (удар попал на 4-м кадре) влияют на геймплей. Визуальная интерполяция — вне тика. |
| 4 | Частицы | **Два класса:** геймплейные (детерминированные, в тике, могут влиять на игру) и декоративные (вне тика, не влияют на sim-хеш). Разделение явное на уровне типа эмиттера, а не соглашения. |
| 5 | Камера | **Отдельная сущность с политиками** (follow, deadzone, границы уровня, тряска, параллакс, pixel-perfect), комбинируемыми, а не единственный «умный» режим. |

## Требования

### Функциональные

- **Спрайт:** компонент со ссылкой на регион атласа, тинт, флип, точка привязки, порядок
  сортировки, слой; батчинг по материалу и текстуре (переиспользует #2).
- **Атласы:** сборка атласа в `assetc` (упаковка, отступы для фильтрации), рантайм-доступ по
  стабильному имени региона без парсинга (#5).
- **Анимации:** клипы, скорость воспроизведения, циклы/пинг-понг, событийные метки на кадрах
  (доставляются игровым системам детерминированно), смена клипа с кроссфейдом там, где он
  осмыслен для 2D (тинт/альфа, не скелет).
- **Стейт-машина анимаций:** переходы по условиям из состояния движения (#16) и произвольных
  флагов игры; приоритеты, прерываемость, «докрутить до конца кадра».
- **9-slice и тайлинг спрайта** — для UI-подложек и повторяющихся текстур.
- **Тайлмап-рендер:** отрисовка чанков с culling'ом по камере, слои, параллакс-слои фона,
  анимированные тайлы.
- **Камера:** следование с ограничением скорости, мёртвая зона, look-ahead по направлению
  движения, границы уровня, тряска (детерминированная — параметризуется тиком, не `rand()`),
  зум, pixel-perfect режим (целочисленный масштаб, без дрожания субпикселей).
- **Частицы:** эмиттеры, описанные данными (форма испускания, время жизни, кривые размера/цвета/
  скорости, гравитация, burst и непрерывный режим); детерминированный генератор для геймплейного
  класса; батчинг и верхний предел числа частиц.
- **Слои и сортировка:** явный порядок отрисовки (фон/игра/эффекты/UI), стабильная сортировка
  внутри слоя — одинаковые Z не «мигают» между кадрами.
- **Отладочная отрисовка:** линии, рамки, текст поверх сцены — общий инструмент для #15/#16 и игры.
- **Игра-образец переводится на слой:** ручные `art/batch/draw/fx` удаляются, поведение сохраняется.
  **Переформулировано 2026-08-31** (разбор — ниже, «Что осталось от ручных четырёх файлов»):
  удаляется из них ФРЕЙМВОРКОВАЯ доля, а не файлы; остаток — RHI и контент, которых у слоя нет и
  по решению 1 не будет.

### Нефункциональные (инварианты)

1. **Рендер не пишет в симуляцию** (инвариант №3 архитектуры). Анимационное состояние, влияющее на
   геймплей, считается в тике; всё остальное читает состояние только на чтение.
2. **Декоративные частицы не влияют на sim-хеш** — доказывается прогоном с включёнными и
   выключенными декоративными эффектами.
3. **Zero-alloc в кадре** на горячем пути: спрайты, анимации, частицы работают на пулах.
4. **Батчинг не деградирует.** Число вызовов отрисовки на типовой сцене не растёт линейно с числом
   спрайтов — метрика замеряется гейтом.
5. **Pixel-perfect стабилен:** при целочисленном масштабе спрайты не дрожат и не «съедают» строку
   пикселей при движении камеры.

## Гейты приёмки (T4)

| # | Гейт | Критерий |
|---|---|---|
| 1 | Golden анимационного состояния | последовательность тиков → кадры + события; хеш идентичен на 3 ОС |
| 2 | **Golden-кадр** | эталонный кадр сцены (спрайты, тайлмап, частицы, камера) сравнивается попиксельно с допуском; расхождение бэкендов — в допуске #2 — **закрыт** 2026-08-31: `game_sidescroller --frames 240 --golden example_ugly_game/golden/scene_960x540.png --golden-selftest`, шаг CI на трёх ОС; эталон пиннут на Metal, допуски в `frame_golden.hpp`; расхождение бэкендов судится по размеру СВЯЗНОГО пятна (12 пикселей), а не по пику — пик на кромке спрайта равен контрасту кромки и дефект от округления растеризатора не отличает. **Тайлмап и камера в этом кадре не участвуют** — их в шутере нет; они пиннуты числами в `game_platformer_view_test` и голдене `camera-view`, и попиксельного эталона у них нет |
| 3 | Декоративность частиц | прогон с декоративными эффектами и без → sim-хеш совпадает бит-в-бит |
| 4 | Детерминизм геймплейных частиц | тот же ввод → тот же хеш состояния эмиттеров |
| 5 | Камера | эталонные траектории камеры для follow/deadzone/границ/тряски; pixel-perfect: тест на дрожание при субпиксельном движении |
| 6 | Батчинг | сцена из N спрайтов и M материалов: число draw-call'ов ≤ ожидаемого; замер зафиксирован |
| 7 | Culling тайлмапа | на большом уровне рисуются только видимые чанки; счётчик подтверждает |
| 8 | Zero-alloc | 0 аллокаций в установившемся кадре |
| 9 | Живой прогон | игра-образец на новом слое, macOS; поведение и вид не хуже прежнего — сравнение записей |
| 10 | Регресс | sim-golden `0x32a094e89eacf2f2` (или осознанно перепечатанный в #16) цел |

## Нарезка на вертикали

Гейты 2 и 9 требуют GPU и живого прогона, гейты 1, 3, 4, 5 и 10 — нет. Поэтому раунд идёт
вертикалями, а не «сначала все данные, потом весь рендер»: первая закрывается headless на трёх ОС и
не ждёт ни атласа, ни бэкенда.

Гейты **6 и 8 переехали из первой группы во вторую** на шаге A вертикали 2, и это уточнение
формулировки, а не смягчение критерия. Число draw-call'ов есть свойство РАСКЛАДКИ: бэкенд не может
добавить вызовов, которых ему не дали, поэтому «N спрайтов на M материалов стоят столько-то» меряется
счётчиком там, где вызовы и решаются. Гейт 8 закрыт на той же половине — раскладка кадра не ходит в
кучу, — а загрузка в вершинный буфер остаётся вертикали 3, где и появится. Половина, закрытая
счётчиком, названа здесь прямо, чтобы «гейт 8 закрыт» не читалось шире, чем есть.

**Счётчиков кадра на игровом пути ДВА** (2026-08-31, хвосты вертикали 3), и второй появился не для
симметрии. `framework::probe` подменяет глобальные `operator new`/`delete`, а flecs написан на C и
ходит в кучу через `ecs_os_api.malloc_` — то есть мимо него ЦЕЛИКОМ. Пока обход сущностей
(`Fx::emit_trails`) стоял ВНЕ окна счётчика «чтобы не обвинить ECS», защищало это ничего: ноль там
значил «не смотрю», а не «не аллоцирует», и половина игрового кадра инвариантом 3 не покрывалась
вовсе. Теперь окно накрывает кадр целиком, а ECS считается своим швом — тем, который flecs для
этого и держит, — с собственным позитивным контролем.

Заодно опровергнута посылка, с которой хвост брался. `world.each(лямбда)` действительно собирает
запрос ЗАНОВО на каждом вызове (`query_delegate_w_ent`: `world.query<Args...>()` прямо в
конструкторе делегата), но В КУЧУ это не ходит: измерено тем же счётчиком — первый мир с запросом
стоит 2939 аллокаций, следующие ДВЕСТИ сборок запроса на прогретом мире — ноль, flecs отдаёт их из
своих пулов. Запрос всё равно строится теперь один раз рядом с миром (`make_trail_query`), но
куплено этим лишнее ПОСТРОЕНИЕ, а не ходка в кучу, и записано это здесь именно так.

**Гейт 7 переехал туда же** на шаге B, и переезд этот исправляет ошибку группировки, а не критерий:
собственная формулировка гейта — «счётчик подтверждает», то есть он с самого начала был утверждением
об ОБХОДЕ, а не о картинке, и GPU для него не нужен вовсе. Слово «чанки» в формулировке читается
шире, чем следует: раскладка сетки плоская и плотная (решение владельца 2026-08-23 по спеке #16), и
стоимость держится не ею, а окном запроса, — поэтому подтверждается ровно то, что гейт и просит:
число обойдённых тайлов не следует за размером уровня.

**Вертикаль 1 — данные и тик (headless).** Гейты 1 и 5.

| шаг | что | состояние |
|---|---|---|
| A | клипы: шкала времени, циклы/пинг-понг, скорость дробью, событийные метки | **закрыт** — `engine/framework/graphics/`, голден `anim-state hash = 0x8db6e6730e0c446f`, 13 сломанных реализаций отбиты |
| B | стейт-машина анимаций: состояния и переходы данными, условия разрядами, приоритеты, прерываемость, «докрутить до конца кадра» | **закрыт** — `machine.cpp`, два гейта по классу отказа, 13 сломанных реализаций отбиты |
| C | камера с политиками: follow, deadzone, look-ahead, границы, детерминированная тряска, параллакс, pixel-perfect + эталонные траектории (гейт 5) | **закрыт** — `camera.cpp`, четыре гейта по классу отказа, голдены `camera-path hash = 0x15028037bdca9f22` и `camera-view hash = 0xd6c2f15de2ab50f8`, 20 сломанных реализаций отбиты |
| D | регионы атласа по стабильному имени: zero-parse секция бандла и пекарь (по образцу `map_format`/`map_bake`/`map_read`) | **закрыт** — `atlas_format.hpp`/`atlas_parse.cpp`/`atlas_bake.cpp`/`atlas_read.cpp`, секция `atlas_regions` в `game.bundle` (`bundle_hash = 0x1a557ae839e76ea0`), голден таблицы `hash 0xa9b9b74a0152af52`, четыре гейта по классу отказа, 28 сломанных реализаций отбиты |
| E | отладочная отрисовка: чистый построитель примитивов линия/рамка/текст → квады; отправка в GPU остаётся в игре-образце | **закрыт** — `debug_draw.cpp`, голден `debug-overlay hash = 0x922ccf9cf4b0aecc`, два гейта по классу отказа, 28 сломанных реализаций отбиты |

Шаг A выбрал целые числа, а не `fix32`: длительность кадра меряется тиками, скорость — целой дробью
(`elapsed * rate_num / rate_den` в `uint64`), потому что событийные метки входят в sim-хеш, а
накопление дробной ошибки разъехалось бы между x86 и ARM. Кадр берётся ФУНКЦИЕЙ от номера тика —
поэтому перемотка и проигрывание дают одно и то же, и это утверждение стоит тестом. Клип не владеет
кадрами: он смотрит в чужую память, чтобы шаг D мог указать его прямо в mmap-секцию бандла.

Шаг C сделал ПОРЯДОК политик утверждением, а не комментарием: границы режут «куда» ДО потолка
скорости, иначе потолок считает потраченной ту часть шага, которую границы потом срежут, и камера у
края уровня отстаёт вдоль свободной оси. Второй срез, после шага, оставлен ради безусловного «центр
внутри границ». Тряска и привязка к сетке в `Camera` не пишут вовсе — они живут в
`camera_layer_center`, и это инвариант 1 спеки на самом соблазнительном для нарушения месте.

Шаг D сделал стабильным ИМЯ, а не номер: клип держит в кадре `RegionId` — номер ОБЪЯВЛЕНИЯ региона
в `atlas.txt`, — а имя ведёт к этому номеру через таблицу. Перепаковка страницы меняет пиксельные
координаты и не трогает ни имя, ни номер; сортировка регионов внутри пекаря сломала бы ровно это,
поэтому «имя ведёт к порядку объявления» стоит утверждением, а не свойством реализации. Потолок
`RegionId` (65536 регионов) читатель отбивает сам, и гейт на него собирает секцию РУКАМИ целиком
законной: испорченная испечённая закрывается проверкой границ блоков раньше, и утверждение о потолке
осталось бы вакуумным.

Guid секции — `atlas_regions`, а не `atlas`: имя `atlas` в бандле игры-образца уже занято САМОЙ
текстурой, и совпадение guid означало бы, что вторая запись молча вытесняет первую. Полтексела
отступа в таблицу НЕ заложены намеренно: отступ от края — приём сэмплера, он зависит от фильтрации
и мипа; таблица говорит, ГДЕ картинка лежит, а UV сдвигает тот, кто их считает. Перекрытие регионов
не отвергается — 9-slice и псевдонимы суть законные записи. Многостраничность не заводилась: у неё
сегодня нет потребителя, и контракт вышел бы вслепую.

У игры-образца регионы объявлены ДВАЖДЫ — в `assets/atlas.txt` и в `game::set_regions()` (`art.cpp`),
и оба источника живут, пока рендер не переехал на слой (вертикаль 3). Расхождение между ними не
видит ни `--verify-game`, ни сверка секции читателем: обе судят бандл. Поэтому заведён
`game_atlas_regions_test` — он снимает полтексела `game::rgn` и сверяет числа.

Шаг E не рисует, а СТРОИТ: на выходе поток квадов, и отправка их в GPU остаётся в игре-образце —
иначе headless-вертикаль потянула бы за собой RHI. Поворот записан ЕДИНИЧНЫМ НАПРАВЛЕНИЕМ, а не
углом: тригонометрии в целочисленной арифметике у нас нет вовсе (`fixmath.hpp` даёт корень,
нормализацию и скалярное, но не atan2), а потребителю всё равно нужна матрица, то есть пара
косинус-синус, — угол по дороге к ней был бы лишним преобразованием, теряющим точность дважды.
Числа — `fix32` по той же причине, что у камеры: поток квадов свёрнут в голден, который обязан
совпасть на трёх ОС.

Буфер под квады даёт ВЫЗЫВАЮЩИЙ (гейт 8, zero-alloc), и переполнение поэтому СЧИТАЕТСЯ, а не
молчит: оверлей, не поместившийся в буфер, снаружи выглядит ровно как оверлей, которого не
рисовали, — и отличить их можно только числом `dropped()`. Тем же счётчиком считается и глиф без
региона: причины разные, следствие одно — пустой экран. Рамка кладёт углы РОВНО ОДИН раз
(горизонтали во всю ширину, вертикали укороченные): под альфой двойное покрытие видно глазом, и
«углы ярче сторон» было бы дефектом, а не мелочью. Шрифт шаг E ищет ПО ИМЕНИ через таблицу шага D
(`solid`, `digit_0..9`, `letter_a..z`) — он первый её потребитель, и атлас без шрифта отбивается
`has_text`, а не рисует мусор.

**Зум перенесён в вертикаль 2** и это не сокращение скоупа молчанием: зум — соглашение о том,
сколько экранных пикселей приходится на мировую единицу, и написать его без потребителя-рендера
значит написать контракт вслепую. Пиксельная сетка при нецелом зуме перестаёт быть сеткой, и какой
именно из двух шагов (мировой или экранный) остаётся целым — вопрос к бэкенду, а не к камере.

**Вертикаль 2 — рендер (GPU).** Спрайты и батчинг, тайлмап с culling'ом, слои и сортировка,
9-slice, частицы обоих классов, зум камеры. Гейты 2, 3, 4, 6, 7, 8.

| шаг | что | состояние |
|---|---|---|
| A | спрайты, слои и сортировка, батчинг по материалу, 9-slice — чистая раскладка без RHI | **закрыт** — `sprite.cpp`/`nine_slice.cpp`, голден `sprite-batch hash = 0xbda7b692a87b1155`, гейт 6 числом (`batches = 13, sprites = 73`), гейт 8 на половине раскладки (`allocs: 119 frames = 0`; загрузка в буфер — вертикаль 3), три гейта по классу отказа, 34 сломанных реализации отбиты |
| B | тайлмап с culling'ом: окно вида → спрайты тайлов | **закрыт** — `tile_draw.cpp`, голден `tile-draw hash = 0x599044fa7aabfddc`, гейт 7 числом на двух картах (`visited: 40x30 map = 336, 1200x30 map = 336`), два гейта по классу отказа, 21 сломанная реализация отбита |
| C | частицы обоих классов: игровые (в тике) и декоративные (вне тика) | **закрыт** — `particles.cpp`, голден `particle-sim hash = 0xab81c90241aaa65a` в Release и Debug, гейт 3 числами трёх расписаний (`decor: 0 fps = 0 alive, 1 fps = 374 alive, 5 fps = 1540 alive`), гейт 8 продлён на оба класса (`particles alive: sim 152, decor 127`), три гейта по классу отказа, 36 сломанных реализаций отбито |
| D | зум камеры: развёртка мира в экран, пиксельная сетка при нецелом зуме | **закрыт** — `viewport.cpp`, голден `viewport hash = 0x5bcea5ecad651b07` в Release и Debug, пиксельная сетка числом (`snap: 64 substeps advanced 16 pixels`), зум доезжает до culling'а и границ уровня (`culling: zoom 2 visits 18, zoom 1/2 visits 273`), гейт по классу отказа, 30 сломанных реализаций отбито |

Шаг A обобщил ВЫРОЖДЕННЫЙ случай игры-образца, а не заменил его: у неё один атлас, один материал и
порядок отрисовки, равный порядку подачи (`SpriteBatch` в `example_ugly_game/batch.cpp` — один буфер
инстансов и один draw-call), поэтому «список из одного материала даёт РОВНО один батч» стоит
утверждением. Гейты 6 и 8 закрыты здесь СЧЁТЧИКАМИ на трёх ОС, а не картинкой: раскладка не знает
про RHI ни одной строкой, и число draw-call'ов — свойство именно её.

Ключ сортировки полон: слой (16 бит), материал (16), НОМЕР ПОДАЧИ (32). Третье поле несущее.
`std::stable_sort` выделяет буфер под слияние, то есть ходит в кучу прямо там, где гейт 8 требует
нуля; `std::sort` при равных ключах даёт порядок, зависящий от реализации, — то есть РАЗНУЮ картинку
на трёх ОС. Стабильность здесь сделана свойством ключа, а не алгоритма, и голден это проверяет.
Знаковый слой смещается в беззнаковый диапазон до сдвига: иначе фон уезжал бы поверх персонажа ровно
на отрицательных слоях.

Батч рвётся по смене МАТЕРИАЛА, а не слоя. Слои уже развели спрайты в порядке, поэтому два соседних
отрезка одного материала из разных слоёв рисуются одним вызовом, ничего не переставляя: граница, за
которой картинка не меняется, не стоит draw-call'а. Слой в материал поэтому и не входит.

9-slice задан ДЕВЯТЬЮ РЕГИОНАМИ, а не одним с отступами: спека шага D прямо называет перекрытие
регионов законной записью, то есть панель нарезается именами в `atlas.txt`. Альтернатива — подрезать
UV одного региона внутри слоя — потребовала бы, чтобы слой считал UV, а он их не считает нигде.
`corner` задан в МИРОВЫХ единицах: сколько экранных пикселей приходится на мировую единицу — это
зум, и он переносится в отдельный шаг этой же вертикали. Угол больше половины панели урезается, а
вырожденный кусок ПРОПУСКАЕТСЯ, а не считается потерей, — то же различение, что у рамки шага E.

Шаг B — ШОВ между тайлами и графикой, и живёт он отдельной целью (`framework_graphics_tiles`), а не
файлом в `framework_graphics`: сетка тянет за собой физику, и затащи её в саму библиотеку — клипы,
машина и камера линковали бы решатель, не спросив ни одного тайла. Тот же довод, по которому RHI нет
среди зависимостей шага A.

Окно ЗАИМСТВУЕТСЯ у `TileGrid::window`, а не считается заново. Второе вычисление было бы вторым
КОНТРАКТОМ, расходящимся с первым на округлении, и расхождение это проявлялось бы не «дрожанием
края», а тем, что нарисованный тайл и тайл, о который бьётся персонаж, — разные тайлы. Плата за
заимствование названа гейтом: окно сетки включает правый и нижний края (зонд впритык к стене обязан
стену видеть), поэтому вид, кончающийся ровно на границе тайла, рисует и её — лишний ряд квадов за
краем экрана. Ряд квадов дешевле второго контракта.

Гейт 7 утверждается ЧИСЛОМ ОБОЙДЁННЫХ тайлов на двух картах — 40x30 и 1200x30 — под одним видом:
«цена кадра не следует за размером уровня» это утверждение об обходе, а не о картинке, и число
спрайтов на него не отвечает (пустая карта даёт ноль при любом окне). Контроля два, и без них
равенство зелено вакуумно: вид обязан что-то накрывать, а большая карта — быть заведомо больше окна.

Тайл отображается в регион по СВОИМ ЖЕ БИТАМ КОЛЛИЗИИ, потому что больше сетка ничего не несёт.
Отдельный визуальный индекс на тайл — правка испечённого формата карты, и заводить её без
потребителя значит писать контракт вслепую (то же основание, по которому зум перенесён из вертикали
1). Регион `0` означает «не рисовать»: пустой тайл иначе занимал бы слот батча прозрачным квадом.
Биты выше таблицы — не отказ карты, а карта новее движка: такой тайл СЧИТАЕТСЯ отдельным счётчиком
и пропускается, потому что «тайлы пропали» обязано быть отличимо от «их там не было».

Шаг C делит частицы НА УРОВНЕ ТИПА, а не соглашения (решение владельца 4): у `GameplayEmitter` есть
`step()` и нет `advance()`, у `DecorEmitter` — наоборот, поэтому «декоративный эмиттер случайно
подвинули тиком» не компилируется. Проверяется это четырьмя `static_assert` на концептах, и ДВА из
них отрицательные: без них один класс с флагом прошёл бы гейт целиком.

Интегратор у обоих классов ОДИН. Вторая реализация была бы вторым КОНТРАКТОМ, расходящимся с первым
на округлении, — то же основание, по которому окно шага B заимствуется у сетки. Разводит классы не
арифметика, а ПОТОК и ЧАСЫ: декоративный эмиттер считает свой шаг долей тика и живёт вне свёртки,
поэтому число у него тоже `fix32` — декоративность это другой поток, а не другой тип числа. Что
интегратор действительно один, стоит утверждением: тридцать тиков игрового класса совпадают
бит-в-бит с тридцатью `advance(1)` декоративного.

Затухание берётся ЛИНЕЙНО по доле шага (`1 - (1-k)*dt`), а не дробной степенью: степени в
целочисленной арифметике нет, а при `dt = 1` формула точна, то есть игровой класс не платит за
существование декоративного ничем.

Поток случайных чисел НЕ СЛЕДУЕТ ЗА РАЗМЕРОМ ПУЛА: числа тянутся до проверки вместимости, иначе
переполнение пула молча меняло бы траекторию всех последующих частиц, и голден ловил бы размер
буфера вместо арифметики. Пинится это парой пулов 16 и 1024 с одинаковым `stream()` и разной
потерей. Уплотнение — СДВИГОМ, а не обменом с последним, как делает образец (`example_ugly_game/fx.cpp`):
номер подачи входит в ключ сортировки шага A, и обмен мигал бы порядком полупрозрачных частиц одного
слоя между кадрами.

Гейт 3 сшит так, чтобы совпадение хеша не могло быть вакуумным: свёртка sim-прогона ЧИТАЕТ поток
игрового эмиттера каждый тик, три расписания кадров (0, 1 и 5 на тик) обязаны дать один хеш, и
рядом стоят контроли — что недекорированный прогон не нарисовал ничего, а декорированные
действительно работали.

Шаг D положил зум в `Viewport`, а НЕ в `CameraConfig`, и это решение об инварианте 1, а не о
раскладке полей. `camera_follow` пишет `Camera::center`, то есть камера стоит на пути состояния;
поле зума рядом с ней пустило бы разрешение экрана в тик, и «поменял размер окна — разъехался
sim-хеш» стало бы законным поведением. Развёртка читает камеру и не пишет в неё ни строкой.

Открытый вопрос вертикали 1 — какой из двух шагов, мировой или экранный, остаётся целым при нецелом
зуме — закрыт в пользу ЭКРАННОГО: бэкенд получает пиксели, поэтому привязка берётся после перевода
координат, делением с округлением ВНИЗ, а не усечением к нулю. Разница между ними видна только при
переходе через экранный ноль, и сломанная реализация с усечением пережила первую версию гейта: та
гоняла развёртку возле центра экрана, где знак не меняется. Пиксельная сетка стоит поэтому не
словом «не дрожит», а числом — 64 субшага обязаны продвинуть картинку ровно на 16 пикселей, монотонно
и без пропусков: мёртвая привязка не дрожит тоже.

Половина видимой области считается ОДНИМ числом (`viewport_half_world`) и его же получают culling
тайлмапа и `CameraConfig::half_view`. Зум, застрявший в переводе координат, рисует верную картинку в
окне прежнего размера — то есть даёт обычный PASS, — поэтому у связки собственная цель гейта: она
утверждает, что уменьшение зума вчетверо расширяет обход culling'а и отпускает камеру дальше к краю
уровня.

Отказа «масштаб схлопнулся в ноль» здесь нет намеренно: `zoom * pixels_per_unit` при целом
`pixels_per_unit ≥ 1` не бывает меньше уже проверенного положительного зума, и проверка, которую
нечем провалить, — декорация (тот же счёт, что закрыл лишние проверки шагов B и C). Порядок отказов
при этом сам стал контрактом: развёртка с тремя одновременными ошибками называет масштаб рисунка, а
не зум, потому что первый — свойство СБОРКИ, а второй — момента.

**Вертикаль 3 — перевод игры-образца.** Ручные `art/batch/draw/fx` удаляются. Гейты 9 и 10.

### Что осталось от ручных четырёх файлов (2026-08-31, хвосты)

Требование «удаляются» проверено пофайлово и оказалось **неверным как написано**. Фреймворковая
доля из всех четырёх ушла: частицы — в `DecorEmitter`, накопитель инстансов — в `InstanceStage`,
раскладка спрайтов со слоями — в `SpriteList` (её взял платформер), нарезка атласа — в
`atlas_regions.cpp`, читающий секцию бандла. Осталось ровно то, чего у слоя нет:

| файл | что осталось | почему это не дубль |
|---|---|---|
| `art.cpp` | процедурная печать пикселей (корабль, звезда, враг, босс, шрифт) + `set_regions` | пекаря картинок у фреймворка нет вовсе; вторая копия нарезки — ОСОЗНАННАЯ (у процедурного пути своя картинка) и держится гейтом `game_atlas_regions_test`, названным анти-дрейфом прямо в своём заголовке |
| `batch.cpp` | владение устройством, конвейером, текстурой и буферами WebGPU | решение 1: слой — РАСКЛАДКА, а не рисование, и RHI в нём не будет; раскладочная половина файла уже вынута — `InstanceStage` стоит его полем |
| `draw.cpp` | подача сцены/HUD/экранов и спрайт-текст шутера | это КОМПОЗИЦИЯ КОНКРЕТНОЙ ИГРЫ поверх её же мира flecs; текстового слоя у фреймворка нет |
| `fx.cpp` | восемь описаний эффектов игры и разбор её событий боя | таблица `EmitDesc` — данные игры, а не механизм; механизм переехал целиком |

Один настоящий дубль в разборе нашёлся и **удалён**: `begin_clear` стоял двумя копиями (в
`draw.cpp` и в `platformer_live.cpp`), отличавшимися ОДНИМ числом — цветом очистки. Он переехал в
`batch.cpp` с цветом-параметром; `draw.hpp` его больше не объявляет, и платформер перестал тянуть
свою копию. Это ровно тот случай из правила 5, который не выкупается длиной: одинаковые блоки,
разные на пару значений, — пропущенная абстракция.

| шаг | что | состояние |
|---|---|---|
| A | платформер на фреймворке: камера, окно вида, отрисовка тайлов и порядок — вместо ручных `camera_at`/`build_quads` | **закрыт** — `example_ugly_game/platformer_view.cpp` стоит на `camera.cpp`/`viewport.cpp`/`tile_draw.cpp`/`sprite.cpp`, гейт 10 числом (`platformer trajectory hash = 0xfead7a87477a9258` не двинулся), `game_platformer_view_test` на трёх ОС, пять сломанных реализаций отбито |
| B1 | нарезка шутера: бейкнутый путь читает секцию `atlas_regions` по именам вместо зашитой копии | **закрыт** — `example_ugly_game/atlas_regions.cpp`, `game_atlas_regions_test` несёт два утверждения вместо одного, шесть сломанных реализаций отбито |
| B2 | нумерация нарезки: спрайт фреймворка адресует регион ЧИСЛОМ, и список имён при этом остаётся один | **закрыт** — `RegionSlot`/`region_name`/`region_at` в `example_ugly_game/atlas_regions.cpp`, `regions_from_table` и гейт стоят на нём, три сломанные реализации отбиты |
| B3 | шутер на фреймворке: `draw`/`fx` → оверлей и частицы фреймворка; загрузка в вершинный буфер и гейт 8 на ней | **закрыт** — `fx.cpp` стоит на `DecorEmitter` с пулами-массивами, шов `sprite_out.cpp` и накопитель `instance_stage.cpp`; голден `shooter particle hash = 0xfd9ca7d2936ad48a`, гейт 8 на ВСЁМ игровом кадре двумя счётчиками (`operator new` и `ecs_os_api`), девять сломанных реализаций отбито |
| C | гейт 9: записи живого прогона до и после перевода, сверяет владелец | **закрыт** 2026-09-02 (Windows, MSVC 14.44, Release) — `docs/owner-verification.md` §7, обе половины со своими «до» (`2bdfcb7` для платформера, `ddd0efa` для шутера). Платформер: вопросы 2–5 поимённо, вопрос 1 общим вердиктом. Шутер: «две визуально одинаковые игры» — ожидаемый ответ, потому что все видимые числа перенесены без изменений, а три заявленных расхождения ниже порога глаза; вопрос 6 закрыт числом `fx: peak 136 of 512, dropped 0`. Провенанс пары доказан контролем `--golden-selftest`, которого сборка `ddd0efa` не знает |

Платформер идёт ПЕРВЫМ, потому что он единственный из двух образцов проверяется без GPU: чистая
половина (`platformer_view.cpp`) отделена от окна ещё спекой #16, и её гейт гоняется на трёх ОС.
Шутер этого не даст — его отрисовка сидит прямо в `batch.cpp`/`draw.cpp`, и «стало не хуже» там
приходится смотреть глазами, то есть ждать владельца. Перевод, начатый с той половины, где ответ
даёт число, вскрывает расхождения контрактов раньше, чем они доедут до картинки.

**Фреймворк дополнен тремя полями, и все три пришли из игры, а не из головы.** `EmitDesc` получил
`life_jitter`, `half_jitter` и `spawn_half`; `Particle` — личные `life` и `scale`, которые они
пишут. Старый float-`fx.cpp` раздавал каждой частице свою длительность (`0.28 + 0.55u` секунды) и
свой размер (`0.6 + 0.7u` от базового), и взрыв без этого умирает одним диском: все частицы гаснут
в один тик. Третье поле — ФОРМА ИСПУСКАНИЯ: выхлоп корабля выходил полосой в десять пикселей
поперёк, а конус, который умел эмиттер вертикали 2, в точке испускания имеет нулевую ширину и
полосу не даёт в принципе. Разброс, равный нулю, НЕ БЕРЁТ У ГЕНЕРАТОРА НИ ОДНОГО ЧИСЛА — иначе
поток случайных чисел сдвинулся бы у всех, кто этих полей не просил, и голден вертикали 2 покраснел
бы на добавлении поля. Он не покраснел: `particle-sim hash = 0xab81c90241aaa65a` тот же.

**Яркость выше единицы стала свойством МАТЕРИАЛА, а не спрайта.** Взрывы шутера светятся: старый
`fx.cpp` умножал цвет на `1.9f` перед записью в инстанс, потому что цель у него HDR. `Sprite::rgba`
— восемь бит на канал, и втащить в него множитель нечем; расширять его до float значило бы платить
HDR-ом за каждый спрайт каждой игры ради одного эффекта одной. Поэтому экспозиция живёт таблицей
по номеру материала (`MAT_Glow` = 1.9) и применяется в шве `sprite_out.cpp` — там же, где
полуразмер разворачивается в полный, а номер региона в UV. Фреймворк порядок байт в `rgba` не
трактует вовсе (`lerp_rgba` симметричен по каналам), поэтому соглашение `0xRRGGBBAA` записано
ТАМ ЖЕ, в потребителе: незаписанное, оно каждым следующим шовом было бы написано заново.

**Личный поворот частицы снят, и это оплачено, а не решено молча.** Старый эмиттер держал у каждой
частицы угол и крутил её. Частица рисуется звездой — радиальным градиентом `pow(1 - d/r, 1.8)` в
квадрате 64×64, — и повёрнутая звезда неотличима от неповёрнутой. Утверждение проверяемо, и
`game_fx_test` его проверяет: альфа обязана совпасть с собой под отражениями по обеим осям И под
транспонированием. Третьего мало не бывает — четырёхлучевая «искра» переживает оба отражения и
разъезжается только на транспонировании; ровно такой мутант (звезда-эллипс) отбит гейтом при нулевом
расхождении по отражениям.

**Накопитель кадра отделён от загрузки в буфер** (`InstanceStage`), и отделён вынужденно: гейт 8
требовал утверждения НА ЗАГРУЗКЕ, а `SpriteBatch` открывает устройство, текстуру и конвейер, то есть
гейт на нём был бы гейтом на видеокарте раннера, которой там нет. Разделённые, они дают headless
утверждение о том, что кадр не растит буфер, отказывает по ёмкости и считает отказы. Память
буфера — у вызывающего, как у `ParticleStore` и `SpriteList`; `SpriteBatch` выделяет её один раз в
`init` и на стек не кладёт: сто килобайт в кадре `run()` — это десятая часть стека главного потока
на Windows.

**Гейт 9 разошёлся на две половины, потому что у них РАЗНЫЙ вопрос.** У платформера вопрос
буквальный — кадр обязан совпасть, потому что переехали камера, окно и порядок, а числа остались те
же. У шутера кадр совпасть НЕ обязан и не совпадёт: случайные числа переехали с float-LCG на
`fix32`, трение теперь применяется до шага, а не после, и корреляция скорости выхлопа с местом
рождения в полосе заменена конусом. Отправить владельца искать «то же самое» там, где траектории
заведомо другие, значит получить назад список из трёх ложных находок — поэтому §7
`docs/owner-verification.md` называет все три расхождения ПЕРВЫМ делом, до команд сборки, и
спрашивает не про совпадение кадров, а про характер: цвета, размах, разброс времён жизни, две волны
взрыва босса в правильном порядке скоростей и видимая яркость `MAT_Glow`. Шестой вопрос —
единственный, на который headless-гейт ответить не может в принципе: скриптованный бой пул не
переполняет, а живой бой владельца — это другой бой, и `Fx::dropped()` считает ровно этот случай,
но пока никем не печатается.

Обобщать оказалось нечего в трёх местах из четырёх: кламп камеры к краю уровня, окно culling'а и
сортировка были в образце ВТОРОЙ реализацией того же контракта, и удаление их — вся работа шага.
Четвёртое место — склон — во фреймворк не переехало и не должно: он рисуется ступенькой подквадов по
той же `physics::Shape`, которую решатель подставляет под ноги, а знание о формах тайлов в отрисовке
означало бы отрисовку, зависящую от физики. Поэтому склоны идут ВТОРЫМ проходом по тому же окну, и
цена этого названа числом: окно — 22×17 тайлов при любом размере карты.

Тон тайла стал ТАБЛИЦЕЙ НА КЛАСС (`TileSet::tint`, ноль значит «взять общий»), а не вторым проходом
с другим набором: односторонняя площадка отличается от стены ровно тем, что сквозь неё проходят
снизу, и покрашенная как стена она врёт игроку именно про это. Второй проход сказал бы то же самое
ценой второго обхода окна — то есть ценой числа, которым меряется гейт 7. Цвет при переезде стал
упакованным `uint32_t`: столько его и несёт спрайт фреймворка, а обратный перевод в доли единицы
точным не был бы — `0.30f` в восьми разрядах не представим, и сравнение в гейте разъехалось бы.

Нарезка шутера была ВТОРОЙ копией `assets/atlas.txt`, зашитой в `game::set_regions()`, и жила она на
БЕЙКНУТОМ пути: пиксели приезжали из бандла через транскод, а UV к ним приписывались руками. Разъезд
такой пары не даёт ни ошибки, ни отказа — он виден глазами и только на том спрайте, по которому
попали. Шаг B1 отдаёт бейкнутому пути таблицу целиком, вместе с размером страницы: зашитые 512x256
были вторым утверждением о ней, и перепаковка атласа сдвинула бы все UV разом.

Процедурный fallback `set_regions()` при этом ОСТАЁТСЯ, и это не недоделанный переезд: он рисует
пиксели сам, по тем же координатам, что и называет, — его нарезка есть свойство его собственной
картинки, и брать её из таблицы значило бы разрешить таблице врать про то, что нарисовано в коде.
Поэтому источников по-прежнему два, но вопрос гейта сменился: раньше он спрашивал, совпадают ли две
копии, теперь — совпадает ли ПРОЦЕДУРНАЯ картинка с таблицей и идёт ли бейкнутая ИЗ ТАБЛИЦЫ. Второе
первым не заменяется: пока числа совпадают, путь, читающий код вместо таблицы, отвечает то же самое,
— поэтому подмена таблицы в гейте несёт сдвинутый на восемь пикселей корабль и вдвое большую
страницу.

Сверка идёт по ВСЕМ регионам, а не по одному, и это находка гейта, а не осторожность: первая версия
проверяла бейкнутую нарезку только на `ship`, и перепутанные местами ширина с высотой прошли её
молча — `ship` квадратный. Отказ у нарезки ЦЕЛЫЙ: таблица без половины имён оставила бы остальные
регионы прежними, то есть смешала бы два источника в один атлас, худший из трёх возможных.

Спрайт фреймворка адресует картинку ЧИСЛОМ (`Sprite::region`), а не полем структуры, поэтому у
нарезки шутера появился ПОРЯДОК — и вот тут второй список имён завёлся бы сам собой. Шаг B2 его не
допускает: имена выписаны одним массивом, `RegionSlot` нумерует его же, а `regions_from_table` и
гейт читают нарезку через ту же пару `region_name`/`region_at`. Номер ноль означает «не рисовать» —
соглашение фреймворка, — поэтому нумерация начинается с единицы.

Утверждение «номер адресует то поле, что обещает имя» стоит в гейте ОТДЕЛЬНО от сверки с таблицей, и
это не перестраховка: цикл сверки и пишет, и читает через одну и ту же нумерацию, поэтому
СОГЛАСОВАННАЯ перестановка внутри неё проходит его молча — сломанная реализация со звездой и врагом
местами это показала. Ловит её то, что вся остальная игра берёт поля по имени (`atlas.star` в
`draw.cpp`): перестановка нарисовала бы врагов звёздами, а гейт сверял бы её саму с собой.

Дальний угол окна отрисовки — ПОСЛЕДНЯЯ ВИДИМАЯ точка, а не первая невидимая. `TileGrid::window`
включает правый край намеренно (зонд, поставленный впритык к стене, обязан стену найти), но столбец,
начинающийся ровно на краю экрана, не виден ни одним пикселем: поданный, он был бы занятым слотом
батча под квадом за экраном. Утверждение стоит в гейте отдельной строкой, и полная коробка вида его
валит.

Порядок отрисовки задан СЛОЯМИ, а не порядком вызовов сборки, и это тоже стоит утверждением: везомый
стоит на крыше платформы, и плита, нарисованная после него, прятала бы его подошвы ровно в тот
момент, ради которого она в уровне есть. Сломанная реализация «персонаж и платформа на одном слое»
гейтом отбита — порядок подачи их разводит не так.

## Не входит в скоуп

- Материалы, шейдерные эффекты, освещение и нормали — **#18**.
- Скелетная анимация и деформация мешей: 2D-кадровая анимация закрывает целевой класс игр;
  скелет — отдельная спека при необходимости.
- UI-фреймворк (виджеты, вёрстка, фокус): здесь только примитивы отрисовки. IDE-панели живут на
  ImGui (#7) и к игровому UI отношения не имеют.
- Редактор анимаций и тюнер частиц как панели IDE — кандидаты в first-party плагины отдельной
  спекой (граница «горячий путь — в ядре, авторинг — в плагине»).

## Риски

| Риск | Митигация |
|---|---|
| Golden-кадр хрупок: драйверы дают разный результат | Допуск и метрика расхождения переиспользуются из #2, где вопрос уже решён; сравнение по перцептивной метрике, а не побайтово |
| Анимации в тике удорожают симуляцию | В тике живёт только то, что влияет на геймплей (номер кадра, события); интерполяция и визуальные эффекты — вне тика |
| Обобщение кода игры-образца поменяет её вид незаметно | Гейт 9 сравнивает записи до/после; гейт 2 фиксирует эталонный кадр |
| Частицы «незаметно» станут геймплейными | Класс эмиттера — часть типа и ассета; гейт 3 ловит нарушение немедленно |
